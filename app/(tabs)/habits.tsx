/**
 * Habits tab - Habit tracking placeholder (Phase 3).
 */
import React from 'react';
import { PlaceholderScreen } from '../../src/components/ui/PlaceholderScreen';

export default function HabitsScreen() {
  return (
    <PlaceholderScreen
      tabName="Habits"
      phase={3}
      description="Habit tracking coming in Phase 3"
    />
  );
}
