---
phase: 06-onboarding-profile-and-notifications
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/stores/settingsStore.ts
  - src/domain/niyyah-options.ts
  - src/domain/starter-packs.ts
  - src/domain/notification-copy.ts
  - src/services/notification-service.ts
  - __tests__/domain/niyyah-options.test.ts
  - __tests__/domain/starter-packs.test.ts
  - __tests__/domain/notification-copy.test.ts
  - __tests__/services/notification-service.test.ts
  - app.json
  - package.json
autonomous: true
requirements:
  - ONBR-01
  - ONBR-02
  - NOTF-01
  - NOTF-02
  - NOTF-03
  - NOTF-04
  - PROF-02

must_haves:
  truths:
    - "Niyyah options filter correctly by Hijri month (seasonal options appear only in relevant months)"
    - "Starter pack bundles contain valid preset habit IDs that exist in the preset library"
    - "All notification copy is invitational — zero instances of guilt language"
    - "NotificationService can schedule prayer reminders and Muhasabah daily trigger"
    - "settingsStore has onboardingComplete flag, notification preferences, and Arabic toggle"
  artifacts:
    - path: "src/domain/niyyah-options.ts"
      provides: "Niyyah motivation list with seasonal Hijri filtering"
      exports: ["NiyyahOption", "getAvailableNiyyahOptions", "NIYYAH_OPTIONS"]
    - path: "src/domain/starter-packs.ts"
      provides: "3 starter habit bundles using existing preset keys"
      exports: ["StarterPack", "STARTER_PACKS"]
    - path: "src/domain/notification-copy.ts"
      provides: "All notification title/body text — adab-safe, invitational"
      exports: ["getPrayerReminderTitle", "getPrayerReminderBody", "getFollowUpTitle", "getFollowUpBody"]
    - path: "src/services/notification-service.ts"
      provides: "Pure TS module for scheduling/cancelling all notifications"
      exports: ["requestPermissions", "rescheduleAll", "NotificationPrefs"]
    - path: "src/stores/settingsStore.ts"
      provides: "Extended with onboardingComplete, notification prefs, arabicTerms, hydrated flag"
  key_links:
    - from: "src/services/notification-service.ts"
      to: "src/services/prayer-times.ts"
      via: "import getPrayerWindows"
      pattern: "getPrayerWindows"
    - from: "src/services/notification-service.ts"
      to: "src/domain/notification-copy.ts"
      via: "import copy functions"
      pattern: "getPrayerReminderTitle|getFollowUpBody"
    - from: "src/stores/settingsStore.ts"
      to: "src/services/notification-service.ts"
      via: "rescheduleAll called from settings actions"
      pattern: "rescheduleAll"
---

<objective>
Install expo-notifications/expo-file-system/expo-sharing, create domain modules (niyyah options, starter packs, notification copy), build NotificationService pure TS module, and extend settingsStore with onboardingComplete flag, notification preferences, Arabic toggle, and hydrated flag.

Purpose: Every subsequent plan in Phase 6 depends on these domain modules and store fields. This is the foundation layer.
Output: 5 domain/service files, extended settingsStore, 4 test files, packages installed.
</objective>

<execution_context>
@C:/Users/jay_p/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jay_p/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-onboarding-profile-and-notifications/06-CONTEXT.md
@.planning/phases/06-onboarding-profile-and-notifications/06-RESEARCH.md

@src/stores/settingsStore.ts
@src/stores/sqliteStorage.ts
@src/services/prayer-times.ts
@src/types/habits.ts
@src/domain/presets.ts

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/types/habits.ts:
```typescript
export type PrayerName = 'fajr' | 'dhuhr' | 'asr' | 'maghrib' | 'isha';
export type CalcMethodKey = 'ISNA' | 'MWL' | 'Egyptian' | 'Karachi' | 'UmmAlQura' | 'MoonsightingCommittee';
export type PresetCategory = 'salah' | 'quran' | 'dhikr' | 'dua' | 'fasting' | 'character';
export interface PrayerWindow { name: PrayerName; start: Date; end: Date; status: 'active' | 'upcoming' | 'passed'; }
```

From src/stores/settingsStore.ts (current — will be extended):
```typescript
interface SettingsState {
  prayerCalcMethod: string;
  darkMode: DarkMode;
  soundEnabled: boolean;
  hapticEnabled: boolean;
  muhasabahReminderTime: string; // "HH:mm"
  locationLat: number | null;
  locationLng: number | null;
  locationName: string | null;
  // ... setters
}
```

From src/types/common.ts:
```typescript
export type DarkMode = 'auto' | 'dark' | 'light';
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install packages + create domain modules with tests</name>
  <files>
    package.json, app.json,
    src/domain/niyyah-options.ts, src/domain/starter-packs.ts, src/domain/notification-copy.ts,
    __tests__/domain/niyyah-options.test.ts, __tests__/domain/starter-packs.test.ts, __tests__/domain/notification-copy.test.ts
  </files>
  <behavior>
    - Niyyah: getAvailableNiyyahOptions() returns all non-seasonal options always
    - Niyyah: "Prepare for Ramadan" only appears when Hijri month is 8 (Sha'ban) or 9 (Ramadan)
    - Niyyah: getCurrentHijriMonth() returns a number 1-12 (with try/catch fallback to show all)
    - StarterPacks: STARTER_PACKS has exactly 3 bundles ("Beginner Path", "Salah Focus", "Full Discipline")
    - StarterPacks: Every habitKey in every bundle exists in the preset library (import and cross-check)
    - StarterPacks: Each bundle has 3-5 habits
    - NotificationCopy: getPrayerReminderBody for each PrayerName returns a string
    - NotificationCopy: getFollowUpBody for each PrayerName returns invitational text (contains "still" or "open", never "missed" or "forgot")
    - NotificationCopy: No function output contains words "missed", "forgot", "failed", "shame", "disappointed"
    - NotificationCopy: getMorningMotivation() returns rotating hadith/encouragement lines
    - NotificationCopy: getStreakMilestoneBody(count, habitName) uses "mashallah" or celebratory tone
  </behavior>
  <action>
    1. Run `npx expo install expo-notifications expo-file-system expo-sharing` to add packages.
    2. Add expo-notifications plugin to app.json plugins array:
       ```json
       ["expo-notifications", { "icon": "./assets/icon.png", "color": "#0a0a14", "defaultChannel": "default", "enableBackgroundRemoteNotifications": false }]
       ```
    3. Create `src/domain/niyyah-options.ts`:
       - Export NiyyahOption interface { id: string, text: string, visibleMonths?: number[] }
       - Export NIYYAH_OPTIONS array with 6-8 options. Always-visible: "Strengthen my salah", "Build daily discipline", "Grow closer to Allah", "Break bad habits", "Find consistency". Seasonal: "Prepare for Ramadan" (months [8,9])
       - Export getCurrentHijriMonth(): wraps Intl.DateTimeFormat('en-u-ca-islamic', { month: 'numeric' }) in try/catch, fallback returns undefined (show all)
       - Export getAvailableNiyyahOptions(): filters NIYYAH_OPTIONS by current Hijri month
    4. Create `src/domain/starter-packs.ts`:
       - Export StarterPack interface { id: string, name: string, description: string, habitKeys: string[] }
       - Export STARTER_PACKS: 3 bundles:
         * "Beginner Path" — 3 easy habits (e.g., fajr, morning-adhkar, quran-reading)
         * "Salah Focus" — 5 salah prayers (fajr, dhuhr, asr, maghrib, isha)
         * "Full Discipline" — 5 habits across categories (fajr, quran-reading, morning-adhkar, daily-dua, evening-adhkar)
       - Import PRESETS from presets.ts to validate keys exist (use preset.key matching)
    5. Create `src/domain/notification-copy.ts`:
       - Export functions: getPrayerReminderTitle(name: PrayerName), getPrayerReminderBody(name: PrayerName), getFollowUpTitle(name: PrayerName), getFollowUpBody(name: PrayerName), getMuhasabahTitle(), getMuhasabahBody(), getMorningMotivation(), getStreakMilestoneBody(count: number, habitName: string), getQuestExpiringBody(questName: string, timeLeft: string)
       - All copy follows wise mentor voice. Prayer reminders: "Ready for Dhuhr?" / "Time for Asr prayer". Follow-ups: "Dhuhr window is still open" / "There's still time for Asr". NEVER use "missed", "forgot", "failed".
       - Morning motivation: rotate through 5+ curated lines (short hadith references or encouragement)
    6. Write tests FIRST (RED), then implement (GREEN). Tests must verify:
       - niyyah-options: seasonal filtering works, fallback shows all
       - starter-packs: all habitKeys resolve to real presets, bundle count is 3
       - notification-copy: no forbidden words in any output, all PrayerNames covered
  </action>
  <verify>
    <automated>npx jest __tests__/domain/niyyah-options.test.ts __tests__/domain/starter-packs.test.ts __tests__/domain/notification-copy.test.ts -x</automated>
  </verify>
  <done>
    All 3 domain modules exist with exported types and functions. All tests pass. Packages installed. No forbidden guilt language in any notification copy.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend settingsStore + create NotificationService</name>
  <files>
    src/stores/settingsStore.ts,
    src/services/notification-service.ts,
    __tests__/services/notification-service.test.ts
  </files>
  <behavior>
    - settingsStore.onboardingComplete defaults to false
    - settingsStore.hydrated defaults to false, set to true on rehydration
    - settingsStore.arabicTermsEnabled defaults to true
    - settingsStore.prayerReminders is Record of PrayerName to { enabled: boolean, leadMinutes: number, followUpEnabled: boolean }
    - settingsStore defaults: all prayers enabled, leadMinutes 10, followUpEnabled true
    - settingsStore.streakMilestonesEnabled defaults to false
    - settingsStore.questExpiringEnabled defaults to false
    - settingsStore.morningMotivationEnabled defaults to false
    - settingsStore.muhasabahNotifEnabled defaults to true
    - NotificationService.rescheduleAll() cancels all existing, then schedules based on prefs
    - NotificationService schedules 0 notifications for a disabled prayer
    - NotificationService schedules 2 notifications for an enabled prayer with follow-up (reminder + follow-up)
    - NotificationService schedules 1 daily trigger for Muhasabah when enabled
    - NotificationService.requestPermissions() returns boolean
  </behavior>
  <action>
    1. Extend `src/stores/settingsStore.ts` SettingsState interface with:
       - `onboardingComplete: boolean` (default false)
       - `hydrated: boolean` (default false, NOT persisted)
       - `arabicTermsEnabled: boolean` (default true)
       - `muhasabahNotifEnabled: boolean` (default true)
       - `prayerReminders: Record<PrayerName, { enabled: boolean; leadMinutes: number; followUpEnabled: boolean }>` (default: all enabled, 10 min lead, followUp true)
       - `streakMilestonesEnabled: boolean` (default false)
       - `questExpiringEnabled: boolean` (default false)
       - `morningMotivationEnabled: boolean` (default false)
       - `selectedNiyyahs: string[]` (default [])
       - `characterPresetId: string | null` (default null)
       - Add setters: setOnboardingComplete, toggleArabicTerms, setPrayerReminder, setMuhasabahNotifEnabled, toggleStreakMilestones, toggleQuestExpiring, toggleMorningMotivation, setSelectedNiyyahs, setCharacterPresetId
       - Add `onRehydrateStorage` callback in persist config to set hydrated: true
       - Add all new persisted fields to `partialize` (except `hydrated`)
    2. Create `src/services/notification-service.ts`:
       - Import expo-notifications, prayer-times service, notification-copy module
       - Export NotificationPrefs interface matching settingsStore notification fields
       - Export requestPermissions(): requests iOS+Android permissions, returns boolean
       - Export rescheduleAll(lat, lng, calcMethod, prefs): cancels all, then schedules:
         * For each enabled prayer: DateTriggerInput at (prayerStart - leadMinutes). If followUpEnabled: second DateTriggerInput at prayerStart + 30min. Only schedule if trigger date is in the future.
         * If muhasabahEnabled: DailyTriggerInput with hour/minute from muhasabahReminderTime
         * Uses notification-copy functions for all title/body text
       - Export cancelAll(): wraps Notifications.cancelAllScheduledNotificationsAsync()
       - Schedule only today's prayer notifications (iOS 64-notification limit). Reschedule on next app launch.
    3. Write test for notification-service FIRST (RED) mocking expo-notifications:
       - Mock Notifications.cancelAllScheduledNotificationsAsync, scheduleNotificationAsync, requestPermissionsAsync
       - Test: all prayers disabled => 0 scheduleNotificationAsync calls (after cancel)
       - Test: 1 prayer enabled with followUp => 2 scheduleNotificationAsync calls
       - Test: muhasabah enabled => 1 DailyTriggerInput call
       - Test: requestPermissions returns true when status granted
  </action>
  <verify>
    <automated>npx jest __tests__/services/notification-service.test.ts -x</automated>
  </verify>
  <done>
    settingsStore has all new fields with correct defaults. NotificationService schedules correct notification count per configuration. Hydrated flag wired via onRehydrateStorage. All tests pass.
  </done>
</task>

</tasks>

<verification>
npx jest __tests__/domain/ __tests__/services/notification-service.test.ts -x
</verification>

<success_criteria>
- expo-notifications, expo-file-system, expo-sharing installed
- 3 domain modules (niyyah-options, starter-packs, notification-copy) with tests
- NotificationService pure TS module with mocked tests
- settingsStore extended with 10+ new fields including onboardingComplete and hydrated
- Zero guilt language in any notification copy (automated test verifies)
- All tests green
</success_criteria>

<output>
After completion, create `.planning/phases/06-onboarding-profile-and-notifications/06-01-SUMMARY.md`
</output>
