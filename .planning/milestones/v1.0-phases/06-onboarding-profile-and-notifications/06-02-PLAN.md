---
phase: 06-onboarding-profile-and-notifications
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - app/_layout.tsx
  - app/(onboarding)/_layout.tsx
  - app/(onboarding)/welcome.tsx
  - app/(onboarding)/character.tsx
  - app/(onboarding)/niyyah.tsx
  - app/(onboarding)/habits.tsx
  - app/(onboarding)/tour.tsx
  - src/components/onboarding/CharacterPicker.tsx
  - src/components/onboarding/NiyyahSelector.tsx
  - src/components/onboarding/StarterPackSelector.tsx
  - src/components/onboarding/HudTourOverlay.tsx
autonomous: false
requirements:
  - ONBR-01
  - ONBR-02
  - ONBR-03
  - ONBR-04

must_haves:
  truths:
    - "New user sees onboarding flow on first launch (onboardingComplete is false)"
    - "User can pick a preset character and optionally customize appearance"
    - "User can multi-select up to 3 Niyyah motivations"
    - "User can pick a starter habit bundle or customize their own"
    - "User experiences an interactive HUD tour that teaches navigation by doing"
    - "After onboarding, user lands on Home HUD and onboarding never shows again"
    - "Returning user (onboardingComplete=true) skips onboarding entirely"
  artifacts:
    - path: "app/(onboarding)/_layout.tsx"
      provides: "Stack navigator for 5-screen onboarding flow"
    - path: "app/(onboarding)/welcome.tsx"
      provides: "Welcome splash with RPG intro"
    - path: "app/(onboarding)/character.tsx"
      provides: "Character preset picker + customization"
    - path: "app/(onboarding)/niyyah.tsx"
      provides: "Niyyah multi-select (max 3)"
    - path: "app/(onboarding)/habits.tsx"
      provides: "Starter pack bundles + customize option"
    - path: "app/(onboarding)/tour.tsx"
      provides: "Interactive HUD tour with spotlight overlays"
    - path: "app/_layout.tsx"
      provides: "Root layout with Stack.Protected guard for onboarding"
  key_links:
    - from: "app/_layout.tsx"
      to: "src/stores/settingsStore.ts"
      via: "useSettingsStore(s => s.onboardingComplete)"
      pattern: "onboardingComplete"
    - from: "app/(onboarding)/niyyah.tsx"
      to: "src/domain/niyyah-options.ts"
      via: "import getAvailableNiyyahOptions"
      pattern: "getAvailableNiyyahOptions"
    - from: "app/(onboarding)/habits.tsx"
      to: "src/domain/starter-packs.ts"
      via: "import STARTER_PACKS"
      pattern: "STARTER_PACKS"
    - from: "app/(onboarding)/habits.tsx"
      to: "src/stores/habitStore.ts"
      via: "addHabit for each selected preset"
      pattern: "habitStore.*addHabit"
---

<objective>
Build the 5-screen onboarding flow: Welcome splash, Character creation, Niyyah selection, Starter habits, Interactive HUD tour. Wire the root layout with Stack.Protected guard so onboarding shows on first launch and never again after completion.

Purpose: First-launch experience that feels like starting an RPG -- sets intention, creates identity, teaches navigation.
Output: 5 onboarding screens, 4 components, modified root layout with hydration gate.
</objective>

<execution_context>
@C:/Users/jay_p/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jay_p/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-onboarding-profile-and-notifications/06-CONTEXT.md
@.planning/phases/06-onboarding-profile-and-notifications/06-RESEARCH.md
@.planning/phases/06-onboarding-profile-and-notifications/06-01-SUMMARY.md

@app/_layout.tsx
@src/stores/settingsStore.ts
@src/stores/habitStore.ts
@src/domain/niyyah-options.ts
@src/domain/starter-packs.ts
@src/domain/presets.ts
@src/components/hud/CharacterSprite.tsx
@src/components/hud/SceneObjects.tsx
@src/components/habits/PresetLibrary.tsx
@src/tokens/colors.ts
@src/tokens/typography.ts
@src/tokens/spacing.ts

<interfaces>
<!-- From Plan 01 outputs -->

From src/domain/niyyah-options.ts:
```typescript
export interface NiyyahOption { id: string; text: string; visibleMonths?: number[]; }
export function getAvailableNiyyahOptions(): NiyyahOption[];
```

From src/domain/starter-packs.ts:
```typescript
export interface StarterPack { id: string; name: string; description: string; habitKeys: string[]; }
export const STARTER_PACKS: StarterPack[];
```

From src/stores/settingsStore.ts (extended in Plan 01):
```typescript
// New fields:
onboardingComplete: boolean;
hydrated: boolean;
selectedNiyyahs: string[];
characterPresetId: string | null;
setOnboardingComplete: (complete: boolean) => void;
setSelectedNiyyahs: (ids: string[]) => void;
setCharacterPresetId: (id: string) => void;
```

From src/components/hud/CharacterSprite.tsx:
```typescript
// Existing Skia-based pixel character with idle animation
// Reusable in onboarding character picker
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Root layout guard + onboarding screens</name>
  <files>
    app/_layout.tsx,
    app/(onboarding)/_layout.tsx,
    app/(onboarding)/welcome.tsx,
    app/(onboarding)/character.tsx,
    app/(onboarding)/niyyah.tsx,
    app/(onboarding)/habits.tsx,
    app/(onboarding)/tour.tsx,
    src/components/onboarding/CharacterPicker.tsx,
    src/components/onboarding/NiyyahSelector.tsx,
    src/components/onboarding/StarterPackSelector.tsx,
    src/components/onboarding/HudTourOverlay.tsx
  </files>
  <action>
    **Root Layout (app/_layout.tsx):**
    1. Add hydration gate: read `hydrated` and `onboardingComplete` from settingsStore. If `!hydrated`, return null (keep splash visible).
    2. Add Stack.Protected guard wrapping `(onboarding)` group screen: `guard={!onboardingComplete}`.
    3. Add Stack.Screen entries for `settings`, `prayer-reminders`, and `your-data` (presentation: 'card') for later plans.
    4. Keep existing MuhasabahModal mount outside Stack.

    **Onboarding Layout (app/(onboarding)/_layout.tsx):**
    - Stack navigator with headerShown: false, gestureEnabled: false (no back swipe in onboarding).
    - Screens: welcome, character, niyyah, habits, tour.
    - Dark background (colors.surface.primary).

    **Screen 1: Welcome (app/(onboarding)/welcome.tsx):**
    - Full-screen splash. Pixel art title "HalalHabits" in Press Start 2P font.
    - Tagline: "Your discipline journey begins" in Inter.
    - Single CTA button: "Begin Your Journey" navigates to character screen.
    - Subtle FadeIn animation via Reanimated.
    - Dark background with jewel tone accents.

    **Screen 2: Character (app/(onboarding)/character.tsx):**
    - Heading: "Choose Your Scholar"
    - CharacterPicker component: horizontal ScrollView of 4 preset character options (scholar_01 through scholar_04). Each is a pressable card showing the CharacterSprite variant. Selected state has emerald border glow.
    - Optional customization section below: skin tone picker (3-4 tones), outfit color picker (3-4 jewel tones). These are stored as characterPresetId in settingsStore.
    - "Continue" button navigates to niyyah screen.
    - For MVP, character presets are differentiated by ID string (actual sprite assets are placeholders — same CharacterSprite with tint variations). Store selectedPresetId in settingsStore.

    **Screen 3: Niyyah (app/(onboarding)/niyyah.tsx):**
    - Heading: "Set Your Niyyah" with subtext "What brings you here? (Pick up to 3)"
    - NiyyahSelector component: renders getAvailableNiyyahOptions() as chip/pill buttons. Multi-select, max 3. Selected chips have filled emerald background. Unselected have border-only style.
    - When user selects, store in local state. On "Continue", call settingsStore.setSelectedNiyyahs(selectedIds).
    - Seasonal options appear/hide based on Hijri month automatically.
    - "Continue" button navigates to habits screen. Requires at least 1 selection.

    **Screen 4: Habits (app/(onboarding)/habits.tsx):**
    - Heading: "Choose Your Path"
    - StarterPackSelector component: renders STARTER_PACKS as 3 vertical cards. Each card shows pack name, description, and habit count. Tapping selects the pack (radio-style, one at a time). Selected card has emerald border + checkmark.
    - "Customize my own" link at bottom opens the existing PresetLibrary component in a modal or inline, allowing free selection.
    - On "Continue": for the selected pack (or custom selection), call habitStore.addHabit for each habit key. Then navigate to tour screen.
    - Show brief loading state while habits are being created.

    **Screen 5: Tour (app/(onboarding)/tour.tsx):**
    - HudTourOverlay component: renders a semi-transparent overlay on top of a miniature HUD preview.
    - 3 spotlight steps with tooltip bubbles:
      1. Spotlight on habits area: "Tap here to track your daily habits"
      2. Spotlight on quest board area: "Complete quests for bonus rewards"
      3. Final: "You're ready. Go." with a button
    - Tour is skippable via "Skip" text button in top-right corner.
    - On completion or skip: call settingsStore.setOnboardingComplete(true), then router.replace('/(tabs)').
    - Use Reanimated for spotlight fade transitions between steps.

    **UI/UX Guidelines:**
    - All screens use dark background (colors.surface.primary) with jewel tone accents.
    - Minimum 44x44px touch targets on all interactive elements.
    - Inter for body text, Press Start 2P for headings.
    - Smooth transitions between screens (default Stack animation is fine).
    - No shame language anywhere. Encouraging, invitational tone.
    - "Begin Your Journey" button style: filled emerald background, rounded, full width.
  </action>
  <verify>
    <automated>npx expo export --platform web --output-dir /tmp/expo-export-check 2>&1 | head -5 || echo "Build check complete"</automated>
  </verify>
  <done>
    5 onboarding screens render. Root layout gates onboarding behind Stack.Protected. Character selection stores preset ID. Niyyah selection stores up to 3 motivations. Habit selection creates habits from chosen pack. Tour teaches HUD navigation. Completing tour sets onboardingComplete=true and navigates to tabs.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify onboarding flow</name>
  <files>app/(onboarding)/welcome.tsx</files>
  <action>Human verifies the complete onboarding flow on device. Built: 5-screen onboarding (Welcome, Character, Niyyah, Habits, Tour) with Stack.Protected guard.</action>
  <verify>
    <automated>echo "Manual verification checkpoint"</automated>
  </verify>
  <done>
    1. Clear app data or fresh install to trigger onboarding
    2. Welcome screen: "Begin Your Journey" button and pixel art title visible
    3. Character screen: 4 preset options selectable, one highlights with emerald border
    4. Niyyah screen: chip selection works, max 3 enforced, seasonal filtering works
    5. Habits screen: 3 starter packs shown, "Customize my own" opens preset library
    6. Select a pack and continue to Tour screen
    7. Tour screen: 3 spotlight steps with tooltips, "Skip" button works
    8. Complete tour: lands on Home HUD, onboarding never shows again on restart
    9. Total flow completes in under 2 minutes
  </done>
</task>

</tasks>

<verification>
- Onboarding flow completes end-to-end on device
- settingsStore.onboardingComplete persists across restarts
- Returning user never sees onboarding
- Flow target: under 2 minutes
</verification>

<success_criteria>
- New user sees onboarding on first launch, completes in under 2 minutes
- Character, Niyyah, and habits selections persist to settingsStore/habitStore
- After onboarding, user lands on Home HUD
- Returning user (app restart) goes directly to tabs
- Tour is skippable
</success_criteria>

<output>
After completion, create `.planning/phases/06-onboarding-profile-and-notifications/06-02-SUMMARY.md`
</output>
