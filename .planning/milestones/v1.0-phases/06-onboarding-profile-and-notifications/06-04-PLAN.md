---
phase: 06-onboarding-profile-and-notifications
plan: 04
type: execute
wave: 3
depends_on: ["06-01", "06-02", "06-03"]
files_modified:
  - app/_layout.tsx
  - src/components/hud/SceneObjects.tsx
  - app/(tabs)/index.tsx
autonomous: false
requirements:
  - NOTF-01
  - NOTF-02
  - NOTF-03
  - NOTF-04

must_haves:
  truths:
    - "Prayer notifications are scheduled on app startup when permissions granted and location available"
    - "Muhasabah daily reminder fires at user's configured time"
    - "Notification copy is invitational across all categories"
    - "Notifications reschedule when relevant settings change"
    - "Glowing journal HUD cue appears on Home screen as Muhasabah in-app reminder"
  artifacts:
    - path: "app/_layout.tsx"
      provides: "Notification scheduling on app startup + response listener"
    - path: "src/components/hud/SceneObjects.tsx"
      provides: "Glowing journal tap zone for Muhasabah in-app cue"
  key_links:
    - from: "app/_layout.tsx"
      to: "src/services/notification-service.ts"
      via: "rescheduleAll on startup"
      pattern: "rescheduleAll"
    - from: "src/components/hud/SceneObjects.tsx"
      to: "src/stores/gameStore.ts"
      via: "check if Muhasabah is due for glowing cue"
      pattern: "muhasabah.*glow|journal.*cue"
---

<objective>
Wire notification scheduling to app lifecycle (startup reschedule, settings-change reschedule), add glowing journal Muhasabah HUD cue, and run full phase verification.

Purpose: Notifications are the retention layer -- inviting users back for prayers and reflection without guilt.
Output: Notification system wired end-to-end, Muhasabah HUD cue, full phase verified.
</objective>

<execution_context>
@C:/Users/jay_p/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jay_p/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-onboarding-profile-and-notifications/06-CONTEXT.md
@.planning/phases/06-onboarding-profile-and-notifications/06-RESEARCH.md
@.planning/phases/06-onboarding-profile-and-notifications/06-01-SUMMARY.md
@.planning/phases/06-onboarding-profile-and-notifications/06-02-SUMMARY.md
@.planning/phases/06-onboarding-profile-and-notifications/06-03-SUMMARY.md

@app/_layout.tsx
@src/services/notification-service.ts
@src/stores/settingsStore.ts
@src/components/hud/SceneObjects.tsx
@app/(tabs)/index.tsx

<interfaces>
From src/services/notification-service.ts (Plan 01):
```typescript
export async function requestPermissions(): Promise<boolean>;
export async function rescheduleAll(lat: number, lng: number, calcMethod: CalcMethodKey, prefs: NotificationPrefs): Promise<void>;
export async function cancelAll(): Promise<void>;
```

From src/stores/settingsStore.ts (Plan 01):
```typescript
locationLat: number | null;
locationLng: number | null;
prayerCalcMethod: string;
prayerReminders: Record<PrayerName, { enabled: boolean; leadMinutes: number; followUpEnabled: boolean }>;
muhasabahNotifEnabled: boolean;
muhasabahReminderTime: string;
// ... other notification prefs
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Notification startup wiring + Muhasabah HUD cue</name>
  <files>
    app/_layout.tsx,
    src/components/hud/SceneObjects.tsx,
    app/(tabs)/index.tsx
  </files>
  <action>
    **Root Layout notification wiring (app/_layout.tsx):**
    1. Add a useEffect that runs after hydration + migrations complete:
       - Check if notification permissions are granted (Notifications.getPermissionsAsync)
       - If granted AND locationLat/locationLng are available from settingsStore:
         - Call NotificationService.rescheduleAll() with current settings
       - This handles the Android reboot case and daily rescheduling
    2. Add Notifications.addNotificationResponseReceivedListener to handle notification taps:
       - Prayer notification tap -> router.push('/(tabs)/habits')
       - Muhasabah notification tap -> open MuhasabahModal (via uiStore or existing trigger)
       - This listener is registered once in root layout and cleaned up on unmount
    3. Set notification handler for foreground notifications:
       ```typescript
       Notifications.setNotificationHandler({
         handleNotification: async () => ({
           shouldShowAlert: true,
           shouldPlaySound: true,
           shouldSetBadge: false,
         }),
       });
       ```
       Place this outside the component (module-level) so it runs once.

    **Muhasabah HUD Cue (src/components/hud/SceneObjects.tsx):**
    - The journal tap zone already exists in SceneObjects for Muhasabah.
    - Add a glowing animation to the journal tap zone when Muhasabah is "due" (current time >= muhasabahReminderTime from settingsStore AND today's Muhasabah not yet completed).
    - Use Reanimated withRepeat + withTiming to pulse opacity (0.5 -> 1.0 -> 0.5) on a glow overlay.
    - Read muhasabah completion status from muhasabahStore (check if today's entry exists).
    - If already completed today, no glow. If before reminder time, no glow.

    **Home screen integration (app/(tabs)/index.tsx):**
    - If the Home screen has any notification-related state, wire it here. Otherwise, the HUD cue in SceneObjects handles the visual.
    - No major changes expected — just ensure SceneObjects receives any needed props for the glow state.
  </action>
  <verify>
    <automated>npx jest --passWithNoTests 2>&1 | tail -5</automated>
  </verify>
  <done>
    Notifications schedule on app startup when permissions granted. Notification taps route to correct screens. Muhasabah journal glows on HUD when reflection is due. Foreground notifications display correctly.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Full Phase 6 verification</name>
  <files>app/_layout.tsx</files>
  <action>Human verifies all Phase 6 features end-to-end on device. Built: Onboarding flow, Profile/Settings screens, Notification system, Muhasabah HUD cue. All 12 requirements (ONBR-01 through NOTF-04) addressed.</action>
  <verify>
    <automated>npx jest 2>&1 | tail -10</automated>
  </verify>
  <done>
    Onboarding: 5-screen flow completes in under 2 minutes, character/niyyah/habits persist, tour skippable, never shows again.
    Profile: character sheet with sprite, title, level, XP, stats, trophy case, streaks, niyyah.
    Settings: 4 sections, per-prayer controls, Arabic toggle, data export/delete.
    Notifications: prayer reminders fire, Muhasabah reminder fires, copy is invitational, categories toggleable, journal glows on HUD.
    Tests: full suite passes.
  </done>
</task>

</tasks>

<verification>
npx jest
- Full test suite passes
- All 12 requirement IDs addressed
- Notification scheduling verified on device
- Muhasabah HUD cue visible when due
</verification>

<success_criteria>
- Notifications schedule on startup and reschedule on settings change
- Prayer notification taps route to habits screen
- Muhasabah notification taps open reflection modal
- Journal glows on HUD when Muhasabah is due
- Full phase verified: onboarding + profile + settings + notifications all working
- All tests green
</success_criteria>

<output>
After completion, create `.planning/phases/06-onboarding-profile-and-notifications/06-04-SUMMARY.md`
</output>
