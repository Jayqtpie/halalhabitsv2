---
phase: 03-core-habit-loop
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/domain/streak-engine.ts
  - __tests__/domain/streak-engine.test.ts
autonomous: true
requirements: [STRK-01, STRK-02, STRK-03, STRK-04]

must_haves:
  truths:
    - "Streak increments on consecutive daily completions"
    - "Multiplier increases by 0.1x per day, capped at 3.0x"
    - "Streak break is detected when last completion was more than 1 day ago"
    - "Mercy Mode activates on break with pre-break streak preserved"
    - "3-day recovery restores 25% of pre-break streak"
    - "Fresh start resets to 0 with no penalty"
    - "Salah Streak Shield applies only when completed within prayer window"
    - "Same-day re-completion is idempotent (no double counting)"
  artifacts:
    - path: "src/domain/streak-engine.ts"
      provides: "Pure streak logic: completion, break detection, mercy mode, recovery"
      exports: ["processCompletion", "detectStreakBreak", "processRecovery", "startFreshReset", "calculateStreakShieldBonus", "isCompletedToday"]
      min_lines: 80
    - path: "__tests__/domain/streak-engine.test.ts"
      provides: "Comprehensive tests for all streak scenarios"
      min_lines: 100
  key_links:
    - from: "src/domain/streak-engine.ts"
      to: "src/types/habits.ts"
      via: "imports StreakState, MercyModeState types"
      pattern: "import.*StreakState.*from"
---

<objective>
Build the streak engine as a pure-TypeScript module with full TDD coverage. This module handles all streak calculation, break detection, Mercy Mode activation/recovery, multiplier management, and Salah Streak Shield logic.

Purpose: The streak engine is the most complex business logic in Phase 3. Pure functions with no React or DB imports make it fully testable and verifiable. Every edge case (day boundaries, idempotent completion, multiplier cap, recovery math) must be covered by tests BEFORE implementation.
Output: streak-engine.ts with 6+ exported functions, streak-engine.test.ts with 15+ test cases.
</objective>

<execution_context>
@C:/Users/jay_p/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jay_p/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-core-habit-loop/03-CONTEXT.md
@.planning/phases/03-core-habit-loop/03-RESEARCH.md

<interfaces>
<!-- Types this plan needs (created by Plan 01, but this plan runs in parallel so define locally or import) -->
<!-- If Plan 01 hasn't run yet, streak-engine.ts should define its own StreakState/MercyModeState types
     OR import from types/habits.ts if it exists. Check at execution time. -->

StreakState interface (from research):
```typescript
export interface StreakState {
  currentCount: number;
  longestCount: number;
  multiplier: number;
  lastCompletedAt: string | null; // ISO 8601
  isRebuilt: boolean;
  mercyMode?: MercyModeState;
}

export interface MercyModeState {
  active: boolean;
  recoveryDay: number; // 0-3 (0 = not started, 3 = complete)
  preBreakStreak: number;
}
```

Key project decisions:
- Day boundary = midnight local time (00:00), not Fajr/Maghrib
- Multiplier: 1.0x base, +0.1x/day, cap 3.0x
- Mercy Mode is PER-HABIT (not global)
- Recovery: 3 consecutive days -> 25% of pre-break streak restored
- Fresh start: reset to 0, equally valid choice
- XP total never decreases (streak multiplier resets, not XP)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED -- Write failing tests for all streak engine behaviors</name>
  <files>__tests__/domain/streak-engine.test.ts</files>
  <behavior>
    processCompletion:
    - First completion: streak = 1, multiplier = 1.0
    - Consecutive day: streak + 1, multiplier + 0.1 (capped at 3.0)
    - Same day re-completion: no change (idempotent)
    - Non-consecutive (gap > 1 day): streak resets to 1, multiplier resets to 1.0
    - Updates longestCount if current exceeds it
    - During mercy recovery: increments recoveryDay

    detectStreakBreak:
    - Returns broken=true if lastCompletedAt is more than 1 calendar day before currentDate
    - Returns broken=false if lastCompletedAt is yesterday or today
    - Returns MercyModeState with preBreakStreak on break
    - Handles null lastCompletedAt (no prior completions, no break)

    processRecovery:
    - Day 1, 2: complete=false, restoredStreak=0
    - Day 3: complete=true, restoredStreak = floor(preBreakStreak * 0.25)
    - Minimum restored streak is 1 (if preBreakStreak >= 4)

    startFreshReset:
    - Returns StreakState with currentCount=0, multiplier=1.0, no mercy mode

    calculateStreakShieldBonus:
    - Returns true if completion time is within prayer window (start <= time < end)
    - Returns false if outside window
    - Only applicable to salah-type habits

    isCompletedToday:
    - Returns true if lastCompletedAt is same calendar day as reference date
    - Uses midnight local time as day boundary
  </behavior>
  <action>
    Create __tests__/domain/streak-engine.test.ts with 15+ test cases covering ALL behaviors listed above. Import the functions from src/domain/streak-engine.ts (which does not exist yet -- tests MUST fail).

    Group tests into describe blocks:
    - "processCompletion" (6+ tests: first completion, consecutive, same-day idempotent, gap reset, multiplier cap at 3.0, longestCount update)
    - "detectStreakBreak" (4+ tests: broken after gap, not broken yesterday, not broken today, null lastCompletedAt)
    - "processRecovery" (3+ tests: day 1-2 incomplete, day 3 complete with 25% restore, minimum restored streak)
    - "startFreshReset" (1 test: clean reset)
    - "calculateStreakShieldBonus" (2+ tests: within window true, outside window false)
    - "isCompletedToday" (2+ tests: same day true, different day false)

    Use fixed dates for deterministic tests (e.g., 2025-01-15T12:00:00.000Z).
    Run tests -- they MUST all fail (RED phase).
    Commit: "test(03-02): add failing tests for streak engine"
  </action>
  <verify>
    <automated>npm test -- --testPathPattern="streak-engine" --no-coverage 2>&1 | tail -5</automated>
  </verify>
  <done>15+ failing tests exist covering all streak engine behaviors. All tests fail because implementation does not exist yet (RED phase confirmed).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN + REFACTOR -- Implement streak engine to pass all tests</name>
  <files>src/domain/streak-engine.ts</files>
  <action>
    Create src/domain/streak-engine.ts implementing all 6 exported functions as pure TypeScript (no React, no DB, no side effects):

    1. Helper functions (not exported):
       - getHabitDay(isoString: string): string -- returns YYYY-MM-DD in local time
       - isNextDay(prev: string, current: string): boolean -- checks calendar day adjacency

    2. processCompletion(currentStreak: StreakState, completionDate: string): StreakState
       - If isCompletedToday: return unchanged (idempotent)
       - If no lastCompletedAt or not consecutive: reset to streak 1, multiplier 1.0
       - If consecutive: increment streak, add 0.1 to multiplier (cap at 3.0)
       - Update longestCount if currentCount > longestCount
       - If mercy mode active: increment recoveryDay
       - Set lastCompletedAt to completionDate

    3. detectStreakBreak(lastCompletedAt: string | null, currentDate: string, currentCount: number): { broken: boolean, mercyMode?: MercyModeState }
       - null lastCompletedAt: not broken
       - Same day or yesterday: not broken
       - More than 1 day gap: broken, return MercyModeState with preBreakStreak = currentCount

    4. processRecovery(mercyState: MercyModeState): { complete: boolean, restoredStreak: number, updatedState: MercyModeState }
       - Increment recoveryDay
       - Day 3: complete=true, restoredStreak = Math.max(1, Math.floor(preBreakStreak * 0.25))
       - Day 1-2: complete=false, restoredStreak=0

    5. startFreshReset(): StreakState -- clean slate, all zeros, no mercy mode

    6. calculateStreakShieldBonus(completionTime: Date, windowStart: Date, windowEnd: Date): boolean
       - Returns start <= completionTime < end

    7. isCompletedToday(lastCompletedAt: string | null, referenceDate: string): boolean
       - Compare YYYY-MM-DD of both dates in local time

    Import StreakState and MercyModeState from src/types/habits.ts if it exists, otherwise define locally and add a TODO to consolidate.

    Run tests -- ALL must pass (GREEN phase).
    Commit: "feat(03-02): implement streak engine"

    REFACTOR if needed: clean up any duplication, ensure function signatures are clean.
    Run tests again -- must still pass.
    Commit (if refactored): "refactor(03-02): clean up streak engine"
  </action>
  <verify>
    <automated>npm test -- --testPathPattern="streak-engine" --no-coverage</automated>
  </verify>
  <done>All 15+ streak engine tests pass. processCompletion handles consecutive/gap/same-day/first cases. Multiplier caps at 3.0. Mercy Mode activates on break, 3-day recovery restores 25%. Fresh start resets cleanly. Streak Shield checks window bounds. isCompletedToday uses midnight boundary.</done>
</task>

</tasks>

<verification>
npm test -- --testPathPattern="streak-engine" --no-coverage
All tests pass. RED-GREEN-REFACTOR cycle followed with atomic commits.
</verification>

<success_criteria>
- All streak scenarios covered by failing tests BEFORE implementation
- processCompletion handles consecutive, gap, same-day, and first-completion cases
- Multiplier correctly caps at 3.0x
- Mercy Mode activates on break with correct preBreakStreak
- 3-day recovery restores 25% (floored)
- Fresh start resets cleanly
- Streak Shield correctly checks prayer window bounds
- isCompletedToday handles day boundary at midnight
- 15+ test cases all passing
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-habit-loop/03-02-SUMMARY.md`
</output>
