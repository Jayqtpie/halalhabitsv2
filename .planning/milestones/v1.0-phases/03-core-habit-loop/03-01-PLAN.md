---
phase: 03-core-habit-loop
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/habits.ts
  - src/domain/presets.ts
  - src/domain/habit-sorter.ts
  - src/services/prayer-times.ts
  - src/services/location.ts
  - __tests__/services/prayer-times.test.ts
  - __tests__/domain/presets.test.ts
  - __tests__/domain/habit-sorter.test.ts
autonomous: true
requirements: [HBIT-01, HBIT-04, PRAY-01, PRAY-02]

must_haves:
  truths:
    - "Prayer times can be calculated for any coordinates and calculation method"
    - "Preset library contains ~15 Islamic habits across 6 categories"
    - "Habits sort with salah in chronological prayer order first, then non-salah by user order"
    - "Prayer windows have correct active/upcoming/passed status based on current time"
  artifacts:
    - path: "src/types/habits.ts"
      provides: "Domain types for presets, prayer windows, mercy mode, completion"
      exports: ["PresetHabit", "PrayerWindow", "PrayerName", "CalcMethodKey", "MercyModeState", "HabitWithStatus"]
    - path: "src/services/prayer-times.ts"
      provides: "adhan-js wrapper returning typed PrayerWindow arrays"
      exports: ["getPrayerWindows", "getNextFajr", "formatPrayerTime"]
    - path: "src/services/location.ts"
      provides: "expo-location wrapper with permission handling and fallback"
      exports: ["getCoordinates", "LocationResult"]
    - path: "src/domain/presets.ts"
      provides: "Islamic habit preset definitions with all categories"
      exports: ["HABIT_PRESETS", "PRESET_CATEGORIES", "getPresetsByCategory"]
    - path: "src/domain/habit-sorter.ts"
      provides: "Sorting logic for daily habit list"
      exports: ["sortHabitsForDisplay"]
  key_links:
    - from: "src/services/prayer-times.ts"
      to: "adhan"
      via: "import { Coordinates, CalculationMethod, PrayerTimes }"
      pattern: "new PrayerTimes"
    - from: "src/domain/habit-sorter.ts"
      to: "src/services/prayer-times.ts"
      via: "uses prayer window times for salah chronological ordering"
      pattern: "sortHabitsForDisplay"
---

<objective>
Install Phase 3 dependencies and build the pure-TypeScript domain layer: domain types, Islamic habit presets, prayer time service (adhan-js wrapper), location service (expo-location wrapper), and habit sorting logic.

Purpose: These are the foundational building blocks that all screens and stores depend on. Pure functions with no React imports, fully unit-testable.
Output: 5 new source files + 3 test files, all passing. adhan, expo-location, expo-haptics installed.
</objective>

<execution_context>
@C:/Users/jay_p/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jay_p/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-core-habit-loop/03-CONTEXT.md
@.planning/phases/03-core-habit-loop/03-RESEARCH.md

<interfaces>
<!-- Existing types from Phase 2 that this plan uses -->

From src/types/database.ts:
```typescript
export type Habit = InferSelectModel<typeof habits>;
export type NewHabit = InferInsertModel<typeof habits>;
```

From src/db/schema.ts (habits table columns):
```typescript
// habits table has: id, userId, name, type, presetKey, category, frequency,
// frequencyDays, timeWindowStart, timeWindowEnd, difficultyTier, baseXp,
// status, sortOrder, icon, createdAt, updatedAt
```

From src/stores/settingsStore.ts:
```typescript
interface SettingsState {
  prayerCalcMethod: string;
  // ... location not yet in store, comes from settingsRepo
}
```

From src/db/schema.ts (settings table):
```typescript
// settings has: prayerCalcMethod, locationLat, locationLng, locationName
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install dependencies and create domain types</name>
  <files>src/types/habits.ts, package.json</files>
  <behavior>
    - PresetHabit type has: key, name, category, type, frequency, icon, difficultyTier, baseXp, description
    - PrayerName is union: 'fajr' | 'dhuhr' | 'asr' | 'maghrib' | 'isha'
    - PrayerWindow has: name (PrayerName), displayName, start (Date), end (Date), status ('active' | 'upcoming' | 'passed')
    - CalcMethodKey is union of supported methods: 'ISNA' | 'MWL' | 'Egyptian' | 'Karachi' | 'UmmAlQura' | 'MoonsightingCommittee'
    - MercyModeState has: active, recoveryDay (0-3), preBreakStreak
    - HabitWithStatus extends Habit with: completedToday (boolean), streak (StreakState), prayerWindow (PrayerWindow | null)
  </behavior>
  <action>
    1. Run `npx expo install adhan expo-location expo-haptics react-native-reanimated` to install Phase 3 dependencies. Use npx expo install (not npm) per research guidance for Expo Go compatibility.
    2. Create src/types/habits.ts with all domain types for Phase 3:
       - PresetHabit: defines shape of preset habit data (key, name, category, type, frequency, icon, difficultyTier, baseXp, description)
       - PrayerName: 'fajr' | 'dhuhr' | 'asr' | 'maghrib' | 'isha'
       - PrayerWindow: { name: PrayerName, displayName: string, start: Date, end: Date, status: 'active' | 'upcoming' | 'passed' }
       - CalcMethodKey: union of supported calculation methods
       - CalcMethodInfo: { key: CalcMethodKey, displayName: string, description: string } for picker UI
       - CALC_METHODS: array of CalcMethodInfo for all 6 methods
       - MercyModeState: { active: boolean, recoveryDay: number, preBreakStreak: number }
       - StreakState: { currentCount, longestCount, multiplier, lastCompletedAt, isRebuilt, mercyMode?: MercyModeState }
       - HabitWithStatus: Habit & { completedToday: boolean, streak: StreakState | null, prayerWindow: PrayerWindow | null }
       - PresetCategory: 'salah' | 'quran' | 'dhikr' | 'dua' | 'fasting' | 'character'
    3. Do NOT add any Reanimated babel plugin to babel.config.js -- babel-preset-expo handles it in SDK 54.
  </action>
  <verify>
    <automated>npx tsc --noEmit src/types/habits.ts 2>&1 | head -20</automated>
  </verify>
  <done>All dependencies installed. Types file exports all domain types with no TypeScript errors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prayer times service and location service</name>
  <files>src/services/prayer-times.ts, src/services/location.ts, __tests__/services/prayer-times.test.ts</files>
  <behavior>
    - getPrayerWindows(lat, lng, date, method) returns 5 PrayerWindow objects for the 5 daily prayers
    - Each PrayerWindow has correct start/end times (end = next prayer's start, Isha end = next Fajr)
    - Status is 'active' when now is between start and end
    - Status is 'upcoming' when now is before start
    - Status is 'passed' when now is after end
    - formatPrayerTime(date) returns human-readable time string (e.g., "5:30 AM")
    - All 6 calculation methods produce valid (non-NaN) prayer times for NYC coordinates
    - getCoordinates() returns {lat, lng} on permission grant, null on denial
  </behavior>
  <action>
    1. Create src/services/prayer-times.ts:
       - Import from 'adhan': Coordinates, CalculationMethod, PrayerTimes
       - Import PrayerWindow, PrayerName, CalcMethodKey from types/habits
       - CALC_METHOD_MAP: Record mapping CalcMethodKey to CalculationMethod factory functions (see research code examples)
       - getPrayerWindows(lat, lng, date, method): Calculate times using adhan, build 5 PrayerWindow objects. End time for each prayer = next prayer's start. For Isha, end = next day's Fajr (call getNextFajr). Determine status by comparing current time to window.
       - getNextFajr(lat, lng, date, method): Calculate tomorrow's Fajr time for Isha window end
       - formatPrayerTime(date: Date): Format as "h:mm A" using Intl.DateTimeFormat or manual formatting
       - NOTE: adhan returns Date objects. Always use local time for display. Store UTC for comparison.
    2. Create src/services/location.ts:
       - getCoordinates(): Request foreground permission via expo-location. On grant, get current position with Accuracy.Low (city-level is fine for prayer times). Return { lat, lng } or null on denial.
       - Export LocationResult type: { lat: number, lng: number } | null
       - Handle all permission states gracefully (denied, never ask again)
    3. Create __tests__/services/prayer-times.test.ts:
       - Test getPrayerWindows returns 5 windows for NYC coordinates with ISNA method
       - Test all 6 calculation methods produce non-NaN times
       - Test status determination: mock current time to verify active/upcoming/passed
       - Test formatPrayerTime produces readable output
       - Test getNextFajr returns a Date after the current day's Isha
       - Do NOT test location.ts (requires native module mocking, test manually on device)
  </action>
  <verify>
    <automated>npm test -- --testPathPattern="prayer-times" --no-coverage</automated>
  </verify>
  <done>Prayer times service returns correct windows for any coordinates/method. Location service handles permission grant/denial. All prayer-times tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Islamic habit presets and habit sorting logic</name>
  <files>src/domain/presets.ts, src/domain/habit-sorter.ts, __tests__/domain/presets.test.ts, __tests__/domain/habit-sorter.test.ts</files>
  <behavior>
    - HABIT_PRESETS contains ~15 presets across 6 categories
    - Salah category has exactly 5 presets (Fajr, Dhuhr, Asr, Maghrib, Isha) with type='salah'
    - getPresetsByCategory('salah') returns only salah presets
    - Each preset has all required fields (key, name, category, type, frequency, icon, difficultyTier, baseXp)
    - sortHabitsForDisplay: salah habits appear first in Fajr->Isha order, then non-salah by sortOrder
    - sortHabitsForDisplay: completed habits sink below uncompleted within each group
  </behavior>
  <action>
    1. Create src/domain/presets.ts:
       - Define HABIT_PRESETS array of PresetHabit objects (~15 total):
         - Salah (5): fajr, dhuhr, asr, maghrib, isha -- type='salah', category='salah', frequency='daily'
         - Quran (2): daily-reading, memorization -- type='custom', category='quran', frequency='daily'
         - Dhikr (2): morning-adhkar, evening-adhkar -- type='custom', category='dhikr', frequency='daily'
         - Dua (1): daily-dua -- type='custom', category='dua', frequency='daily'
         - Fasting (2): monday-thursday, white-days -- type='custom', category='fasting', frequency='specific_days'
         - Character (3): gratitude, patience, charity -- type='custom', category='character', frequency='daily'
       - Each preset: key (unique string), name, category, type, frequency, frequencyDays (for specific_days), icon (emoji), difficultyTier, baseXp, description
       - baseXp values: salah=30-50 (Fajr highest at 50 for difficulty), others 15-30
       - PRESET_CATEGORIES: array of { key: PresetCategory, displayName: string, icon: string }
       - getPresetsByCategory(category): filter HABIT_PRESETS by category
       - getPresetByKey(key): find single preset by key
    2. Create src/domain/habit-sorter.ts:
       - SALAH_ORDER: ['fajr', 'dhuhr', 'asr', 'maghrib', 'isha'] for chronological sort
       - sortHabitsForDisplay(habits: HabitWithStatus[]): HabitWithStatus[]
         - First: uncompleted salah habits in prayer time order (Fajr->Isha)
         - Then: uncompleted non-salah habits in sortOrder
         - Then: completed salah habits in prayer time order
         - Then: completed non-salah habits in sortOrder
       - Salah habits identified by type === 'salah' or presetKey starting with salah prayer name
    3. Write tests for both:
       - presets.test.ts: verify count, categories, all required fields, getPresetsByCategory
       - habit-sorter.test.ts: verify salah-first ordering, completed-last, mixed scenarios
  </action>
  <verify>
    <automated>npm test -- --testPathPattern="presets|habit-sorter" --no-coverage</automated>
  </verify>
  <done>15 presets defined across 6 categories. Sorting places salah first in chronological order, completed habits last. All tests pass.</done>
</task>

</tasks>

<verification>
npm test -- --testPathPattern="prayer-times|presets|habit-sorter" --no-coverage
All tests green. No TypeScript errors in new files.
adhan, expo-location, expo-haptics visible in package.json dependencies.
</verification>

<success_criteria>
- adhan, expo-location, expo-haptics, react-native-reanimated installed
- src/types/habits.ts exports all domain types
- getPrayerWindows returns 5 correct windows for any coordinates + method
- 15 preset habits defined with correct categories and XP values
- Habit sorter puts salah first chronologically, completed last
- All unit tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-habit-loop/03-01-SUMMARY.md`
</output>
