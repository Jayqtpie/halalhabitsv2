---
phase: 02-foundation-and-data-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - app.json
  - eas.json
  - babel.config.js
  - metro.config.js
  - jest.config.js
  - src/app/_layout.tsx
  - src/tokens/colors.ts
  - src/tokens/typography.ts
  - src/tokens/spacing.ts
  - src/tokens/radius.ts
  - src/tokens/motion.ts
  - src/tokens/index.ts
  - src/i18n/config.ts
  - src/i18n/locales/en/translation.json
  - src/types/common.ts
  - src/utils/uuid.ts
  - assets/fonts/Inter-Regular.ttf
  - assets/fonts/Inter-Bold.ttf
  - assets/fonts/Inter-SemiBold.ttf
  - assets/fonts/PressStart2P-Regular.ttf
  - __tests__/tokens/tokens.test.ts
  - __tests__/i18n/i18n.test.ts
autonomous: true
requirements: [FOUN-01, FOUN-06, FOUN-07]

must_haves:
  truths:
    - "Expo project builds successfully with TypeScript and development build config"
    - "Design tokens export all color, typography, spacing, radius, and motion values matching the blueprint"
    - "i18next initializes with English translations and detects device locale"
    - "Token system supports dark and light semantic color sets"
    - "All spacing values are multiples of 4px base unit"
  artifacts:
    - path: "src/tokens/colors.ts"
      provides: "Jewel tone palette primitives + dark/light semantic color sets"
      exports: ["palette", "colors"]
    - path: "src/tokens/typography.ts"
      provides: "Type scale with font families, sizes, weights, line heights"
      exports: ["typography", "fontFamilies"]
    - path: "src/tokens/spacing.ts"
      provides: "4px-based spacing scale"
      exports: ["spacing"]
    - path: "src/tokens/index.ts"
      provides: "Unified token re-export"
      exports: ["colors", "typography", "spacing", "radius", "motion"]
    - path: "src/i18n/config.ts"
      provides: "i18next initialization with expo-localization"
      exports: ["default"]
    - path: "jest.config.js"
      provides: "Jest configuration for Expo + TypeScript"
  key_links:
    - from: "src/tokens/colors.ts"
      to: "blueprint/08-ui-design-tokens.md"
      via: "hex values match blueprint palette"
      pattern: "#0D7C3D|#1B3A6B|#9B1B30|#FFD700"
    - from: "src/i18n/config.ts"
      to: "expo-localization"
      via: "getLocales() for initial language"
      pattern: "Localization\\.getLocales"
---

<objective>
Create the Expo project scaffold with development build configuration, the complete design token system (colors, typography, spacing, radius, motion), and i18n infrastructure. This is the foundation everything else plugs into.

Purpose: Establish the project skeleton, visual identity tokens, and internationalization so all subsequent plans can import tokens and translate strings from day one.
Output: Running Expo project with EAS config, full token system, i18next setup, test infrastructure, and shared types.
</objective>

<execution_context>
@C:/Users/jay_p/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jay_p/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-foundation-and-data-layer/02-CONTEXT.md
@.planning/phases/02-foundation-and-data-layer/02-RESEARCH.md
@blueprint/08-ui-design-tokens.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expo project scaffold with dev build config, test infra, and shared utilities</name>
  <files>
    package.json, tsconfig.json, app.json, eas.json, babel.config.js, metro.config.js,
    jest.config.js, src/types/common.ts, src/utils/uuid.ts, src/utils/date.ts
  </files>
  <action>
    1. Create Expo project: `npx create-expo-app@latest . --template blank-typescript` (or initialize in current directory if needed — adapt to the existing git repo structure).

    2. Install all Phase 2 dependencies:
       ```
       npx expo install expo-sqlite expo-font expo-localization expo-dev-client
       npm install drizzle-orm zustand i18next react-i18next uuid
       npm install -D drizzle-kit babel-plugin-inline-import @types/uuid jest-expo @testing-library/react-native @testing-library/jest-native
       ```

    3. Configure babel.config.js: Add `babel-plugin-inline-import` with `extensions: [".sql"]` for Drizzle migration files.

    4. Configure metro.config.js: Add `'sql'` to `config.resolver.sourceExts` array for Drizzle migration file resolution.

    5. Configure eas.json with three profiles: `development` (developmentClient: true, distribution: internal), `preview` (distribution: internal), `production` (autoSubmit: false). This is for cloud builds on Windows — do NOT attempt local builds.

    6. Configure jest.config.js using `jest-expo` preset, with `transformIgnorePatterns` for expo/react-native modules, `moduleFileExtensions` including ts/tsx, and `setupFilesAfterSetup` pointing to jest-native setup.

    7. Create src/types/common.ts with shared types:
       - `PrivacyLevel = 'PRIVATE' | 'SYNCABLE' | 'LOCAL_ONLY'`
       - `HabitCategory = 'salah' | 'worship' | 'dhikr' | 'fasting' | 'character' | 'custom'`
       - `HabitFrequency = 'daily' | 'weekly'`
       - `DifficultyTier = 'easy' | 'medium' | 'hard' | 'intense'`
       - `HabitStatus = 'active' | 'paused' | 'archived'`
       - `QuestType = 'daily' | 'weekly' | 'stretch' | 'recovery'`
       - `QuestStatus = 'available' | 'in_progress' | 'completed' | 'expired'`
       - `TitleRarity = 'common' | 'rare' | 'legendary'`
       - `XPSourceType = 'habit' | 'quest' | 'muhasabah' | 'mercy_recovery' | 'milestone'`
       - `DarkMode = 'auto' | 'dark' | 'light'`

    8. Create src/utils/uuid.ts: Thin wrapper around `uuid` v4 (`export function generateId(): string { return v4(); }`).

    9. Create src/utils/date.ts: `toISOString()` helper that returns current ISO timestamp string, for consistent date formatting across the app.

    10. Update app.json: Set `scheme` to "halalhabits", set `expo.plugins` to include `expo-dev-client`, set app name and slug.

    IMPORTANT: Use `paddingStart`/`paddingEnd` (not left/right) everywhere for RTL readiness per FOUN-07.
  </action>
  <verify>
    <automated>npx expo export --platform web 2>&1 | tail -5</automated>
  </verify>
  <done>Expo project builds successfully. All dependencies installed. EAS, Babel, Metro, and Jest configured. Shared types and utility modules exported.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Design token system (colors, typography, spacing, radius, motion)</name>
  <files>
    src/tokens/colors.ts, src/tokens/typography.ts, src/tokens/spacing.ts,
    src/tokens/radius.ts, src/tokens/motion.ts, src/tokens/index.ts,
    assets/fonts/Inter-Regular.ttf, assets/fonts/Inter-Bold.ttf,
    assets/fonts/Inter-SemiBold.ttf, assets/fonts/PressStart2P-Regular.ttf,
    __tests__/tokens/tokens.test.ts
  </files>
  <behavior>
    - colors.ts exports `palette` object with all primitive hex values (emerald-50 through emerald-900, sapphire, ruby, gold, surface, text, border)
    - colors.ts exports `colors` object with `dark` and `light` sub-objects containing identical semantic keys (background, surface, textPrimary, textSecondary, primary, error, xp, mercy, border, etc.)
    - typography.ts exports `fontFamilies` with Inter, Inter-Bold, Inter-SemiBold, PressStart2P
    - typography.ts exports `typography` with all 10 type scale entries from blueprint (heading-xl through hud-label) each with fontSize, lineHeight, fontWeight, fontFamily, letterSpacing
    - spacing.ts exports `spacing` with xs=4, sm=8, md=16, lg=24, xl=32, xxl=48
    - radius.ts exports `radius` tokens (sm=4, md=8, lg=12, xl=16, full=9999)
    - motion.ts exports duration and easing tokens
    - index.ts re-exports all token modules
    - All spacing values are multiples of 4
  </behavior>
  <action>
    Create the two-tier design token system per user decision (primitives + semantic, NOT three-tier).

    colors.ts: Extract ALL hex values from blueprint/08-ui-design-tokens.md:
    - Tier 1 (`palette`): emerald-50 through 900, sapphire-50 through 900, ruby-300 through 600, gold-300 through 700, surface-50 through 950, plus text-primary/secondary/muted, border-default/focus
    - Tier 2 (`colors.dark` and `colors.light`): Semantic keys mapping to palette values. Dark mode is the primary/default. Light mode uses same keys but light-appropriate palette values. Include: background, backgroundDeep, surface, textPrimary, textSecondary, textMuted, primary, primaryPressed, secondary, error, xp, mercy, border, borderFocus, success, warning, info. Also include rarity sub-object (common, rare, legendary with glow/badge/text colors). Also include HUD overlay tokens.

    typography.ts: Define fontFamilies constant and typography scale matching blueprint exactly:
    - heading-xl: 28px/36px, Bold, Inter-Bold, tracking-tight (-0.5)
    - heading-lg: 24px/32px, Bold, Inter-Bold, tracking-tight
    - heading-md: 20px/28px, SemiBold, Inter-SemiBold, tracking-tight
    - body-lg: 17px/26px, Regular, Inter, tracking-normal (0)
    - body-md: 15px/22px, Regular, Inter, tracking-normal
    - body-sm: 13px/18px, Regular, Inter, tracking-normal
    - caption: 11px/16px, Regular, Inter, tracking-wide (0.5)
    - hud-level: 16px/20px, Bold, PressStart2P, tracking-pixel (1)
    - hud-xp: 12px/16px, Bold, PressStart2P, tracking-pixel
    - hud-label: 10px/14px, Bold, PressStart2P, tracking-pixel

    spacing.ts: xs=4, sm=8, md=16, lg=24, xl=32, xxl=48. Also export component spacing (card padding, button padding, etc.) from blueprint.

    radius.ts: sm=4, md=8, lg=12, xl=16, full=9999 (pill shape).

    motion.ts: Duration tokens (instant=100, fast=200, normal=300, slow=500, celebration=800). Easing tokens (default='ease-in-out', bounce, spring for Reanimated).

    index.ts: Re-export everything as named exports.

    Download font files: Download Inter (Regular, Bold, SemiBold) and Press Start 2P TTF files from Google Fonts into assets/fonts/. Use curl or npx google-fonts-helper.
  </action>
  <verify>
    <automated>npx jest --testPathPattern=tokens --no-coverage</automated>
  </verify>
  <done>Token system exports all values matching blueprint. Tests verify all semantic color keys exist in both dark and light modes. All spacing values are multiples of 4. Typography scale has all 10 entries. Font files present in assets/fonts/.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: i18n infrastructure with i18next and expo-localization</name>
  <files>
    src/i18n/config.ts, src/i18n/locales/en/translation.json, __tests__/i18n/i18n.test.ts
  </files>
  <behavior>
    - i18next initializes without errors
    - Default language is detected from device locale (falls back to 'en')
    - Translation keys resolve: t('app.name') returns 'HalalHabits'
    - Missing keys return the key itself (standard i18next behavior)
    - compatibilityJSON is set to 'v4'
  </behavior>
  <action>
    Create src/i18n/config.ts:
    - Import i18next, initReactI18next, and expo-localization
    - Initialize with: resources (en namespace), lng from Localization.getLocales()[0]?.languageCode ?? 'en', fallbackLng 'en', interpolation.escapeValue false, compatibilityJSON 'v4'
    - Export default i18n instance

    Create src/i18n/locales/en/translation.json with initial keys:
    - app.name: "HalalHabits"
    - app.tagline: "Your discipline grows stronger"
    - tabs.home: "Home"
    - tabs.habits: "Habits"
    - tabs.quests: "Quests"
    - tabs.profile: "Profile"
    - common.loading: "Loading..."
    - common.error: "Something went wrong"
    - common.retry: "Try Again"
    - common.cancel: "Cancel"
    - common.save: "Save"
    - common.delete: "Delete"
    - placeholder.comingSoon: "Coming in a future update"
    - placeholder.phaseIndicator: "Phase {{phase}}"

    Use the wise mentor voice per CLAUDE.md ("Your discipline grows stronger" not "You're doing great!").
  </action>
  <verify>
    <automated>npx jest --testPathPattern=i18n --no-coverage</automated>
  </verify>
  <done>i18next initializes with English translations. All tab names and common strings have translation keys. Device locale detection works with English fallback.</done>
</task>

</tasks>

<verification>
- `npx expo export --platform web` completes without errors
- `npx jest --no-coverage` runs token and i18n tests green
- All font TTF files exist in assets/fonts/
- Token files export typed constants with no `any` types
- i18n config initializes without errors
</verification>

<success_criteria>
- Expo project scaffolded with TypeScript, dev build config, and EAS profiles
- Jest test infrastructure configured and running
- Complete design token system matching blueprint (colors, typography, spacing, radius, motion)
- Dark and light semantic color sets with identical keys
- i18next initialized with English translations and device locale detection
- All shared types exported from src/types/common.ts
</success_criteria>

<output>
After completion, create `.planning/phases/02-foundation-and-data-layer/02-01-SUMMARY.md`
</output>
