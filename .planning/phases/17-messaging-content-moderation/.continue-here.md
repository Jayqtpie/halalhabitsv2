---
phase: 17-messaging-content-moderation
task: 0
total_tasks: 5
status: planned
last_updated: "2026-03-30T02:57:11.241Z"
---

<current_state>
Phase 17 planning is COMPLETE. Research and all 5 plan files created. Plan checker was attempted but hit usage limits — verification not yet run. No code has been written yet.
</current_state>

<completed_work>

- Research: 17-RESEARCH.md — Full domain research covering schema (already exists), leo-profanity (already installed), Edge Function patterns, offline queue strategy, Supabase Realtime, report/block integration
- Plan 17-01: Message domain engine (message-engine.ts) + profanity utilities with TDD — Wave 1
- Plan 17-02: messageRepo (SYNCABLE pattern) + message_reports schema + privacy gate + Supabase migration — Wave 1
- Plan 17-03: messageStore (Zustand) + offline message queue service with Edge Function routing — Wave 2
- Plan 17-04: Supabase Edge Function `moderate-message` for server-side content filtering — Wave 1
- Plan 17-05: Chat UI (ChatScreen, MessageBubble, ChatComposer, ReportMessageSheet) + Expo Router routes — Wave 3
</completed_work>

<remaining_work>

- Plan verification: gsd-plan-checker needs to run (hit usage limit on first attempt)
- Execute all 5 plans (Wave 1: 17-01 + 17-02 + 17-04 parallel, Wave 2: 17-03, Wave 3: 17-05)
- Phase verification after execution
</remaining_work>

<decisions_made>

- Messages route through Edge Function (not generic sync engine) to maintain server-side moderation for both online and offline messages
- Dedicated message-queue.ts service flushes offline messages through Edge Function on reconnect
- Supabase Realtime uses postgres_changes (not Broadcast) for delivery guarantees tied to DB state
- Edge Function extracts senderId from JWT — never trusts client-supplied senderId
- Zero new npm packages — leo-profanity already installed, all other deps in place
- message_reports is a new Supabase-only table (no local SQLite equivalent needed)
- Inverted FlatList for chat UI (newest messages at bottom)
</decisions_made>

<blockers>

- Plan checker verification not yet run (usage limit hit) — should run before execution
- Islamic content blocklist must be hand-curated (no open-source list exists) — start minimal
</blockers>

<context>
Phase 17 is the final phase of v2.0 milestone. All prior phases (11-16) are complete. The messages table schema already exists in both SQLite and Supabase from Phase 11. leo-profanity is already installed from Phase 16 (CreateDuoQuestSheet). The main new work is: domain engine, repo, store, Edge Function, and chat UI. The plan checker should be run first before executing.
</context>

<next_action>
Run `/gsd:plan-phase 17 --skip-research` to re-verify plans (plan checker), then `/gsd:execute-phase 17` to begin execution starting with Wave 1 (plans 01, 02, 04 in parallel).
</next_action>
