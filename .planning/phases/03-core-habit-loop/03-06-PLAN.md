---
phase: 03-core-habit-loop
plan: 06
type: execute
wave: 4
depends_on: [03-04, 03-05]
files_modified:
  - src/components/habits/MercyModeBanner.tsx
  - src/components/habits/MercyModeRecoveryTracker.tsx
  - src/components/prayer/PrayerTimeWindow.tsx
  - src/components/prayer/CalcMethodPicker.tsx
  - src/components/calendar/HabitCalendar.tsx
  - src/components/habits/HabitCard.tsx
  - app/(tabs)/habits.tsx
autonomous: false
requirements: [HBIT-06, PRAY-02, PRAY-03, PRAY-04, STRK-02, STRK-03, STRK-04, STRK-05]
user_setup: []

must_haves:
  truths:
    - "Salah cards show correct prayer time windows with active/upcoming/passed status"
    - "User can select prayer calculation method from settings"
    - "Mercy Mode banner appears after streak break with compassionate recovery copy"
    - "User can complete 3-day recovery and see progress tracker"
    - "User can choose 'Start fresh' as equally valid alternative to recovery"
    - "User can view habit history in calendar/heatmap for any habit"
    - "No shame language appears anywhere in the UI"
    - "Streak Shield bonus is visually indicated when salah completed within window"
  artifacts:
    - path: "src/components/habits/MercyModeBanner.tsx"
      provides: "Compassionate streak break notification banner"
      min_lines: 30
    - path: "src/components/habits/MercyModeRecoveryTracker.tsx"
      provides: "3-step visual progress tracker for recovery"
      min_lines: 25
    - path: "src/components/prayer/PrayerTimeWindow.tsx"
      provides: "Prayer time window badge for salah cards"
      min_lines: 20
    - path: "src/components/prayer/CalcMethodPicker.tsx"
      provides: "Prayer calculation method selector"
      min_lines: 30
    - path: "src/components/calendar/HabitCalendar.tsx"
      provides: "Monthly calendar heatmap showing completions"
      min_lines: 50
  key_links:
    - from: "src/components/habits/MercyModeBanner.tsx"
      to: "src/stores/habitStore.ts"
      via: "startRecovery and resetStreak callbacks"
      pattern: "startRecovery|resetStreak"
    - from: "src/components/prayer/PrayerTimeWindow.tsx"
      to: "src/services/prayer-times.ts"
      via: "receives PrayerWindow data from parent"
      pattern: "PrayerWindow"
    - from: "src/components/calendar/HabitCalendar.tsx"
      to: "src/db/repos/completionRepo.ts"
      via: "queries completion dates for heatmap"
      pattern: "completionRepo\\.getForDateRange"
---

<objective>
Build the remaining UI: prayer time windows on salah cards, calculation method picker, Mercy Mode banner and recovery tracker, and calendar/heatmap view. Then verify the complete habit loop on device.

Purpose: These features complete the daily discipline loop. Prayer time awareness makes salah habits dynamic and living. Mercy Mode ensures compassionate recovery. Calendar view gives users a sense of their journey.
Output: 5 new components, updated HabitCard and habits screen. Complete Phase 3 feature set.
</objective>

<execution_context>
@C:/Users/jay_p/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jay_p/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-core-habit-loop/03-CONTEXT.md

@.planning/phases/03-core-habit-loop/03-01-SUMMARY.md
@.planning/phases/03-core-habit-loop/03-04-SUMMARY.md
@.planning/phases/03-core-habit-loop/03-05-SUMMARY.md

<interfaces>
From src/types/habits.ts:
```typescript
export interface PrayerWindow {
  name: PrayerName; displayName: string; start: Date; end: Date;
  status: 'active' | 'upcoming' | 'passed';
}
export type CalcMethodKey = 'ISNA' | 'MWL' | 'Egyptian' | 'Karachi' | 'UmmAlQura' | 'MoonsightingCommittee';
export interface MercyModeState { active: boolean; recoveryDay: number; preBreakStreak: number; }
export const CALC_METHODS: CalcMethodInfo[];
```

From src/services/prayer-times.ts:
```typescript
export function getPrayerWindows(lat, lng, date, method): PrayerWindow[];
export function formatPrayerTime(date: Date): string;
```

From src/stores/habitStore.ts:
```typescript
startRecovery: (habitId: string) => Promise<void>;
resetStreak: (habitId: string) => Promise<void>;
mercyModes: Record<string, MercyModeState>;
```

From src/db/repos/completionRepo.ts:
```typescript
getForDateRange(habitId, startDate, endDate): Promise<HabitCompletion[]>;
```

Locked Mercy Mode copy:
- Banner: "Your momentum paused. The door of tawbah is always open."
- Button: "Begin Recovery"
- Dismissible but persistent until recovery starts or today's habits completed
- Recovery: 3-step visual tracker with encouraging copy at each step
- Fresh start: "Start fresh" resets to 0, presented as equally valid
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prayer time windows and calculation method picker</name>
  <files>src/components/prayer/PrayerTimeWindow.tsx, src/components/prayer/CalcMethodPicker.tsx, src/components/habits/HabitCard.tsx, src/stores/settingsStore.ts</files>
  <action>
    1. Create src/components/prayer/PrayerTimeWindow.tsx:
       - Props: prayerWindow (PrayerWindow | null)
       - If null, render nothing (non-salah habits)
       - Display: time range (e.g., "12:05 PM - 3:30 PM") + status badge
       - Status badges per locked decisions:
         - Active: green dot + "Now" (use emerald token)
         - Upcoming: clock emoji or icon + "In X hours/min" (calculate from now to start)
         - Passed: muted text + "Passed" (use muted/dimmed token)
       - Compact design to fit in HabitCard without crowding
       - Use formatPrayerTime from prayer-times service for time display

    2. Create src/components/prayer/CalcMethodPicker.tsx:
       - Modal or bottom sheet showing all calculation methods from CALC_METHODS
       - Each method: display name + brief description (e.g., "ISNA - North America standard")
       - Current selection highlighted with emerald accent
       - On select: call settingsStore.setPrayerCalcMethod(method)
       - Accessible from habits screen settings gear icon or from a "Prayer Settings" link

    3. Update src/stores/settingsStore.ts:
       - Add locationLat, locationLng, locationName to state
       - Add setLocation(lat, lng, name?) method
       - These persist via the existing SQLite persist middleware

    4. Update src/components/habits/HabitCard.tsx:
       - Integrate PrayerTimeWindow component for salah habits
       - Show PrayerTimeWindow between habit name and streak display
       - Prayer window data comes from habit.prayerWindow (already in HabitWithStatus)
       - Add Streak Shield visual indicator: when salah habit is completed within prayer window, show a small shield icon or "Streak Shield" badge near the streak count. This is purely visual -- the XP bonus calculation is Phase 4.

    5. PRAY-04 stub: Add a TODO comment in the habits tab or prayer-times service: "// TODO(Phase 6): Schedule local notifications for prayer time reminders (PRAY-04, NOTF-01)". Do NOT implement notification scheduling -- that's Phase 6 infrastructure.
  </action>
  <verify>
    <automated>npx tsc --noEmit src/components/prayer/PrayerTimeWindow.tsx src/components/prayer/CalcMethodPicker.tsx 2>&1 | head -20</automated>
  </verify>
  <done>Salah cards show prayer time windows with active/upcoming/passed badges. Calculation method picker allows selection from 6+ methods. Settings store persists location and calc method. Streak Shield indicator shown for in-window completions. PRAY-04 deferred to Phase 6 with TODO.</done>
</task>

<task type="auto">
  <name>Task 2: Mercy Mode banner, recovery tracker, and calendar heatmap</name>
  <files>src/components/habits/MercyModeBanner.tsx, src/components/habits/MercyModeRecoveryTracker.tsx, src/components/calendar/HabitCalendar.tsx, app/(tabs)/habits.tsx</files>
  <action>
    1. Create src/components/habits/MercyModeBanner.tsx:
       - Props: habitName (string), mercyState (MercyModeState), onStartRecovery (callback), onStartFresh (callback), onDismiss (callback)
       - Banner at top of habits tab (above progress bar) when any habit has active mercy mode
       - Copy EXACTLY per locked decision: "Your momentum paused. The door of tawbah is always open."
       - "Begin Recovery" button (primary, emerald/jewel-tone)
       - "Start fresh" link (secondary, equally visible, not hidden or small)
       - Dismissible (X button) but persistent -- reappears on next visit until user acts or completes today's habits
       - If multiple habits in mercy mode, show banner for the first one with "(and X more)" indicator
       - Warm, inviting visual tone. NO shame, no guilt, no urgency. This is a door opening, not a warning.
       - Use gentle animation: slide in from top with FadeIn

    2. Create src/components/habits/MercyModeRecoveryTracker.tsx:
       - Props: recoveryDay (0-3), habitName (string)
       - 3-step horizontal progress indicator (circles connected by lines)
       - Completed steps: filled emerald circle with checkmark
       - Current step: pulsing/glowing circle
       - Upcoming steps: muted outline circle
       - Encouraging copy at each step:
         - Day 1 complete: "One step forward. Well done."
         - Day 2 complete: "Two steps. Your consistency is building."
         - Day 3 complete: "Recovery complete! Your momentum has been restored." + show restored streak count
       - Display within or near the relevant HabitCard, or as a compact inline element

    3. Create src/components/calendar/HabitCalendar.tsx:
       - Props: habitId (string), habitName (string)
       - Simple monthly calendar grid (custom build per research recommendation, no external library)
       - Shows current month by default, arrows to navigate months
       - Each day cell: colored if completion exists for that day
         - Completed: jewel-tone emerald fill
         - Today (no completion): subtle highlight/outline
         - No completion (past): empty/muted
       - Heat intensity not needed for v1 -- simple completed/not-completed binary
       - Loads data via completionRepo.getForDateRange for visible month
       - Current streak count and longest streak shown above calendar
       - Month/year header in Press Start 2P font
       - Accessible from HabitCard long-press menu or a "History" option in EditHabitSheet
       - Can be a modal, bottom sheet, or separate route (Claude's discretion)

    4. Wire into app/(tabs)/habits.tsx:
       - Show MercyModeBanner at top when store.mercyModes has any active entries
       - Connect banner callbacks to store.startRecovery and store.resetStreak
       - Show recovery tracker on habit cards that are mid-recovery
       - Add access point to HabitCalendar (from card long-press or edit sheet)
  </action>
  <verify>
    <automated>npx tsc --noEmit src/components/habits/MercyModeBanner.tsx src/components/habits/MercyModeRecoveryTracker.tsx src/components/calendar/HabitCalendar.tsx 2>&1 | head -20</automated>
  </verify>
  <done>Mercy Mode banner shows compassionate recovery prompt with exact locked copy. Recovery tracker visualizes 3-day progress. Calendar heatmap shows monthly completion history. All wired into habits tab.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete Phase 3 habit loop: daily habit list with completion, prayer time windows, streak tracking, Mercy Mode, preset library, custom habits, calendar history</what-built>
  <how-to-verify>
    1. Open the app on device via Expo Go
    2. Navigate to Habits tab -- should show empty state with "+" button
    3. Tap "+" -- preset library should show categories (Salah, Quran, Dhikr, etc.)
    4. Add Fajr and one custom habit (e.g., "Quran Reading")
       - First salah add should request location permission
    5. Return to habits tab -- should see Fajr with prayer time window, Quran Reading below
    6. Tap to complete Fajr -- should see:
       - Haptic pulse
       - Checkmark animation with glow
       - Card dims and moves to bottom
       - Progress bar updates ("1 of 2 complete")
       - Streak shows "1-day momentum" (or similar)
    7. Check prayer time window shows correct status (active/upcoming/passed based on current time)
    8. If possible, check Mercy Mode: this requires a streak break (day gap), so may need to test by:
       - Looking at the banner styling with a mock/forced state, or
       - Confirming the banner copy matches: "Your momentum paused. The door of tawbah is always open."
    9. Check that NO shame language appears anywhere ("missed", "failed", "lost" in negative framing)
    10. Long-press a habit to verify edit/archive options exist
    11. Check calendar view loads for a habit (even if empty)
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
npx tsc --noEmit
npm test -- --no-coverage
All TypeScript compiles. All tests pass. Complete habit loop functional on device.
</verification>

<success_criteria>
- Salah cards show live prayer time windows with correct status badges
- Calculation method selectable and persisted
- Mercy Mode banner uses exact locked copy, no shame language anywhere
- 3-day recovery tracker shows progress with encouraging copy
- "Start fresh" presented as equally valid choice
- Calendar shows monthly completion history
- PRAY-04 deferred with TODO comment
- User verification of complete habit loop on device
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-habit-loop/03-06-SUMMARY.md`
</output>
