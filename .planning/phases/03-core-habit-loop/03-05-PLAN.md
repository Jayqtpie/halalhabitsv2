---
phase: 03-core-habit-loop
plan: 05
type: execute
wave: 3
depends_on: [03-01, 03-03]
files_modified:
  - src/components/habits/PresetLibrary.tsx
  - src/components/habits/CustomHabitForm.tsx
  - src/components/habits/EditHabitSheet.tsx
  - app/add-habit.tsx
autonomous: true
requirements: [HBIT-01, HBIT-02, HBIT-05]

must_haves:
  truths:
    - "User can browse preset habits organized by category"
    - "User can select individual salah prayers (not all-or-nothing)"
    - "User can create a custom habit with name, frequency, and optional time window"
    - "User can edit an existing habit's name, frequency, and time window"
    - "User can archive a habit (soft delete)"
    - "Adding a preset creates a real habit record in the database"
  artifacts:
    - path: "src/components/habits/PresetLibrary.tsx"
      provides: "Categorized preset selection grid"
      min_lines: 60
    - path: "src/components/habits/CustomHabitForm.tsx"
      provides: "Form for creating custom habits"
      min_lines: 50
    - path: "src/components/habits/EditHabitSheet.tsx"
      provides: "Edit/archive bottom sheet for existing habits"
      min_lines: 40
    - path: "app/add-habit.tsx"
      provides: "Add habit screen (route) hosting preset library + custom form"
      min_lines: 30
  key_links:
    - from: "src/components/habits/PresetLibrary.tsx"
      to: "src/domain/presets.ts"
      via: "imports PRESET_CATEGORIES, getPresetsByCategory"
      pattern: "getPresetsByCategory"
    - from: "src/components/habits/PresetLibrary.tsx"
      to: "src/stores/habitStore.ts"
      via: "addHabit creates habit from selected preset"
      pattern: "addHabit"
    - from: "app/add-habit.tsx"
      to: "app/(tabs)/habits.tsx"
      via: "router.back() after habit creation"
      pattern: "router\\.back"
---

<objective>
Build the habit creation and management flows: preset library for browsing Islamic habit presets by category, custom habit creation form, and edit/archive functionality for existing habits.

Purpose: Users need to populate their habit list. The preset library makes onboarding frictionless (tap to add a salah prayer), while custom habits allow personalization. Edit/archive lets users maintain their list over time.
Output: 3 components + 1 new route screen. Full habit CRUD lifecycle.
</objective>

<execution_context>
@C:/Users/jay_p/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jay_p/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-core-habit-loop/03-CONTEXT.md

@.planning/phases/03-core-habit-loop/03-01-SUMMARY.md
@.planning/phases/03-core-habit-loop/03-03-SUMMARY.md

<interfaces>
From src/domain/presets.ts (Plan 01):
```typescript
export const HABIT_PRESETS: PresetHabit[];
export const PRESET_CATEGORIES: { key: PresetCategory, displayName: string, icon: string }[];
export function getPresetsByCategory(category: PresetCategory): PresetHabit[];
export function getPresetByKey(key: string): PresetHabit | undefined;
```

From src/stores/habitStore.ts:
```typescript
addHabit: (habit: NewHabit) => Promise<void>;
archiveHabit: (habitId: string) => Promise<void>;
updateHabit: (habitId: string, data: Partial) => Promise<void>;
```

From src/types/database.ts:
```typescript
export type NewHabit = InferInsertModel<typeof habits>;
// NewHabit fields: id, userId, name, type, presetKey, category, frequency,
// frequencyDays, timeWindowStart, timeWindowEnd, difficultyTier, baseXp,
// status, sortOrder, icon, createdAt, updatedAt
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Preset library and add-habit screen</name>
  <files>src/components/habits/PresetLibrary.tsx, app/add-habit.tsx</files>
  <action>
    1. Create app/add-habit.tsx as a modal/push screen (Expo Router):
       - Route: /add-habit (outside tabs, pushed from habits tab "+" button)
       - Stack header with "Add Habit" title in Press Start 2P font, back button
       - Two sections: "Choose a preset" (PresetLibrary) and "Create custom" link at bottom
       - "Create custom" navigates to inline custom form (can be a toggle/section within same screen or a separate route -- Claude's discretion on simplest approach)
       - After any habit is added, call router.back() to return to habits tab

    2. Create src/components/habits/PresetLibrary.tsx:
       - Shows categories as expandable sections or tabs: Salah, Quran, Dhikr, Dua, Fasting, Character
       - Each category header: icon + category name
       - Each preset item: icon + name + description + "Add" button
       - Salah presets are individually selectable per locked decision. Each prayer is a separate tappable item (Fajr, Dhuhr, Asr, Maghrib, Isha). User picks which ones they want.
       - On tap "Add":
         a. Build NewHabit from preset data (map PresetHabit fields to NewHabit)
         b. Generate id via generateId()
         c. Set userId to hardcoded 'default-user' for now
         d. Set createdAt and updatedAt to new Date().toISOString()
         e. Call store.addHabit(newHabit)
         f. Show brief success feedback (toast or checkmark on the item)
         g. If salah preset, request location permission via location service (needed for prayer times). Store coordinates in settings if granted.
       - Already-added presets: check if habit with matching presetKey exists in store.habits. If yes, show as "Added" (disabled) to prevent duplicates.
       - Style: jewel-tone category headers, dark card backgrounds, Inter body text, consistent with app theme
  </action>
  <verify>
    <automated>npx tsc --noEmit app/add-habit.tsx src/components/habits/PresetLibrary.tsx 2>&1 | head -20 && npm test -- --testPathPattern="habits|prayer|streak|presets|habit-sorter" --no-coverage --passWithNoTests 2>&1 | tail -10</automated>
  </verify>
  <done>Preset library shows ~15 habits across 6 categories. Salah prayers individually selectable. Tapping "Add" creates habit in database. Already-added presets shown as disabled. First salah add triggers location permission request. All existing domain tests still pass.</done>
</task>

<task type="auto">
  <name>Task 2: Custom habit form and edit/archive sheet</name>
  <files>src/components/habits/CustomHabitForm.tsx, src/components/habits/EditHabitSheet.tsx</files>
  <action>
    1. Create src/components/habits/CustomHabitForm.tsx:
       - Form fields (Claude's discretion on exact UX, keeping it simple):
         - Name (text input, required, 3-50 chars)
         - Category (picker/selector from PresetCategory options, default 'character')
         - Frequency: 'daily' | 'specific_days' (toggle or picker)
         - If specific_days: day selector (Mon-Sun checkboxes)
         - Optional time window: start time + end time pickers (for habits with specific times)
         - Icon: small emoji picker or predefined list of 10-15 relevant emojis
       - Difficulty tier auto-set to 'medium', baseXp auto-set to 20 (reasonable default)
       - On submit: validate, build NewHabit, call store.addHabit, navigate back
       - Style: dark form inputs with jewel-tone accents, consistent with app theme
       - Error states: show validation messages inline (name too short, no days selected for specific_days)

    2. Create src/components/habits/EditHabitSheet.tsx:
       - Bottom sheet or modal that appears when user long-presses or taps edit on a HabitCard
       - Shows current habit data in editable form (reuse CustomHabitForm fields or simplified version)
       - Editable fields: name, frequency, frequencyDays, timeWindowStart/End, icon
       - NOT editable: type, category, presetKey (these are structural)
       - "Save" button: calls store.updateHabit(habitId, changes)
       - "Archive" button: confirmation prompt ("Archive this habit? You can restore it later."), then calls store.archiveHabit(habitId)
       - Archive is soft-delete (status='archived'), NOT permanent delete
       - Style: consistent with app theme, clear destructive action styling for archive (ruby/red tones)

    3. Wire edit sheet into HabitCard (Plan 04): Add an onEdit or onLongPress prop to HabitCard that opens EditHabitSheet. If HabitCard from Plan 04 doesn't have this prop, add it. The sheet should receive the habit data and callbacks.
  </action>
  <verify>
    <automated>npx tsc --noEmit src/components/habits/CustomHabitForm.tsx src/components/habits/EditHabitSheet.tsx 2>&1 | head -20 && npm test -- --testPathPattern="habits|prayer|streak|presets|habit-sorter" --no-coverage --passWithNoTests 2>&1 | tail -10</automated>
  </verify>
  <done>Custom habit form validates and creates habits with name, frequency, time window. Edit sheet allows modifying existing habits. Archive soft-deletes with confirmation. Both styled consistently with app theme. All existing domain tests still pass (no regressions).</done>
</task>

</tasks>

<verification>
npx tsc --noEmit && npm test -- --no-coverage --passWithNoTests
Full TypeScript compilation passes. All existing tests pass (no regressions). add-habit route accessible from habits tab "+" button.
Manual: can add preset, create custom, edit existing, archive.
</verification>

<success_criteria>
- Preset library shows all ~15 presets across 6 categories
- Individual salah prayers selectable (not all-or-nothing)
- Custom habit form creates valid habit records
- Edit sheet allows modifying habit properties
- Archive soft-deletes with confirmation
- Already-added presets shown as disabled
- First salah add triggers location permission
- No TypeScript errors
- All existing domain/store tests pass (no regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-habit-loop/03-05-SUMMARY.md`
</output>
