---
phase: 06-onboarding-profile-and-notifications
plan: 03
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - app/(tabs)/profile.tsx
  - app/settings.tsx
  - app/prayer-reminders.tsx
  - app/your-data.tsx
  - src/components/profile/ProfileHeader.tsx
  - src/components/profile/StatsGrid.tsx
  - src/components/profile/TrophyCase.tsx
  - src/components/profile/StreakBars.tsx
  - src/components/profile/NiyyahDisplay.tsx
  - src/components/settings/SettingsList.tsx
  - src/components/settings/PrayerReminderRow.tsx
  - src/services/data-export.ts
  - __tests__/services/data-export.test.ts
autonomous: false
requirements:
  - PROF-01
  - PROF-02
  - PROF-03
  - PROF-04
  - NOTF-04

must_haves:
  truths:
    - "User can view their profile with character sprite, active title, level, XP, streaks, and titles"
    - "User can access settings from profile and configure all notification categories"
    - "User can configure prayer calculation method and notification lead time per prayer"
    - "User can toggle Arabic terminology on/off and it applies app-wide"
    - "User can export all data as JSON and share via native share sheet"
    - "User can delete all data, which resets stores and re-triggers onboarding"
    - "Dark mode is the default (v1 dark-only, PROF-04 satisfied by existing dark-only decision)"
  artifacts:
    - path: "app/(tabs)/profile.tsx"
      provides: "RPG character sheet profile screen replacing PlaceholderScreen"
    - path: "app/settings.tsx"
      provides: "Grouped settings screen (Prayer, Notifications, Appearance, About)"
    - path: "app/prayer-reminders.tsx"
      provides: "Per-prayer notification sub-screen"
    - path: "app/your-data.tsx"
      provides: "Data management screen (view, export JSON, delete all)"
    - path: "src/services/data-export.ts"
      provides: "collectAllUserData + exportUserData + deleteAllUserData"
  key_links:
    - from: "app/(tabs)/profile.tsx"
      to: "src/stores/gameStore.ts"
      via: "useGameStore for level, totalXP, equippedTitle"
      pattern: "useGameStore"
    - from: "app/(tabs)/profile.tsx"
      to: "src/stores/habitStore.ts"
      via: "useHabitStore for streak data"
      pattern: "useHabitStore"
    - from: "app/settings.tsx"
      to: "src/stores/settingsStore.ts"
      via: "reads/writes all notification prefs"
      pattern: "useSettingsStore"
    - from: "app/your-data.tsx"
      to: "src/services/data-export.ts"
      via: "exportUserData and deleteAllUserData"
      pattern: "exportUserData|deleteAllUserData"
---

<objective>
Build the Profile screen (RPG character sheet), Settings screen (grouped sections), Prayer Reminders sub-screen, Your Data screen (export/delete), and data export service.

Purpose: Profile is the player's identity hub; Settings gives control over the app experience; Data management fulfills privacy requirements.
Output: 4 screens, 7 components, data export service with test.
</objective>

<execution_context>
@C:/Users/jay_p/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jay_p/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-onboarding-profile-and-notifications/06-CONTEXT.md
@.planning/phases/06-onboarding-profile-and-notifications/06-RESEARCH.md
@.planning/phases/06-onboarding-profile-and-notifications/06-01-SUMMARY.md

@app/(tabs)/profile.tsx
@app/_layout.tsx
@src/stores/settingsStore.ts
@src/stores/gameStore.ts
@src/stores/habitStore.ts
@src/components/hud/CharacterSprite.tsx
@src/components/quests/TitleGrid.tsx
@src/tokens/colors.ts
@src/tokens/typography.ts
@src/tokens/spacing.ts
@src/db/client.ts

<interfaces>
<!-- From existing codebase + Plan 01 outputs -->

From src/stores/gameStore.ts:
```typescript
// Key selectors for profile:
currentLevel: number;
totalXP: number;
equippedTitleId: string | null;
titles: Title[];  // earned titles
loadGame: () => Promise<void>;
```

From src/stores/habitStore.ts:
```typescript
// Key selectors for profile:
habits: HabitWithStatus[];
getHabitsForDisplay: () => HabitWithStatus[];
```

From src/stores/settingsStore.ts (extended in Plan 01):
```typescript
prayerCalcMethod: string;
prayerReminders: Record<PrayerName, { enabled: boolean; leadMinutes: number; followUpEnabled: boolean }>;
muhasabahNotifEnabled: boolean;
muhasabahReminderTime: string;
streakMilestonesEnabled: boolean;
questExpiringEnabled: boolean;
morningMotivationEnabled: boolean;
arabicTermsEnabled: boolean;
soundEnabled: boolean;
hapticEnabled: boolean;
darkMode: DarkMode;
selectedNiyyahs: string[];
characterPresetId: string | null;
onboardingComplete: boolean;
// setters for all above
```

From src/components/quests/TitleGrid.tsx:
```typescript
// Existing component showing title grid with rarity grouping
// Can be adapted/reused for profile trophy case
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Profile screen + components</name>
  <files>
    app/(tabs)/profile.tsx,
    src/components/profile/ProfileHeader.tsx,
    src/components/profile/StatsGrid.tsx,
    src/components/profile/TrophyCase.tsx,
    src/components/profile/StreakBars.tsx,
    src/components/profile/NiyyahDisplay.tsx
  </files>
  <action>
    **Profile Screen (app/(tabs)/profile.tsx):**
    Replace PlaceholderScreen with a ScrollView-based RPG character sheet layout. Read from gameStore (level, totalXP, equippedTitleId, titles) and habitStore (habits with streaks) and settingsStore (selectedNiyyahs, characterPresetId).

    Header area: gear icon (Pressable) in top-right navigates to settings screen via router.push('/settings'). "Your Data" text link navigates to router.push('/your-data').

    **ProfileHeader component:**
    - CharacterSprite (reuse from HUD) centered at top
    - Active Identity Title below sprite (from gameStore.equippedTitleId, look up title name)
    - Level display: "Level {currentLevel}" in Press Start 2P
    - XP progress bar showing progress to next level (reuse pattern from XPProgressBar)

    **StatsGrid component:**
    - 3 horizontal cards in a row:
      1. Total XP: gameStore.totalXP formatted with commas
      2. Best Active Streak: max of all habit streaks currentCount
      3. Days Active: calculate from earliest habit creation date or fallback to "1"
    - Each card: dark surface card with jewel tone accent border, label above, value below
    - Cards should be equal width using flex

    **TrophyCase component:**
    - Adapt existing TitleGrid pattern for profile context
    - Show all 26 titles in a grid (3 columns). Earned titles glow with rarity-colored border (Common=emerald, Rare=sapphire, Legendary=gold). Locked titles show silhouette (opacity 0.3) with unlock condition hint text on tap.
    - Section heading: "Identity Titles"

    **StreakBars component:**
    - For each active habit: horizontal bar showing habit name + current streak count
    - Bar fill proportional to streak relative to longest streak across all habits
    - Best streak highlighted with gold accent
    - If no habits have streaks, show encouraging empty state

    **NiyyahDisplay component:**
    - Section heading: "Your Niyyah"
    - Render selectedNiyyahs as styled chips/pills (same style as onboarding but read-only)
    - If empty (shouldn't happen post-onboarding), show nothing

    **UI/UX Guidelines:**
    - ScrollView with dark background, consistent spacing (spacing.lg between sections)
    - RPG character sheet feel — the character sprite and title are the hero
    - 44x44px minimum touch targets on gear icon and data link
    - Press Start 2P for section headings, Inter for body
  </action>
  <verify>
    <automated>npx expo export --platform web --output-dir /tmp/expo-export-check 2>&1 | head -5 || echo "Build check complete"</automated>
  </verify>
  <done>
    Profile tab shows RPG character sheet with sprite, title, level, XP bar, stats grid, trophy case, streak bars, and niyyah display. Gear icon navigates to settings. "Your Data" link navigates to data management.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Settings, Prayer Reminders, Your Data screens + data export service</name>
  <files>
    app/settings.tsx,
    app/prayer-reminders.tsx,
    app/your-data.tsx,
    src/components/settings/SettingsList.tsx,
    src/components/settings/PrayerReminderRow.tsx,
    src/services/data-export.ts,
    __tests__/services/data-export.test.ts
  </files>
  <behavior>
    - collectAllUserData() returns object with habits, completions, streaks, xp_ledger, titles, quests, muhasabah, settings keys
    - exportUserData() writes JSON to cache dir and calls Sharing.shareAsync
    - deleteAllUserData() drops all user data from DB, resets all stores, sets onboardingComplete=false
  </behavior>
  <action>
    **Settings Screen (app/settings.tsx):**
    - ScrollView with grouped sections. Each section has a heading in Press Start 2P (smaller size).
    - **Prayer section:** Calculation method picker (pressable row showing current method, opens a modal/ActionSheet with CALC_METHODS list). Location row showing current locationName or "Not set" (tap to re-request location permission and update).
    - **Notifications section:**
      - "Prayer Reminders" row with chevron — navigates to prayer-reminders screen
      - "Muhasabah Reminder" toggle + time picker (shows current time, tap to change)
      - "Streak Milestones" toggle (default OFF)
      - "Quest Expiring" toggle (default OFF)
      - "Morning Motivation" toggle (default OFF)
    - **Appearance section:**
      - "Sound Effects" toggle (settingsStore.soundEnabled)
      - "Haptic Feedback" toggle (settingsStore.hapticEnabled)
      - "Arabic Terminology" toggle (settingsStore.arabicTermsEnabled). Subtext: "Show Islamic terms with English context"
    - **About section:**
      - Version row (read from Constants.expoConfig.version or hardcode "1.0.0")
      - Credits row (static text)
    - Each toggle row: label on left, Switch on right. Use React Native Switch component.
    - When any notification setting changes, call NotificationService.rescheduleAll() with current settingsStore values (only if location is available and permissions granted).

    **Prayer Reminders Screen (app/prayer-reminders.tsx):**
    - Heading: "Prayer Reminders"
    - For each of 5 prayers (Fajr, Dhuhr, Asr, Maghrib, Isha):
      - PrayerReminderRow component: prayer name, reminder toggle, lead time picker (5/10/15/30 min dropdown), gentle follow-up toggle
    - Each change calls settingsStore.setPrayerReminder(prayerName, updates) and triggers rescheduleAll.
    - Back navigation returns to settings.

    **Your Data Screen (app/your-data.tsx):**
    - Heading: "Your Data"
    - Info text explaining what data is stored locally
    - "What's stored" section: list of data categories (Habits, Completions, Streaks, XP, Titles, Quests, Muhasabah, Settings) — informational only
    - "Export Data" button: calls exportUserData(), shows loading state during export
    - "Delete Everything" button: RED destructive styling. Shows confirmation Alert before proceeding. On confirm: calls deleteAllUserData() which drops all data, resets stores (gameStore, habitStore, settingsStore including onboardingComplete=false), then router.replace('/(onboarding)/welcome') to restart onboarding.

    **Data Export Service (src/services/data-export.ts):**
    - collectAllUserData(): reads all tables via repos (habitRepo, completionRepo, streakRepo, xpRepo, questRepo, muhasabahRepo, settingsStore state). Returns structured object with all data.
    - exportUserData(): calls collectAllUserData(), JSON.stringify with 2-space indent, writes to FileSystem.cacheDirectory + 'halalhabits-export.json', calls Sharing.shareAsync with mimeType 'application/json'.
    - deleteAllUserData(): drops all rows from all user tables (using db.delete or raw SQL), then resets gameStore, habitStore, settingsStore to initial state. Sets settingsStore.onboardingComplete = false.

    **Test (data-export.test.ts):**
    - Mock expo-file-system and expo-sharing
    - Test collectAllUserData returns expected structure with all keys
    - Test exportUserData calls writeAsStringAsync and shareAsync
    - Test deleteAllUserData clears stores (mock store resets)
  </action>
  <verify>
    <automated>npx jest __tests__/services/data-export.test.ts -x</automated>
  </verify>
  <done>
    Settings screen shows all 4 grouped sections with working toggles. Prayer reminders sub-screen shows per-prayer controls. Your Data screen can export JSON and delete all data. Data export test passes. Notification reschedule triggers on settings changes.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify profile and settings</name>
  <files>app/(tabs)/profile.tsx</files>
  <action>Human verifies Profile, Settings, Prayer Reminders, and Your Data screens on device. Built: RPG character sheet profile, grouped settings with per-prayer notification controls, data export and delete.</action>
  <verify>
    <automated>echo "Manual verification checkpoint"</automated>
  </verify>
  <done>
    1. Profile tab: character sprite, title, level, XP bar, stats grid, trophy case, streak bars, niyyah chips visible
    2. Gear icon opens Settings with Prayer, Notifications, Appearance, About sections
    3. Toggles for Sound, Haptics, Arabic Terminology persist on re-enter
    4. "Prayer Reminders" opens per-prayer controls (toggle, lead time, follow-up)
    5. "Your Data" shows data categories, Export produces JSON via share sheet
    6. "Delete Everything" shows confirmation, clears data, restarts onboarding
  </done>
</task>

</tasks>

<verification>
- Profile tab shows full RPG character sheet reading from gameStore + habitStore
- Settings controls all notification preferences, prayer method, appearance toggles
- Data export produces valid JSON with all user data categories
- Delete all data resets app to onboarding state
- npx jest __tests__/services/data-export.test.ts passes
</verification>

<success_criteria>
- Profile replaces PlaceholderScreen with RPG character sheet
- Settings has 4 grouped sections with all required controls
- Per-prayer notification configuration works
- Arabic terminology toggle persists
- Export data produces shareable JSON
- Delete data resets to onboarding
- Dark mode is default (v1 dark-only -- PROF-04 satisfied)
</success_criteria>

<output>
After completion, create `.planning/phases/06-onboarding-profile-and-notifications/06-03-SUMMARY.md`
</output>
