---
phase: 16-shared-activities-duo-quests
plan: 04
type: execute
wave: 4
depends_on: [16-03]
files_modified:
  - app/(tabs)/buddies.tsx
  - src/components/activities/SharedHabitCard.tsx
  - src/components/activities/SharedHabitProposalCard.tsx
  - src/components/activities/ProposeSharedHabitSheet.tsx
  - src/components/activities/ActivitiesTab.tsx
  - app/buddy-profile/[id].tsx
autonomous: false
requirements: [DUOQ-01, DUOQ-05]
must_haves:
  truths:
    - "Activities sub-tab is visible alongside Buddies sub-tab in buddy screen"
    - "Player can propose a shared habit from buddy profile screen"
    - "Buddy receives proposal and can accept or decline"
    - "Active shared habits show shared streak count only (not individual data)"
    - "Either player can end a shared habit with confirmation"
  artifacts:
    - path: "src/components/activities/ActivitiesTab.tsx"
      provides: "Activities sub-tab content with shared habits and duo quests"
    - path: "src/components/activities/SharedHabitCard.tsx"
      provides: "Card showing shared habit name, streak, buddy name, end button"
    - path: "src/components/activities/ProposeSharedHabitSheet.tsx"
      provides: "Modal for proposing a shared habit to a buddy"
    - path: "src/components/activities/SharedHabitProposalCard.tsx"
      provides: "Card for incoming proposals with accept/decline"
  key_links:
    - from: "app/(tabs)/buddies.tsx"
      to: "src/components/activities/ActivitiesTab.tsx"
      via: "sub-tab navigation"
      pattern: "ActivitiesTab"
    - from: "app/buddy-profile/[id].tsx"
      to: "src/components/activities/ProposeSharedHabitSheet.tsx"
      via: "Propose Shared Habit button opens sheet"
      pattern: "ProposeSharedHabitSheet"
    - from: "src/components/activities/SharedHabitCard.tsx"
      to: "src/stores/sharedHabitStore.ts"
      via: "useSharedHabitStore for end action"
      pattern: "useSharedHabitStore"
---

<objective>
Build the Activities sub-tab and shared habit UI components. This adds the two-tab navigation (Buddies | Activities) to the buddy screen, shared habit cards with streak display, the proposal flow (propose from buddy profile, accept/decline incoming), and end-habit functionality.

Purpose: Deliver the user-facing shared habit feature (DUOQ-01) with privacy-safe progress display (DUOQ-05).
Output: Activities sub-tab + shared habit components + buddy profile integration
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-shared-activities-duo-quests/16-CONTEXT.md
@.planning/phases/16-shared-activities-duo-quests/16-03-SUMMARY.md

<interfaces>
From src/stores/sharedHabitStore.ts (Plan 03):
```typescript
interface SharedHabitState {
  activeSharedHabits: SharedHabit[];
  proposals: SharedHabit[];
  loading: boolean;
  loadSharedHabits(buddyPairIds: string[], userId: string): Promise<void>;
  proposeSharedHabit(params): Promise<void>;
  acceptProposal(id: string, userId: string, pairIds: string[]): Promise<void>;
  declineProposal(id: string, userId: string, pairIds: string[]): Promise<void>;
  endSharedHabit(id: string, userId: string, pairIds: string[]): Promise<void>;
  getSharedStreak(id: string, userADates: string[], userBDates: string[]): number;
}
```

From src/stores/buddyStore.ts:
```typescript
interface BuddyState {
  accepted: Buddy[];
  pendingIncoming: Buddy[];
  pendingBadgeCount: number;
  loadBuddies(userId: string): Promise<void>;
}
```

From app/(tabs)/buddies.tsx (existing structure):
- BuddiesScreen component with search bar, pending requests, buddy list
- Uses useBuddyStore, useAuthStore
- Has modals: InviteCodeSheet, EnterCodeSheet, DiscoverabilitySheet

From app/buddy-profile/[id].tsx:
- Buddy profile with stat cards, avatar, three-dot menu
- Uses useBuddyStore.getBuddyProfile
- Entry point for "Propose Shared Habit" button per D-01

Design tokens:
```typescript
import { colors, palette } from '../../src/tokens/colors';
import { typography } from '../../src/tokens/typography';
import { spacing, componentSpacing } from '../../src/tokens/spacing';
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Activities Sub-Tab + Shared Habit Components</name>
  <files>src/components/activities/ActivitiesTab.tsx, src/components/activities/SharedHabitCard.tsx, src/components/activities/SharedHabitProposalCard.tsx, src/components/activities/ProposeSharedHabitSheet.tsx</files>
  <action>
    Create src/components/activities/ directory with 4 components:

    1. SharedHabitCard.tsx:
       - Props: { sharedHabit: SharedHabit, buddyName: string, sharedStreak: number, onEnd: () => void }
       - Display: habit name (bold), "with [buddyName]", "Shared streak: [N] days" per D-14
       - End button (small, secondary style) with confirmation via Alert.alert before calling onEnd
       - Per D-04: either player can end unilaterally
       - Style: card with subtle border, emerald accent for active streak, warm copy
       - Per project memory: no flex:1 in Modal children

    2. SharedHabitProposalCard.tsx:
       - Props: { proposal: SharedHabit, fromName: string, onAccept: () => void, onDecline: () => void }
       - Display: "[fromName] wants to share: [habit name]" with accept/decline buttons
       - Accept = emerald button, Decline = subtle text button
       - Style: card with gold-700 left border (incoming proposal indicator)

    3. ProposeSharedHabitSheet.tsx:
       - Props: { visible: boolean, onClose: () => void, buddyPairId: string, buddyName: string }
       - Modal sheet with:
         * Title: "Propose Shared Habit"
         * Subtitle: "with [buddyName]"
         * Habit type picker: FlatList of eligible types (custom + character habits). Use isEligibleForSharing from shared-habit-engine to filter. Show options like: "Custom Habit", "Focus", "Kindness", "Patience", "Gratitude", "Exercise", "Reading"
         * Name input (TextInput): e.g. "Read 10 pages daily"
         * Frequency selector: "Daily" (default) -- just show as text for now, daily is default per schema
         * "Propose" button (disabled until name filled)
       - On propose: call useSharedHabitStore.getState().proposeSharedHabit({ buddyPairId, userId, habitType, name })
       - Close sheet on success
       - Per project memory: do NOT add flex:1 to Modal children

    4. ActivitiesTab.tsx:
       - Props: { userId: string, buddyPairIds: string[], buddyProfiles: Map<string, { name: string }> }
       - Loads shared habits + duo quests on mount via useSharedHabitStore.loadSharedHabits and useDuoQuestStore.loadAllDuoQuests
       - Sections:
         * "Proposals" section (if proposals.length > 0): list of SharedHabitProposalCard
         * "Shared Habits" section: list of SharedHabitCard with shared streak
         * "Duo Quests" section: placeholder text "Duo quest cards coming soon" (actual cards in Plan 05)
       - Empty state: "No shared activities yet. Visit a buddy's profile to start one."
       - Pull-to-refresh
       - Per D-12: this is the content for the Activities sub-tab
  </action>
  <verify>
    <automated>npx tsc --noEmit src/components/activities/ActivitiesTab.tsx src/components/activities/SharedHabitCard.tsx src/components/activities/SharedHabitProposalCard.tsx src/components/activities/ProposeSharedHabitSheet.tsx</automated>
  </verify>
  <done>All 4 components compile. SharedHabitCard shows streak-only progress per D-14. ProposeSharedHabitSheet filters worship habits per D-02. ActivitiesTab renders proposals + shared habits sections.</done>
</task>

<task type="auto">
  <name>Task 2: Buddy Screen Sub-Tabs + Profile Integration</name>
  <files>app/(tabs)/buddies.tsx, app/buddy-profile/[id].tsx</files>
  <action>
    Modify app/(tabs)/buddies.tsx:
    1. Add sub-tab state: const [activeTab, setActiveTab] = useState<'buddies' | 'activities'>('buddies')
    2. Add sub-tab bar below search bar area:
       - Two touchable labels: "Buddies" and "Activities"
       - Active tab has emerald underline + bold text, inactive has muted text
       - Style: horizontal row, centered text, 48px tap target height
    3. Conditionally render:
       - activeTab === 'buddies': existing buddy list content (search, pending, FlatList)
       - activeTab === 'activities': <ActivitiesTab userId={userId} buddyPairIds={accepted.map(b => b.id)} buddyProfiles={profileCache} />
    4. Pass buddy profile map (name lookups) to ActivitiesTab. Use existing profileCache state or build a simple map from accepted buddies.
    5. Per D-12: two sub-tabs "Buddies" and "Activities"

    Modify app/buddy-profile/[id].tsx:
    1. Add "Propose Shared Habit" button below the stat grid:
       - Emerald outlined button with text "Propose Shared Habit"
       - Opens ProposeSharedHabitSheet modal
       - Per D-01: entry point is buddy profile screen
    2. Add state: const [showProposeSheet, setShowProposeSheet] = useState(false)
    3. Import and render ProposeSharedHabitSheet with buddyPairId (the buddy row ID from route params) and buddyName
    4. Position button between stat grid and the existing three-dot menu area. Use consistent spacing.
    5. Also add a "Start Duo Quest" button placeholder (disabled, styled as secondary) -- actual functionality wired in Plan 05
  </action>
  <verify>
    <automated>npx tsc --noEmit app/(tabs)/buddies.tsx app/buddy-profile/[id].tsx</automated>
  </verify>
  <done>Buddy screen has Buddies | Activities sub-tabs per D-12. Activities tab shows shared habits and proposals. Buddy profile has "Propose Shared Habit" button per D-01. Both screens compile without errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Activities sub-tab navigation and shared habit UI flow: sub-tab switching on buddy screen, shared habit cards with streak display, proposal flow from buddy profile.</what-built>
  <how-to-verify>
    1. Open the app and navigate to the Buddies tab
    2. Verify two sub-tabs appear: "Buddies" and "Activities"
    3. Tap "Activities" -- should show empty state or shared habits if any exist
    4. Tap "Buddies" -- should show the existing buddy list
    5. Tap a buddy to open their profile
    6. Verify "Propose Shared Habit" button is visible below stat grid
    7. Tap it -- ProposeSharedHabitSheet should open
    8. Verify salah and muhasabah are NOT in the habit type picker
    9. Fill in a habit name and tap Propose
    10. Switch to Activities tab -- the shared habit should appear (if testing with same device, check proposal card)
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- Sub-tabs render: Activities tab shows shared habits and proposals
- Privacy: worship habits excluded from proposal picker
- Streak display shows "Shared streak: N days" not individual progress per D-14
- Buddy profile has Propose button per D-01
- No flex:1 in Modal children (project memory constraint)
</verification>

<success_criteria>
- Buddy screen has functioning Buddies | Activities sub-tabs per D-12
- SharedHabitCard shows shared streak only per D-14
- ProposeSharedHabitSheet excludes salah/muhasabah per D-02
- Buddy profile has Propose Shared Habit button per D-01
- Human verification approved
</success_criteria>

<output>
After completion, create `.planning/phases/16-shared-activities-duo-quests/16-04-SUMMARY.md`
</output>
