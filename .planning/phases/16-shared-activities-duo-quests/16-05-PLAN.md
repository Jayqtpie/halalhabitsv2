---
phase: 16-shared-activities-duo-quests
plan: 05
type: execute
wave: 5
depends_on: [16-03, 16-04]
files_modified:
  - src/components/activities/DuoQuestCard.tsx
  - src/components/activities/CreateDuoQuestSheet.tsx
  - src/components/activities/DuoQuestDetailSheet.tsx
  - src/components/activities/InactivityBanner.tsx
  - src/components/activities/ActivitiesTab.tsx
  - app/buddy-profile/[id].tsx
autonomous: false
requirements: [DUOQ-02, DUOQ-03, DUOQ-04, DUOQ-05]
must_haves:
  truths:
    - "Player can create a duo quest from template catalog or custom creation"
    - "Duo quest card shows combined progress bar with aggregate percentage only"
    - "Custom quest text is filtered via leo-profanity before creation"
    - "48h inactivity shows yellow warning banner on quest card"
    - "72h inactivity shows exit option with partial XP calculation"
    - "Completing own part awards individual XP; both completing awards bonus"
    - "Max 3 active duo quests per buddy pair enforced in UI"
  artifacts:
    - path: "src/components/activities/DuoQuestCard.tsx"
      provides: "Duo quest card with combined progress bar and aggregate %"
    - path: "src/components/activities/CreateDuoQuestSheet.tsx"
      provides: "Modal for creating duo quest from template or custom"
    - path: "src/components/activities/DuoQuestDetailSheet.tsx"
      provides: "Full quest detail view with progress and inactivity handling"
    - path: "src/components/activities/InactivityBanner.tsx"
      provides: "Yellow warning banner for paused quests and exit option"
  key_links:
    - from: "src/components/activities/DuoQuestCard.tsx"
      to: "src/domain/duo-quest-engine.ts"
      via: "getAggregateProgress for display"
      pattern: "getAggregateProgress"
    - from: "src/components/activities/CreateDuoQuestSheet.tsx"
      to: "src/stores/duoQuestStore.ts"
      via: "createFromTemplate and createCustom"
      pattern: "useDuoQuestStore"
    - from: "src/components/activities/CreateDuoQuestSheet.tsx"
      to: "leo-profanity"
      via: "client-side text filter"
      pattern: "leo-profanity"
    - from: "src/components/activities/InactivityBanner.tsx"
      to: "src/domain/duo-quest-engine.ts"
      via: "checkInactivity for threshold detection"
      pattern: "checkInactivity"
    - from: "app/buddy-profile/[id].tsx"
      to: "src/components/activities/CreateDuoQuestSheet.tsx"
      via: "Start Duo Quest button"
      pattern: "CreateDuoQuestSheet"
---

<objective>
Build the duo quest UI components: quest cards with privacy-safe aggregate progress, template/custom quest creation with profanity filtering, inactivity warning/exit banners, and buddy profile integration. Complete the Activities tab by replacing the duo quest placeholder.

Purpose: Deliver the user-facing duo quest feature (DUOQ-02 through DUOQ-05) with privacy-safe progress and inactivity handling.
Output: DuoQuestCard + CreateDuoQuestSheet + DuoQuestDetailSheet + InactivityBanner + full integration
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-shared-activities-duo-quests/16-CONTEXT.md
@.planning/phases/16-shared-activities-duo-quests/16-03-SUMMARY.md
@.planning/phases/16-shared-activities-duo-quests/16-04-SUMMARY.md

<interfaces>
From src/stores/duoQuestStore.ts (Plan 03):
```typescript
interface DuoQuestState {
  activeQuests: DuoQuest[];
  completedQuests: DuoQuest[];
  loading: boolean;
  loadDuoQuests(buddyPairId: string): Promise<void>;
  loadAllDuoQuests(buddyPairIds: string[]): Promise<void>;
  createFromTemplate(params): Promise<'success' | 'max_reached'>;
  createCustom(params): Promise<'success' | 'max_reached'>;
  recordMyProgress(questId, side, userId): Promise<void>;
  checkInactivityStatus(questId): Promise<'ok' | 'warning' | 'exit_eligible'>;
  exitQuest(questId, side, userId): Promise<void>;
}
```

From src/domain/duo-quest-engine.ts (Plan 01):
```typescript
getAggregateProgress(quest): { totalProgress: number, totalTarget: number, percentage: number }
checkInactivity(partnerLastProgressAt, now?): 'ok' | 'warning' | 'exit_eligible'
calculatePartialXP(params): { individualXP: number, bonusXP: number }
```

From src/domain/duo-quest-templates.ts (Plan 01):
```typescript
export interface DuoQuestTemplate {
  id: string;
  title: string;
  description: string;
  targetType: string;
  targetValue: number;
  durationDays: number;
  xpRewardEach: number;
  xpRewardBonus: number;
}
export const DUO_QUEST_TEMPLATES: DuoQuestTemplate[];
```

From src/types/database.ts:
```typescript
export type DuoQuest = {
  id, buddyPairId, createdByUserId, title, description,
  xpRewardEach, xpRewardBonus, targetType, targetValue,
  userAProgress, userBProgress, userACompleted, userBCompleted,
  status, expiresAt, completedAt, createdAt, updatedAt
}
```

Design tokens: colors, palette, typography, spacing, componentSpacing from src/tokens/
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Duo Quest Components</name>
  <files>src/components/activities/DuoQuestCard.tsx, src/components/activities/DuoQuestDetailSheet.tsx, src/components/activities/InactivityBanner.tsx, src/components/activities/CreateDuoQuestSheet.tsx</files>
  <action>
    Install leo-profanity first: `npx expo install leo-profanity` (or npm install leo-profanity). This is the one new npm package per STATE.md decisions.

    Create 4 components in src/components/activities/:

    1. InactivityBanner.tsx:
       - Props: { status: 'ok' | 'warning' | 'exit_eligible', buddyName: string, onExit?: () => void, partialXP?: number }
       - If status === 'ok': render nothing (return null)
       - If status === 'warning': yellow banner with text "Paused -- waiting for [buddyName]" per D-09. Background: palette['gold-700'] at 15% opacity, gold-700 text.
       - If status === 'exit_eligible': yellow banner + "Exit with [partialXP] XP" button. Per D-10: exit with proportional partial credit. Button calls onExit.
       - Style: rounded corners, 12px padding, warm non-shame copy

    2. DuoQuestCard.tsx:
       - Props: { quest: DuoQuest, buddyName: string, onPress: () => void, inactivityStatus: 'ok' | 'warning' | 'exit_eligible' }
       - Call getAggregateProgress(quest) from duo-quest-engine to get { percentage, totalProgress, totalTarget }
       - Display:
         * Quest title (bold)
         * "with [buddyName]"
         * Combined progress bar (single bar, emerald fill, per D-13)
         * "Team progress: [percentage]%" -- aggregate only, per DUOQ-05
         * Time remaining (calculated from expiresAt - now)
         * XP reward: "[xpRewardEach] XP + [xpRewardBonus] bonus"
         * Warm copy at bottom: "Working together!" or "Almost there!" when > 75%
       - If inactivityStatus !== 'ok': render InactivityBanner below the card
       - Per D-13: single combined bar, aggregate %, NEVER individual progress
       - onPress opens DuoQuestDetailSheet

    3. DuoQuestDetailSheet.tsx:
       - Props: { visible: boolean, onClose: () => void, quest: DuoQuest, buddyName: string, side: 'a' | 'b', userId: string }
       - Modal sheet showing full quest detail:
         * Title, description
         * Large combined progress bar with percentage
         * "Your progress" section: button to "Mark Progress" (calls duoQuestStore.recordMyProgress)
         * Time remaining countdown
         * XP breakdown: "Individual: [xpRewardEach] XP | Bonus: [xpRewardBonus] XP (when both complete)"
         * InactivityBanner if partner inactive
         * If exit_eligible: exit button with partial XP shown via calculatePartialXP
       - Per DUOQ-05: progress display is aggregate only. The "Mark Progress" button advances YOUR progress without showing partner's individual progress.
       - Per project memory: no flex:1 in Modal children

    4. CreateDuoQuestSheet.tsx:
       - Props: { visible: boolean, onClose: () => void, buddyPairId: string, buddyName: string, userId: string }
       - Two tabs at top: "Templates" | "Custom"
       - Templates tab:
         * FlatList of DUO_QUEST_TEMPLATES
         * Each template card: title, description, duration, XP reward
         * Tap to select, then "Start Quest" button
         * Calls duoQuestStore.createFromTemplate
         * If result === 'max_reached': show Alert "Maximum 3 active quests with this buddy" per D-08
       - Custom tab:
         * Title input (TextInput, max 50 chars)
         * Description input (TextInput, multiline, max 200 chars)
         * Target value input (numeric, "How many times?")
         * Duration picker: 3, 5, 7, or 14 days
         * XP reward: auto-calculated as Math.min(150, Math.max(50, targetValue * 15)) for individual
         * On submit: filter title + description through leo-profanity. Import { filter } from 'leo-profanity'. If filter(text) !== text, show Alert "Please use appropriate language". Per D-06: client-side filter.
         * Calls duoQuestStore.createCustom
       - Per D-05: template-based AND custom creation
       - Per D-07: started from buddy profile screen
       - Per project memory: no flex:1 in Modal children
  </action>
  <verify>
    <automated>npx tsc --noEmit src/components/activities/DuoQuestCard.tsx src/components/activities/CreateDuoQuestSheet.tsx src/components/activities/DuoQuestDetailSheet.tsx src/components/activities/InactivityBanner.tsx</automated>
  </verify>
  <done>All 4 duo quest components compile. DuoQuestCard shows aggregate progress only per DUOQ-05/D-13. InactivityBanner shows warning at 48h and exit at 72h per D-09/D-10. CreateDuoQuestSheet supports templates and custom with leo-profanity filter per D-05/D-06. Max 3 quests enforced per D-08.</done>
</task>

<task type="auto">
  <name>Task 2: Activities Tab Completion + Profile Integration</name>
  <files>src/components/activities/ActivitiesTab.tsx, app/buddy-profile/[id].tsx</files>
  <action>
    Update src/components/activities/ActivitiesTab.tsx:
    1. Replace the "Duo quest cards coming soon" placeholder with actual DuoQuestCard rendering
    2. Import DuoQuestCard and DuoQuestDetailSheet
    3. In the "Duo Quests" section: map over useDuoQuestStore.activeQuests, render DuoQuestCard for each
    4. Add state for selected quest detail: const [selectedQuest, setSelectedQuest] = useState<DuoQuest | null>(null)
    5. Render DuoQuestDetailSheet when selectedQuest is set
    6. For each quest card, compute inactivity status. Either:
       a. Call checkInactivity from domain engine directly (simpler, no async needed if we pass timestamps)
       b. Or use a useEffect to batch-check inactivity for all active quests on load
       Choose option (a) for simplicity -- checkInactivity is a pure function on timestamps.
    7. Determine 'side' for current user: if quest.createdByUserId === userId then 'a' else 'b' (matches schema convention where creator is typically userA via the buddy pair)
    8. Add "Completed Quests" collapsed section showing completedQuests with green checkmark icon

    Update app/buddy-profile/[id].tsx:
    1. Replace the disabled "Start Duo Quest" placeholder button with an active button
    2. Add state: const [showCreateQuest, setShowCreateQuest] = useState(false)
    3. Import and render CreateDuoQuestSheet with buddyPairId and buddyName
    4. Wire "Start Duo Quest" button to open the sheet
    5. Per D-07: duo quests started from buddy profile screen
    6. Style both buttons ("Propose Shared Habit" and "Start Duo Quest") as a horizontal row of outlined buttons with consistent spacing
  </action>
  <verify>
    <automated>npx tsc --noEmit src/components/activities/ActivitiesTab.tsx app/buddy-profile/[id].tsx</automated>
  </verify>
  <done>Activities tab shows both shared habits and duo quest cards. Buddy profile has working "Start Duo Quest" button per D-07. Duo quest cards show aggregate progress per DUOQ-05. Inactivity banners render correctly.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete duo quest UI: quest cards with aggregate progress bars, template/custom creation with profanity filter, inactivity banners, exit with partial XP, and full Activities tab with both shared habits and duo quests.</what-built>
  <how-to-verify>
    1. Open a buddy's profile screen
    2. Tap "Start Duo Quest" -- CreateDuoQuestSheet should open
    3. Browse template catalog -- verify quest titles, XP values, durations
    4. Switch to "Custom" tab -- fill in a quest with test title/description
    5. Try entering profanity in the title -- should be caught by filter per D-06
    6. Create a valid quest -- should succeed and appear in Activities tab
    7. Navigate to Activities tab -- verify duo quest card shows:
       - Combined progress bar (not two separate bars)
       - "Team progress: X%" (aggregate only, per DUOQ-05)
       - Quest title, buddy name, time remaining, XP reward
    8. Tap a quest card -- detail sheet should open with "Mark Progress" button
    9. Tap "Mark Progress" -- verify XP is awarded
    10. Verify no individual partner progress is visible anywhere (DUOQ-05 privacy)
    11. If possible, simulate 48h inactivity -- verify yellow warning banner per D-09
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- Aggregate-only progress: `grep -r "userAProgress\|userBProgress" src/components/activities/DuoQuestCard.tsx` returns nothing (should use getAggregateProgress only)
- Leo-profanity used: `grep "leo-profanity" src/components/activities/CreateDuoQuestSheet.tsx` shows import
- Inactivity thresholds: InactivityBanner renders for warning and exit_eligible states
- Max quests enforced: CreateDuoQuestSheet handles 'max_reached' result
- No flex:1 in Modal children
</verification>

<success_criteria>
- DuoQuestCard shows aggregate-only progress per DUOQ-05 and D-13
- CreateDuoQuestSheet supports templates (D-05) and custom with profanity filter (D-06)
- InactivityBanner shows at 48h (D-09) and exit at 72h (D-10)
- Max 3 active quests enforced per D-08
- Buddy profile has working Start Duo Quest button per D-07
- Activities tab shows complete list of shared habits and duo quests
- Human verification approved
</success_criteria>

<output>
After completion, create `.planning/phases/16-shared-activities-duo-quests/16-05-SUMMARY.md`
</output>
