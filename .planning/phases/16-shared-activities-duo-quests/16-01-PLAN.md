---
phase: 16-shared-activities-duo-quests
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/domain/duo-quest-engine.ts
  - src/domain/shared-habit-engine.ts
  - src/domain/duo-quest-templates.ts
  - __tests__/domain/duo-quest-engine.test.ts
  - __tests__/domain/shared-habit-engine.test.ts
autonomous: true
requirements: [DUOQ-01, DUOQ-02, DUOQ-03, DUOQ-04, DUOQ-05]
must_haves:
  truths:
    - "Shared habit eligibility rejects salah and muhasabah types per Privacy Gate"
    - "Shared habit proposal creates correct proposal data for accept/decline flow"
    - "Shared streak calculates days both players completed the habit"
    - "Duo quest creation enforces max 3 active quests per buddy pair"
    - "Duo quest progress returns aggregate percentage without revealing individual completions"
    - "Inactivity check detects 48h warning and 72h exit thresholds"
    - "Partial XP calculation awards proportional individual XP with no bonus"
    - "Duo quest status transitions follow valid state machine"
  artifacts:
    - path: "src/domain/shared-habit-engine.ts"
      provides: "Pure TS shared habit domain logic"
      exports: ["isEligibleForSharing", "createSharedHabitProposal", "calculateSharedStreak", "canEndSharedHabit", "WORSHIP_HABIT_TYPES"]
    - path: "src/domain/duo-quest-engine.ts"
      provides: "Pure TS duo quest domain logic"
      exports: ["canCreateDuoQuest", "createDuoQuest", "recordProgress", "getAggregateProgress", "checkInactivity", "calculatePartialXP", "getDuoQuestStatusTransition", "isQuestComplete", "MAX_ACTIVE_DUO_QUESTS", "INACTIVITY_WARNING_MS", "INACTIVITY_EXIT_MS"]
    - path: "src/domain/duo-quest-templates.ts"
      provides: "Curated duo quest template catalog"
      exports: ["DUO_QUEST_TEMPLATES"]
    - path: "__tests__/domain/duo-quest-engine.test.ts"
      provides: "Unit tests for duo quest domain functions"
      min_lines: 120
    - path: "__tests__/domain/shared-habit-engine.test.ts"
      provides: "Unit tests for shared habit domain functions"
      min_lines: 60
  key_links:
    - from: "src/domain/duo-quest-engine.ts"
      to: "src/domain/duo-quest-templates.ts"
      via: "DuoQuestTemplate type import"
      pattern: "DuoQuestTemplate"
    - from: "src/domain/shared-habit-engine.ts"
      to: "Privacy Gate concept"
      via: "WORSHIP_HABIT_TYPES constant"
      pattern: "salah|muhasabah"
---

<objective>
Create the pure TypeScript domain engines for shared habits and duo quests with TDD. These modules contain all business logic with zero React or database imports, following the established pattern of buddy-engine.ts, quest-engine.ts, and detox-engine.ts.

Purpose: Establish testable domain logic before any IO layer. All downstream plans (repos, stores, UI) consume these functions.
Output: shared-habit-engine.ts + duo-quest-engine.ts + duo-quest-templates.ts + full test suites
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-shared-activities-duo-quests/16-CONTEXT.md

<interfaces>
<!-- Existing domain engine patterns to follow -->
From src/domain/buddy-engine.ts (pattern reference):
- Pure TS functions, no React imports, no database imports
- Exported constants for magic numbers
- Each function takes simple args, returns simple values

From src/domain/quest-engine.ts:
```typescript
export interface QuestTemplate {
  id: string;
  type: 'daily' | 'weekly' | 'stretch';
  description: string;
  targetType: string;
  targetValue: number;
  targetHabitType?: string;
  minLevel: number;
  xpReward: number;
}
```

From src/db/schema.ts (duo_quests table columns):
```typescript
// duo_quests: id, buddyPairId, createdByUserId, title, description,
//   xpRewardEach, xpRewardBonus, targetType, targetValue,
//   userAProgress, userBProgress, userACompleted, userBCompleted,
//   status, expiresAt, completedAt, createdAt, updatedAt

// shared_habits: id, buddyPairId, createdByUserId, habitType, name,
//   targetFrequency, status, createdAt, updatedAt
```

From src/types/database.ts:
```typescript
export type SharedHabit = InferSelectModel<typeof sharedHabits>;
export type NewSharedHabit = InferInsertModel<typeof sharedHabits>;
export type DuoQuest = InferSelectModel<typeof duoQuests>;
export type NewDuoQuest = InferInsertModel<typeof duoQuests>;
```
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Shared Habit Engine -- TDD Red-Green-Refactor</name>
  <files>src/domain/shared-habit-engine.ts, __tests__/domain/shared-habit-engine.test.ts</files>
  <behavior>
    - isEligibleForSharing('salah') returns false (per D-02, Privacy Gate)
    - isEligibleForSharing('muhasabah') returns false (per D-02, Privacy Gate)
    - isEligibleForSharing('custom') returns true
    - isEligibleForSharing('focus') returns true (character habit)
    - isEligibleForSharing('kindness') returns true (character habit)
    - createSharedHabitProposal({ buddyPairId, createdByUserId, habitType: 'custom', name: 'Read 10 pages' }) returns a valid NewSharedHabit-shaped object with status='proposed', targetFrequency='daily'
    - createSharedHabitProposal throws/returns error if habitType is 'salah'
    - calculateSharedStreak(userACompletionDates: string[], userBCompletionDates: string[]) returns count of dates present in both arrays (consecutive from most recent)
    - calculateSharedStreak with no overlap returns 0
    - calculateSharedStreak(['2026-03-25','2026-03-24'], ['2026-03-25','2026-03-24','2026-03-23']) returns 2 (only overlap)
    - canEndSharedHabit('active') returns true (per D-04, either player can end)
    - canEndSharedHabit('ended') returns false
    - canEndSharedHabit('proposed') returns true (can decline a proposal)
  </behavior>
  <action>
    RED: Write test file __tests__/domain/shared-habit-engine.test.ts with all behavior cases above. Run tests -- all must fail.

    GREEN: Create src/domain/shared-habit-engine.ts:
    - Export const WORSHIP_HABIT_TYPES = ['salah', 'muhasabah'] as const (per D-02)
    - isEligibleForSharing(habitType: string): boolean -- returns false if habitType is in WORSHIP_HABIT_TYPES
    - createSharedHabitProposal(params: { buddyPairId, createdByUserId, habitType, name, targetFrequency? }): returns proposal object with status='proposed', timestamps filled. Throws if habitType not eligible.
    - calculateSharedStreak(userADates: string[], userBDates: string[]): number -- find consecutive shared completion dates counting backward from most recent. Uses Set intersection, then sorts descending and counts consecutive run from day 0.
    - canEndSharedHabit(status: string): boolean -- returns true for 'active' or 'proposed', false for 'ended'

    Run tests -- all must pass. Commit.

    REFACTOR: Clean up if needed. Run tests again. Commit.

    Per D-14: shared streak is "how many consecutive days BOTH players completed" -- this is a pure computation on two date arrays.
  </action>
  <verify>
    <automated>npx jest __tests__/domain/shared-habit-engine.test.ts --no-cache</automated>
  </verify>
  <done>All shared habit engine tests pass. Functions enforce Privacy Gate (salah/muhasabah excluded), calculate shared streaks correctly, and handle proposal lifecycle.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Duo Quest Engine + Templates -- TDD Red-Green-Refactor</name>
  <files>src/domain/duo-quest-engine.ts, src/domain/duo-quest-templates.ts, __tests__/domain/duo-quest-engine.test.ts</files>
  <behavior>
    - canCreateDuoQuest(activeQuestCount=2) returns true (per D-08, max 3)
    - canCreateDuoQuest(activeQuestCount=3) returns false
    - canCreateDuoQuest(activeQuestCount=0) returns true
    - createDuoQuest({ buddyPairId, createdByUserId, title, description, xpRewardEach: 100, xpRewardBonus: 50, targetType: 'completion_count', targetValue: 5, durationDays: 7 }) returns valid NewDuoQuest-shaped object with status='active', expiresAt set correctly, progress=0/0, completed=false/false
    - recordProgress(quest, side='a', increment=1) returns updated quest with userAProgress incremented, clamped to targetValue
    - recordProgress(quest, side='b', increment=1) returns updated quest with userBProgress incremented
    - recordProgress marks userACompleted=true when userAProgress reaches targetValue
    - getAggregateProgress({ userAProgress: 3, userBProgress: 2, targetValue: 5 }) returns { totalProgress: 5, totalTarget: 10, percentage: 50 } (per D-13, D-05 -- combined aggregate)
    - getAggregateProgress never reveals which side contributed what (returns only aggregate)
    - checkInactivity({ updatedAt, partnerLastProgressAt }, now) returns 'ok' if within 48h
    - checkInactivity returns 'warning' if partner inactive 48-72h (per D-09)
    - checkInactivity returns 'exit_eligible' if partner inactive 72h+ (per D-10)
    - calculatePartialXP({ xpRewardEach: 100, userProgress: 3, targetValue: 5 }) returns 60 (60% of individual, per D-10)
    - calculatePartialXP returns 0 bonus XP (no bonus on partial exit)
    - getDuoQuestStatusTransition('active', 'complete') returns 'completed'
    - getDuoQuestStatusTransition('active', 'pause') returns 'paused'
    - getDuoQuestStatusTransition('paused', 'resume') returns 'active'
    - getDuoQuestStatusTransition('active', 'exit') returns 'exited'
    - getDuoQuestStatusTransition('completed', 'complete') returns null (invalid)
    - isQuestComplete({ userACompleted: true, userBCompleted: true }) returns true
    - isQuestComplete({ userACompleted: true, userBCompleted: false }) returns false
    - DUO_QUEST_TEMPLATES has at least 6 templates with xpRewardEach between 50-150
  </behavior>
  <action>
    RED: Write test file __tests__/domain/duo-quest-engine.test.ts with all behavior cases above. Run tests -- all must fail.

    GREEN: Create src/domain/duo-quest-engine.ts:
    - Export constants: MAX_ACTIVE_DUO_QUESTS = 3, INACTIVITY_WARNING_MS = 48 * 60 * 60 * 1000, INACTIVITY_EXIT_MS = 72 * 60 * 60 * 1000
    - canCreateDuoQuest(activeCount: number): boolean
    - createDuoQuest(params: { buddyPairId, createdByUserId, title, description, xpRewardEach, xpRewardBonus?, targetType, targetValue, durationDays }): returns NewDuoQuest-shaped object with expiresAt = now + durationDays, all progress at 0
    - recordProgress(quest: { userAProgress, userBProgress, userACompleted, userBCompleted, targetValue }, side: 'a' | 'b', increment?: number): returns updated quest fields with progress incremented and completed flag set if progress >= targetValue
    - getAggregateProgress(quest: { userAProgress, userBProgress, targetValue }): { totalProgress, totalTarget, percentage } -- per D-13/DUOQ-05, aggregate only, never individual
    - checkInactivity(partnerLastProgressAt: string, now?: Date): 'ok' | 'warning' | 'exit_eligible' -- per D-09/D-10
    - calculatePartialXP(params: { xpRewardEach, userProgress, targetValue }): { individualXP, bonusXP } -- proportional individual, 0 bonus (per D-10)
    - getDuoQuestStatusTransition(current: string, action: string): string | null -- state machine for active/paused/completed/exited
    - isQuestComplete(quest: { userACompleted, userBCompleted }): boolean

    Create src/domain/duo-quest-templates.ts:
    - Export DUO_QUEST_TEMPLATES array following QuestTemplate-like pattern
    - Include 8-10 curated duo quest templates:
      * "Read Together" (both read for 7 days, targetType: 'completion_count', targetValue: 7, xpRewardEach: 75, xpRewardBonus: 40)
      * "Hydration Challenge" (drink 8 glasses daily for 5 days, 60 each, 30 bonus)
      * "Morning Routine" (complete morning habits for 5 days, 80 each, 45 bonus)
      * "Gratitude Week" (log gratitude for 7 days, 70 each, 35 bonus)
      * "Exercise Buddies" (exercise 5 times in a week, 90 each, 50 bonus)
      * "Screen-Free Evenings" (no screens after 9pm for 5 days, 100 each, 55 bonus)
      * "Early Bird Challenge" (wake before Fajr for 3 days, 120 each, 60 bonus)
      * "Dhikr Together" (complete dhikr for 7 days, 75 each, 40 bonus)
    - All templates have xpRewardEach within 50-150 range per XP economy model
    - Export DuoQuestTemplate interface extending base template with durationDays field

    Run tests -- all must pass. Commit.

    REFACTOR: Clean up. Run tests. Commit.
  </action>
  <verify>
    <automated>npx jest __tests__/domain/duo-quest-engine.test.ts --no-cache</automated>
  </verify>
  <done>All duo quest engine tests pass. Max 3 active quests enforced, aggregate progress never reveals individual data, inactivity thresholds at 48h/72h, partial XP is proportional with no bonus, status transitions are valid, and template catalog has 8+ entries in XP range.</done>
</task>

</tasks>

<verification>
- All domain functions are pure TS with no React/DB imports: `grep -r "from 'react'" src/domain/shared-habit-engine.ts src/domain/duo-quest-engine.ts` returns nothing
- Privacy Gate respected: isEligibleForSharing('salah') === false
- Aggregate progress never exposes individual: getAggregateProgress return type has no userA/userB fields
- XP values in templates all between 50-150 for individual rewards
</verification>

<success_criteria>
- shared-habit-engine.ts exports all listed functions with correct signatures
- duo-quest-engine.ts exports all listed functions and constants
- duo-quest-templates.ts has 8+ templates within XP economy bounds
- All tests pass with zero failures
- No React, database, or side-effect imports in any domain file
</success_criteria>

<output>
After completion, create `.planning/phases/16-shared-activities-duo-quests/16-01-SUMMARY.md`
</output>
