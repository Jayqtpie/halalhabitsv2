---
phase: 13-dopamine-detox-dungeon
plan: 04
type: execute
wave: 3
depends_on: ["13-02"]
files_modified:
  - src/components/hud/HudScene.tsx
  - src/components/hud/DungeonDoorIcon.tsx
  - src/components/hud/WelcomeBackToast.tsx
  - src/components/detox/DungeonClearedFanfare.tsx
autonomous: false
requirements: [DTOX-01, DTOX-02, DTOX-03, DTOX-04, DTOX-06]

must_haves:
  truths:
    - "HUD shows a dungeon door icon that opens the DungeonSheet on tap"
    - "During active session the HUD transforms to dungeon theme with stone tint and torch flicker"
    - "When app returns to foreground during active session, a welcome-back toast shows for 3 seconds"
    - "When session completes, a full-screen 'Dungeon Cleared!' fanfare plays with XP count-up animation"
    - "Dungeon door shows countdown timer badge during active session"
  artifacts:
    - path: "src/components/hud/HudScene.tsx"
      provides: "Modified HUD with dungeonActive prop, dungeon theme layers, DungeonDoorIcon integration"
      contains: "dungeonActive"
    - path: "src/components/hud/DungeonDoorIcon.tsx"
      provides: "Pixel art dungeon door with glow ring and countdown badge"
      exports: ["DungeonDoorIcon"]
    - path: "src/components/hud/WelcomeBackToast.tsx"
      provides: "Toast displayed on app foreground during active session"
      exports: ["WelcomeBackToast"]
    - path: "src/components/detox/DungeonClearedFanfare.tsx"
      provides: "Full-screen celebration overlay with XP count-up"
      exports: ["DungeonClearedFanfare"]
  key_links:
    - from: "src/components/hud/HudScene.tsx"
      to: "src/stores/detoxStore.ts"
      via: "useDetoxStore for activeSession state"
      pattern: "useDetoxStore"
    - from: "src/components/hud/DungeonDoorIcon.tsx"
      to: "src/components/detox/DungeonSheet.tsx"
      via: "Opens DungeonSheet on tap"
      pattern: "DungeonSheet"
    - from: "src/components/hud/WelcomeBackToast.tsx"
      to: "AppState"
      via: "AppState listener for foreground detection"
      pattern: "AppState.addEventListener"
    - from: "src/components/detox/DungeonClearedFanfare.tsx"
      to: "src/stores/detoxStore.ts"
      via: "Triggered on session completion"
      pattern: "useDetoxStore"
---

<objective>
Build the HUD dungeon theme (Skia canvas layers), DungeonDoorIcon (entry point), WelcomeBackToast (foreground notification), and DungeonClearedFanfare (completion celebration).

Purpose: These components create the immersive dungeon experience on the HUD and provide the visual feedback loop for session lifecycle events. The HUD transformation is the most dramatic visual feature of Phase 13.
Output: Modified HudScene with dungeon theme, three new components for HUD and celebration UI.
</objective>

<execution_context>
@C:\Users\jay_p\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\jay_p\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-dopamine-detox-dungeon/13-CONTEXT.md
@.planning/phases/13-dopamine-detox-dungeon/13-UI-SPEC.md
@.planning/phases/13-dopamine-detox-dungeon/13-02-SUMMARY.md

<interfaces>
<!-- From HudScene.tsx (existing) -->
```typescript
// Canvas with Skia layers: background, DayNightOverlay, character sprite
// React Native View siblings rendered outside Canvas (e.g., FridayMessageBanner)
// Pattern established in Phase 12: RN Views are siblings to Canvas, not inside it
```

<!-- From detoxStore (Plan 02) -->
```typescript
interface DetoxState {
  activeSession: DetoxSession | null;
  completeSession: (userId: string) => Promise<number>; // returns XP earned
  loadActiveSession: (userId: string) => Promise<void>;
}
```

<!-- From detox-engine (Plan 01) -->
```typescript
export function getRemainingMs(startedAt: string, durationHours: number): number;
export function calculateDetoxXP(variant: 'daily' | 'deep', durationHours: number): number;
```

<!-- Dungeon theme colors from UI-SPEC -->
```
Dungeon stone surface: #1A1410 (warm near-black, 30% opacity layer)
Torch amber: #C8621A (flicker animation fill)
Stone border: #3D2E1E (card borders during dungeon mode)
Dungeon shadow: rgba(0,0,0,0.65) (DayNightOverlay tint replacement)
```

<!-- Animation tokens from UI-SPEC -->
```
Torch flicker: 400ms easeInOut sine, opacity 0.6-1.0, offset per torch
Dungeon door glow: 2000ms easeInOut, opacity 0.4-1.0 loop
Fanfare ZoomIn: spring damping=15 stiffness=150 (350ms)
XP count-up: 800ms linear (celebration duration)
WelcomeBackToast: 300ms fadeIn, 3s display, 300ms fadeOut
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build DungeonDoorIcon, WelcomeBackToast, and modify HudScene for dungeon theme</name>
  <read_first>
    - src/components/hud/HudScene.tsx (current implementation — Canvas layers, View siblings, DayNightOverlay)
    - src/components/hud/HudStatBar.tsx (HUD layout reference)
    - src/components/hud/FridayBoostBadge.tsx (existing HUD badge pattern if exists)
    - src/stores/detoxStore.ts (activeSession state from Plan 02)
    - src/tokens/colors.ts (all color tokens)
    - src/tokens/motion.ts (animation duration tokens)
    - .planning/phases/13-dopamine-detox-dungeon/13-UI-SPEC.md (Component 1, 3, 4, 5 specs)
  </read_first>
  <files>src/components/hud/DungeonDoorIcon.tsx, src/components/hud/WelcomeBackToast.tsx, src/components/hud/HudScene.tsx</files>
  <action>
    **DungeonDoorIcon.tsx (per D-01, D-04, D-06):**
    - Rendered as RN View sibling outside Canvas (same pattern as FridayMessageBanner per Phase 12 decision)
    - Visual: placeholder pixel art dungeon gate icon — use a Unicode character or simple View-based pixel representation (actual pixel art PNG is a deferred asset task)
    - Size: 32x32 visual, 44x44 hit slop via `hitSlop={{ top: 6, bottom: 6, left: 6, right: 6 }}`
    - Glow ring states:
      - No session, tappable: static emerald glow ring at 60% opacity — use a View with `borderColor: '#0D7C3D'`, `borderWidth: 2`, `borderRadius: 8`, `opacity: 0.6`
      - Active session: glow ring pulses opacity 0.4-1.0 over 2000ms using Reanimated `withRepeat(withTiming(1.0, {duration: 2000}), -1, true)` on a `useSharedValue`
    - Countdown badge (during active session): small pill below icon showing `DetoxCountdownTimer` variant='hud' (import from Plan 03's component)
    - `onPress` prop: opens DungeonSheet (caller provides this callback)
    - `accessibilityLabel="Dopamine Detox Dungeon — tap to enter"`
    - Reduced motion: if `AccessibilityInfo.isReduceMotionEnabled()` is true, skip glow pulse animation (static opacity 0.8)

    **WelcomeBackToast.tsx (per D-07):**
    - RN View sibling outside Canvas (same pattern as FridayMessageBanner)
    - Uses `AppState.addEventListener('change', ...)` to detect foreground
    - When app returns to foreground AND `detoxStore.activeSession` is active:
      - Show toast for 3 seconds then fade out over 300ms
      - Copy: "Still in the dungeon — [X]h [Y]m remaining" at `bodyMd` (15px Inter-Regular)
      - Format remaining time from `getRemainingMs(session.startedAt, session.durationHours)` — convert ms to hours and minutes
      - Background: `surface` (#1E293B) with `border` (#334155) border, `borderRadius: 12` (radius.lg)
      - Padding: `spacing.md` (16px) horizontal, `spacing.sm` (8px) vertical
    - Fade animation: Reanimated `withTiming` opacity 0->1 (300ms fadeIn), then after 3s delay `withTiming` opacity 1->0 (300ms fadeOut)
    - Position: absolute, top ~80px from safe area top, centered horizontally, `zIndex: 10`
    - Reduced motion: skip fade animation, use instant show/hide

    **HudScene.tsx modification (per D-05):**
    - Import `useDetoxStore` and read `activeSession`
    - Derive `dungeonActive = activeSession !== null && activeSession.status === 'active'`
    - Inside the Canvas, when `dungeonActive === true`, add two Skia layers ABOVE background, BELOW character:
      1. Stone wall tint: `<Rect x={0} y={0} width={canvasWidth} height={canvasHeight} color="rgba(26,20,16,0.30)" />` (dungeon stone surface #1A1410 at 30% opacity)
      2. Two torch elements: `<Circle cx={canvasWidth * 0.15} cy={canvasHeight * 0.4} r={6} color="#C8621A" />` and `<Circle cx={canvasWidth * 0.85} cy={canvasHeight * 0.4} r={6} color="#C8621A" />`
      - Torch flicker: use two Reanimated `useSharedValue` for opacity, animated with `withRepeat(withTiming(1.0, {duration: 400}), -1, true)` starting from 0.6. Second torch offset by 200ms (start at different phase)
      - Reduced motion: both torches at static opacity 0.8 (no flicker)
    - Replace DayNightOverlay tint color when `dungeonActive`: use `rgba(0,0,0,0.65)` instead of the normal DayNightOverlay value
    - Add `DungeonDoorIcon` as a View sibling of the Canvas (positioned in top-right area of HUD, or alongside existing HUD elements)
    - Add `WelcomeBackToast` as a View sibling
    - State for DungeonSheet visibility: `const [dungeonSheetVisible, setDungeonSheetVisible] = useState(false)` — pass to DungeonDoorIcon onPress and DungeonSheet visible/onClose
    - Import and render `DungeonSheet` (from Plan 03) with visible/onClose props

    Canvas architecture reminder: RN Views (DungeonDoorIcon, WelcomeBackToast, DungeonSheet) are siblings OUTSIDE the Canvas element. Only Skia drawing primitives (Rect, Circle) go inside Canvas.
  </action>
  <verify>
    <automated>npx tsc --noEmit src/components/hud/HudScene.tsx src/components/hud/DungeonDoorIcon.tsx src/components/hud/WelcomeBackToast.tsx 2>&1 || echo "Type check completed"</automated>
  </verify>
  <acceptance_criteria>
    - src/components/hud/DungeonDoorIcon.tsx exists
    - DungeonDoorIcon.tsx contains `accessibilityLabel` with "Dopamine Detox Dungeon"
    - DungeonDoorIcon.tsx contains `hitSlop`
    - DungeonDoorIcon.tsx contains `useSharedValue` or `withRepeat` for glow animation
    - DungeonDoorIcon.tsx contains `AccessibilityInfo` for reduced motion check
    - src/components/hud/WelcomeBackToast.tsx exists
    - WelcomeBackToast.tsx contains `AppState.addEventListener`
    - WelcomeBackToast.tsx contains "Still in the dungeon" string
    - WelcomeBackToast.tsx contains `getRemainingMs` import
    - WelcomeBackToast.tsx contains `withTiming` for fade animation
    - HudScene.tsx contains `useDetoxStore` import
    - HudScene.tsx contains `dungeonActive` variable or equivalent boolean
    - HudScene.tsx contains `#1A1410` or `rgba(26,20,16` for stone tint
    - HudScene.tsx contains `#C8621A` for torch color
    - HudScene.tsx contains `DungeonDoorIcon` import
    - HudScene.tsx contains `WelcomeBackToast` import
    - HudScene.tsx contains `DungeonSheet` import
  </acceptance_criteria>
  <done>HUD transforms to dungeon theme during active session with stone tint and flickering torches. DungeonDoorIcon provides entry point with glow animation and countdown badge. WelcomeBackToast shows on app foreground during active session.</done>
</task>

<task type="auto">
  <name>Task 2: Build DungeonClearedFanfare completion celebration</name>
  <read_first>
    - src/components/hud/HudScene.tsx (to understand where fanfare renders relative to HUD)
    - src/components/hud/LevelUpOverlay.tsx (if exists — pattern reference for spring animation and haptics)
    - src/tokens/colors.ts (xp gold #FFD700, textSecondary)
    - src/tokens/typography.ts (headingXl 28px, bodyMd 15px)
    - src/tokens/motion.ts (celebration 800ms, spring params)
    - .planning/phases/13-dopamine-detox-dungeon/13-UI-SPEC.md (Component 6 — DungeonClearedFanfare spec)
  </read_first>
  <files>src/components/detox/DungeonClearedFanfare.tsx</files>
  <action>
    **DungeonClearedFanfare.tsx (per D-12):**
    - Full-screen overlay, absolute positioned, `zIndex: 50` (above all per UI-SPEC z-index scale)
    - Props: `{ visible: boolean; xpEarned: number; onDismiss: () => void }`
    - Backdrop: `rgba(15,23,42,0.92)` (semi-transparent dark, matches LevelUpOverlay pattern)

    **Animated entry (per UI-SPEC Motion Contract):**
    - ZoomIn spring: `useSharedValue(0.85)` -> `withSpring(1, { damping: 15, stiffness: 150 })` on mount
    - `useAnimatedStyle(() => ({ transform: [{ scale: scale.value }], opacity: scale.value }))`
    - Reduced motion: skip spring, set scale immediately to 1

    **Content layout:**
    - "Dungeon Cleared!" at `headingXl` (28px Inter-Bold), color `#FFD700` (xp gold)
    - XP count-up number: `headingXl` (28px Inter-Bold), color `#FFD700`
      - Count-up animation: 0 to `xpEarned` over 800ms (`duration.celebration`)
      - Use `useSharedValue(0)` with `withTiming(xpEarned, {duration: 800})` then `useDerivedValue` to get integer
      - Display: `+${Math.round(currentValue)} XP`
      - Reduced motion: show final value immediately, no count-up
    - Mentor praise line: "Your discipline cut through the noise. [X] XP earned." at `bodyMd` (15px Inter-Regular), color `textSecondary` (#94A3B8)
    - "Continue Journey" button: full-width, accent fill (`primary` #0D7C3D), min 44px height, calls `onDismiss`

    **Haptic burst (per UI-SPEC, matches LevelUpOverlay):**
    - On mount: 3x `Haptics.impactAsync(Haptics.ImpactFeedbackStyle.Heavy)` at 0ms, 100ms, 180ms
    - Use `setTimeout` for the 100ms and 180ms delays

    **Integration with HudScene:**
    - In HudScene.tsx (already modified in Task 1), add state: `const [showFanfare, setShowFanfare] = useState(false)` and `const [fanfareXP, setFanfareXP] = useState(0)`
    - When DetoxCountdownTimer's `onComplete` fires (session timer reaches 0):
      1. Call `detoxStore.completeSession(userId)` to get XP earned
      2. Set `fanfareXP = xpEarned`, `showFanfare = true`
    - Render `<DungeonClearedFanfare visible={showFanfare} xpEarned={fanfareXP} onDismiss={() => setShowFanfare(false)} />`
    - On fanfare dismiss: HudScene dungeon theme fades out over 500ms (`duration.slow`)

    Never auto-dismiss: fanfare stays until player taps "Continue Journey"
  </action>
  <verify>
    <automated>npx tsc --noEmit src/components/detox/DungeonClearedFanfare.tsx 2>&1 || echo "Type check completed"</automated>
  </verify>
  <acceptance_criteria>
    - src/components/detox/DungeonClearedFanfare.tsx exists and is >60 lines
    - DungeonClearedFanfare.tsx contains "Dungeon Cleared!" string
    - DungeonClearedFanfare.tsx contains "Continue Journey" string
    - DungeonClearedFanfare.tsx contains "Your discipline cut through the noise" string
    - DungeonClearedFanfare.tsx contains `#FFD700` or `xp` color reference for gold
    - DungeonClearedFanfare.tsx contains `withSpring` with `damping: 15` and `stiffness: 150`
    - DungeonClearedFanfare.tsx contains `Haptics.impactAsync`
    - DungeonClearedFanfare.tsx contains `withTiming` for XP count-up
    - DungeonClearedFanfare.tsx contains `AccessibilityInfo` for reduced motion
    - DungeonClearedFanfare.tsx contains `zIndex: 50` or equivalent
    - HudScene.tsx contains `DungeonClearedFanfare` import
    - HudScene.tsx contains `showFanfare` or equivalent state variable
  </acceptance_criteria>
  <done>DungeonClearedFanfare plays full celebration with spring animation, XP count-up, haptic burst, and mentor praise. Integrated into HudScene and triggered on session completion.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of complete Dopamine Detox Dungeon experience</name>
  <action>
    User visually verifies the complete Dopamine Detox Dungeon feature across all plans (01-04): HUD dungeon door icon with glow animation, dungeon theme transformation (stone tint, flickering torches), DungeonSheet with variant toggle and duration chips, countdown timer, early exit with penalty confirmation, habit card shield badges, welcome-back toast on foreground, and Dungeon Cleared fanfare with XP count-up and haptics.
  </action>
  <what-built>
    Complete Dopamine Detox Dungeon feature: HUD dungeon door icon with glow animation, dungeon theme transformation (stone tint, flickering torches), DungeonSheet with variant toggle and duration chips, countdown timer, early exit with penalty confirmation, habit card shield badges, welcome-back toast on foreground, and Dungeon Cleared fanfare with XP count-up and haptics.
  </what-built>
  <how-to-verify>
    1. Launch the app on a development build
    2. On the HUD, tap the dungeon door icon — the DungeonSheet should open
    3. Select "Daily" variant, pick a short duration (2H), tap "Enter the Dungeon"
    4. Verify: HUD transforms to dungeon theme (darker stone tint, torch flicker)
    5. Verify: countdown timer appears near dungeon door icon on HUD
    6. Background the app, then return — verify welcome-back toast appears for ~3 seconds
    7. Open dungeon sheet — verify large timer display and "Exit Early" button with penalty preview
    8. Tap "Exit Early" — verify confirmation dialog appears with compassionate copy
    9. Tap "Keep Going" to dismiss dialog
    10. Check habit cards — verify "PROTECTED" shield badge visible on at least one habit
    11. Wait for timer to complete (or start a very short session for testing) — verify Dungeon Cleared fanfare plays with XP count-up and haptic feedback
    12. Tap "Continue Journey" — verify HUD returns to normal theme
    13. Test "Deep" variant toggle — verify 6H/8H chips shown, "Once per week" badge visible
  </how-to-verify>
  <verify>User confirms all 13 verification steps pass</verify>
  <done>User has visually approved the complete Dopamine Detox Dungeon experience</done>
  <resume-signal>Type "approved" or describe any issues with the dungeon experience</resume-signal>
</task>

</tasks>

<verification>
- HudScene renders dungeon door icon and opens DungeonSheet on tap
- During active session: stone tint and torch flicker visible on HUD canvas
- Countdown timer updates every second using wall-clock delta
- Welcome-back toast appears on app foreground during active session
- Fanfare plays on completion with gold XP count-up and haptic burst
- All reduced motion checks in place for accessibility
- `npx tsc --noEmit` passes for all modified/new files
</verification>

<success_criteria>
- HUD dungeon door icon is tappable with emerald glow ring
- HUD fully transforms to dungeon theme during active session
- Welcome-back toast shows remaining time on app foreground
- Dungeon Cleared fanfare feels like beating a game level
- All animations respect reduced motion setting
- User visually approves the complete dungeon experience
</success_criteria>

<output>
After completion, create `.planning/phases/13-dopamine-detox-dungeon/13-04-SUMMARY.md`
</output>
