---
phase: 15-buddy-connection-system
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/domain/buddy-engine.ts
  - __tests__/domain/buddy-engine.test.ts
autonomous: true
requirements: [BUDY-01, BUDY-02, BUDY-04]
must_haves:
  truths:
    - "generateInviteCode() produces codes in HH-XXXXX format using crypto randomness"
    - "isInviteCodeExpired() correctly identifies codes older than 48 hours"
    - "canSendRequest() blocks requests when 10 pending outbound exist"
    - "getStatusTransition() enforces valid buddy status state machine"
    - "isBlocked() detects block status regardless of which party blocked"
  artifacts:
    - path: "src/domain/buddy-engine.ts"
      provides: "Pure TS buddy domain logic"
      exports: ["generateInviteCode", "isInviteCodeExpired", "canSendRequest", "getStatusTransition", "isBlocked", "getBuddyId", "INVITE_CODE_EXPIRY_MS", "MAX_PENDING_OUTBOUND", "MAX_BUDDIES"]
    - path: "__tests__/domain/buddy-engine.test.ts"
      provides: "Unit tests for all buddy domain functions"
      min_lines: 80
  key_links:
    - from: "src/domain/buddy-engine.ts"
      to: "expo-crypto"
      via: "Crypto.getRandomValues import"
      pattern: "getRandomValues"
---

<objective>
Create the pure TypeScript buddy domain engine with TDD. This module contains all buddy connection logic (invite code generation, expiry checks, status transitions, rate limiting, block detection) with zero React or database imports. Downstream plans (buddyRepo, buddyStore) consume these functions.

Purpose: Establish testable domain logic before any IO layer, following the project pattern of boss-engine.ts and detox-engine.ts.
Output: src/domain/buddy-engine.ts + full test suite
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-buddy-connection-system/15-CONTEXT.md
@.planning/phases/15-buddy-connection-system/15-RESEARCH.md

<interfaces>
<!-- Existing domain engine pattern to follow -->
From src/domain/detox-engine.ts (pattern reference):
- Pure TS functions, no React imports, no database imports
- Exported constants for magic numbers
- Each function takes simple args, returns simple values

From src/types/database.ts:
```typescript
export type Buddy = InferSelectModel<typeof buddies>;
// Buddy has: id, userA, userB, status, inviteCode, createdAt, acceptedAt, updatedAt
```

Buddy status values (from schema default + CONTEXT.md D-04):
- 'pending' (default on creation)
- 'accepted' (after acceptance)
- 'blocked_by_a' (user_a blocked user_b)
- 'blocked_by_b' (user_b blocked user_a)
- 'removed' (soft delete on decline/remove)
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Buddy Domain Engine — TDD Red-Green-Refactor</name>
  <files>src/domain/buddy-engine.ts, __tests__/domain/buddy-engine.test.ts</files>
  <behavior>
    - generateInviteCode() returns string matching /^HH-[A-Z0-9]{4,5}$/
    - generateInviteCode() uses expo-crypto Crypto.getRandomValues (not Math.random)
    - INVITE_CODE_EXPIRY_MS equals 48 * 60 * 60 * 1000 (172800000)
    - isInviteCodeExpired('2020-01-01T00:00:00.000Z') returns true (old date)
    - isInviteCodeExpired(new Date().toISOString()) returns false (just created)
    - MAX_PENDING_OUTBOUND equals 10
    - canSendRequest(9) returns true
    - canSendRequest(10) returns false
    - canSendRequest(11) returns false
    - MAX_BUDDIES equals 20
    - canAddBuddy(19) returns true
    - canAddBuddy(20) returns false
    - getStatusTransition('pending', 'accept') returns 'accepted'
    - getStatusTransition('pending', 'decline') returns 'removed'
    - getStatusTransition('accepted', 'remove') returns 'removed'
    - getStatusTransition('accepted', 'block', 'a') returns 'blocked_by_a'
    - getStatusTransition('accepted', 'block', 'b') returns 'blocked_by_b'
    - getStatusTransition('removed', 'accept') returns null (invalid)
    - isBlocked('blocked_by_a') returns true
    - isBlocked('blocked_by_b') returns true
    - isBlocked('accepted') returns false
    - isBlocked('pending') returns false
    - getBuddyId('user-a-id', 'user-b-id', 'user-a-id') returns 'user-b-id'
    - getBuddyId('user-a-id', 'user-b-id', 'user-b-id') returns 'user-a-id'
    - getBlockerSide('user-a-id', 'user-b-id', 'user-a-id') returns 'a'
    - getBlockerSide('user-a-id', 'user-b-id', 'user-b-id') returns 'b'
  </behavior>
  <action>
    **RED phase:** Create __tests__/domain/buddy-engine.test.ts with all test cases from the behavior list above. Tests import from src/domain/buddy-engine.ts. Run tests — all must fail (module does not exist yet). Commit: `test(15-01): add failing tests for buddy domain engine`

    **GREEN phase:** Create src/domain/buddy-engine.ts with these exports:

    Constants:
    - INVITE_CODE_EXPIRY_MS = 48 * 60 * 60 * 1000
    - MAX_PENDING_OUTBOUND = 10
    - MAX_BUDDIES = 20
    - INVITE_CODE_CHARSET = 'ABCDEFGHJKLMNPQRSTUVWXYZ23456789' (no 0/O/1/I/L to avoid ambiguity)

    Functions:
    - generateInviteCode(): string — uses Crypto.getRandomValues(new Uint8Array(5)), maps each byte to CHARSET via modulo, returns `HH-${code}`
    - isInviteCodeExpired(createdAt: string): boolean — Date.now() - new Date(createdAt).getTime() > INVITE_CODE_EXPIRY_MS
    - canSendRequest(pendingOutboundCount: number): boolean — pendingOutboundCount < MAX_PENDING_OUTBOUND
    - canAddBuddy(currentBuddyCount: number): boolean — currentBuddyCount < MAX_BUDDIES
    - getStatusTransition(currentStatus: string, action: 'accept' | 'decline' | 'remove' | 'block', side?: 'a' | 'b'): string | null
    - isBlocked(status: string): boolean — status.startsWith('blocked_by_')
    - getBuddyId(userA: string, userB: string, currentUserId: string): string — returns the other user's ID
    - getBlockerSide(userA: string, userB: string, blockerId: string): 'a' | 'b'

    Run tests — all must pass. Commit: `feat(15-01): implement buddy domain engine`

    **REFACTOR phase (if needed):** Clean up, run tests (must still pass). Commit: `refactor(15-01): clean up buddy domain engine`
  </action>
  <verify>
    <automated>npm test -- --testPathPattern="buddy-engine" --no-coverage</automated>
  </verify>
  <done>
    All buddy-engine tests pass (15+ test cases). buddy-engine.ts has zero React imports (grep confirms no 'react' import). buddy-engine.ts has zero database imports (grep confirms no 'drizzle' or 'schema' import). generateInviteCode uses expo-crypto (grep confirms 'expo-crypto' import).
  </done>
</task>

</tasks>

<verification>
npm test -- --testPathPattern="buddy-engine" --no-coverage
</verification>

<success_criteria>
- All buddy-engine tests pass (expect 15+ test cases)
- buddy-engine.ts has zero React imports (grep confirms no 'react' import)
- buddy-engine.ts has zero database imports (grep confirms no 'drizzle' or 'schema' import)
- generateInviteCode uses expo-crypto (grep confirms 'expo-crypto' import)
</success_criteria>

<output>
After completion, create `.planning/phases/15-buddy-connection-system/15-01-SUMMARY.md`
</output>
