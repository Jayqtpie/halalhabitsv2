---
phase: 14
slug: nafs-boss-arena
status: draft
shadcn_initialized: false
preset: none
created: 2026-03-23
revised: 2026-03-23
revision_reason: Typography consolidated to 4 Inter sizes; aria-label added for header close icon
---

# Phase 14 — UI Design Contract: Nafs Boss Arena

> Visual and interaction contract for Phase 14 frontend implementation.
> Generated by gsd-ui-researcher. Verified by gsd-ui-checker.

---

## Design System

| Property | Value | Source |
|----------|-------|--------|
| Tool | none (React Native — shadcn not applicable) | RESEARCH.md |
| Preset | not applicable | detected |
| Component library | Custom token system (src/tokens/) | codebase scan |
| Icon library | Custom pixel-art SVG components (see PixelGearIcon.tsx, DungeonDoorIcon.tsx pattern) | codebase scan |
| Font — body | Inter-Regular / Inter-SemiBold / Inter-Bold (loaded via Expo) | src/tokens/typography.ts |
| Font — pixel/game | PressStart2P-Regular (loaded via Expo) | src/tokens/typography.ts |
| Rendering | Skia Canvas (FilterMode.Nearest for all pixel art) | RESEARCH.md, HudScene.tsx |

---

## Spacing Scale

Declared values (all multiples of 4 — from src/tokens/spacing.ts):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, inline chip gaps |
| sm | 8px | HUD card internal gaps, list item gaps |
| md | 16px | Default element spacing, card padding |
| lg | 24px | Screen outer padding, modal outer padding |
| xl | 32px | Layout section gaps |
| xxl | 48px | Major section breaks |

Component-level presets (from src/tokens/spacing.ts — componentSpacing):

| Preset | Value | Usage in this phase |
|--------|-------|---------------------|
| cardPadding | 16px | ArchetypeCard inner padding, BossStatusCard padding |
| modalPadding | 24px | ArenaScreen outer horizontal padding |
| modalElementGap | 16px | Between sections on Arena screen |
| hudCardPadding | 12px | HUD Arena Gate icon overlay |
| buttonPaddingVertical | 12px | Primary and secondary CTA buttons |
| buttonPaddingHorizontal | 24px | Primary and secondary CTA buttons |

Exceptions:
- Touch targets: minimum 44x44px for all interactive elements (ui-ux-pro-max touch-target-size rule). This includes archetype selection cards, CTA buttons, and the HUD Arena Gate icon.
- RPG dialogue text box: minimum height 80px to accommodate 2-3 lines of typewriter text without reflow.
- Boss HP bar: fixed height 16px, full width minus 32px (lg padding each side).

---

## Typography

### Inter Ramp (4 sizes — maximum)

All roles map to existing tokens in src/tokens/typography.ts. The Inter ramp is capped at 4 sizes. PressStart2P is declared separately as the pixel/game font ramp and does not count toward the Inter cap.

| Role | Token | Size | Weight | Line Height | Font | Usage in this phase |
|------|-------|------|--------|-------------|------|---------------------|
| Display | headingXl | 28px | 700 | 36px (1.29) | Inter-Bold | Arena screen title, boss defeat celebration heading, XP award figure in BossDefeatFanfare |
| Heading | headingMd | 20px | 600 | 28px (1.4) | Inter-SemiBold | Archetype name in gallery cards, section headers, BossEscapedNotice heading |
| Body | bodyMd | 16px | 400 | 24px (1.5) | Inter-Regular | Archetype lore text, cooldown notice, battle history, error messages, BossEscapedNotice body |
| Label | bodySm | 13px | 400 | 18px (1.38) | Inter-Regular | XP preview labels, day counter ("Day 3 of 7"), Mercy Mode indicator copy, HP percentage readout, cooldown remaining hours |

Notes on consolidation (vs previous draft):
- 15px body retired — replaced with standard 16px (bodyMd). Line height adjusted to 24px (1.5).
- 11px caption retired — merged into 13px label (bodySm). Where visual de-emphasis is needed (HP percentage readout, cooldown remaining hours), apply `opacity: 0.6` to the bodySm text rather than introducing a fifth size.
- 24px headingLg (used in BossDefeatFanfare XP award line) retired — mapped to 28px headingXl. The fanfare context (full-screen overlay, large gold text) suits the display size equally well.

### PressStart2P Ramp (pixel/game font — outside Inter cap)

PressStart2P is a distinct font family reserved exclusively for game-layer elements. Its sizes are declared separately and do not count toward the Inter 4-size cap. Justification: pixel fonts at small sizes are not interchangeable with Inter at equivalent sizes — the rasterisation grid of PressStart2P makes sub-14px Inter sizes inappropriate substitutes.

| Role | Token | Size | Weight | Line Height | Font | Usage in this phase |
|------|-------|------|--------|-------------|------|---------------------|
| Pixel — HUD | hudLabel | 10px | 700 | 14px | PressStart2P | HUD Arena Gate icon label (active battle indicator) |
| Pixel — Dialogue | hudXp | 12px | 700 | 16px | PressStart2P | Boss dialogue text box (RPG text box typewriter output) |

---

## Color

All values sourced from src/tokens/colors.ts (dark mode — primary theme for this app).

| Role | Value | Source token | Usage |
|------|-------|-------------|-------|
| Dominant (60%) | #0F172A | colors.dark.background / palette.surface-900 | Arena screen background, full-screen surface |
| Secondary (30%) | #1E293B | colors.dark.surface / palette.surface-800 | Archetype cards, dialogue text box background, battle status card |
| Accent — emerald (10%) | #0D7C3D | colors.dark.primary / palette.emerald-500 | Primary CTA ("Challenge Boss" button), active battle indicator ring, focus states |
| XP / Reward | #FFD700 | colors.dark.xp / palette.gold-500 | XP preview text, defeat XP award fanfare, boss HP bar (player damage portion) |
| Destructive | #9B1B30 | colors.dark.error / palette.ruby-500 | Boss HP bar fill (enemy HP), counter-attack heal flash overlay, "Abandon Battle" destructive action |
| Mercy | #FFB347 | colors.dark.mercy / palette.mercy | Mercy Mode active indicator, reduced counter-attack copy |

Accent (emerald #0D7C3D) reserved for:
- "Challenge Boss" primary CTA button background
- "Return to Arena" button when battle is active
- Active archetype selection border ring (2px solid)
- Archetype suggestion indicator ("Recommended" badge border)
- Focus rings on interactive elements
- Boss HP bar background track (drained/dealt portion)

Boss Arena HUD Theme palette (applied to HudScene when boss battle is active AND no detox session is active):

| Element | Value | Notes |
|---------|-------|-------|
| Arena stone tint | rgba(26, 20, 16, 0.35) | Slightly deeper than dungeon tint (#1A1410 @ 35%) |
| Arena shadow overlay | rgba(0, 0, 0, 0.70) | Replaces DayNightOverlay during boss theme — darker than dungeon |
| Boss silhouette tint | rgba(155, 27, 48, 0.15) | Subtle ruby wash over boss background layer (ruby-500 @ 15%) |
| Battle pulse ring | rgba(13, 124, 61, 0.25) | Emerald glow pulse around Arena Gate icon when battle is active |

Theme priority: detox dungeon theme takes visual priority when both are active (D-04). Boss theme only shows when dungeonActive is false and battleActive is true.

---

## Screens and Component Inventory

### 1. Arena Screen (full-screen route)

Route: `/(tabs)/arena` or modal stack from HUD icon — planner decides exact Expo Router path.

Layout (top to bottom):

1. **Screen header bar** (React Native View, height 56px)
   - Left: back/close icon (44x44px touch target, `aria-label="Close Arena"`)
   - Center: "Nafs Boss Arena" in headingMd (Inter-SemiBold, 20px)
   - Right: empty (no menu)

2. **Skia Canvas battle scene** (full-width, height = screen width × 0.65, max 280px)
   - Layer 1 (bottom): Arena background PNG (pixel art — placeholder until AI asset generated)
   - Layer 2: Boss silhouette PNG (archetype-specific pixel art — FilterMode.Nearest)
   - Layer 3: HP bar drawn in Skia (see HP Bar spec below)
   - Layer 4: Hit flash overlay (Reanimated shared value, ruby tint, withSequence)
   - Layer 5 (top): Boss sprite idle animation (4-frame withRepeat, 400ms per frame)

3. **RPG dialogue text box** (React Native View, sits below Canvas, no overlap)
   - Background: colors.dark.surface (#1E293B)
   - Border: 2px solid colors.dark.border (#1E293B lighter — use rgba(255,255,255,0.12))
   - Border radius: radius.md (8px)
   - Padding: spacing.md (16px)
   - Text: PressStart2P-Regular, 12px (hudXp token), colors.dark.textPrimary (#F1F5F9)
   - Typewriter animation: setInterval at 30ms per character (matches detox pattern — not Reanimated worklet to avoid bridge issue with Text children)
   - Minimum height: 80px to avoid layout shift during typewriter

4. **Battle status section** (React Native View, spacing.lg padding each side)
   - Day counter: "Day X of Y" — bodySm (13px), textSecondary
   - HP percentage readout: "Boss HP: 63%" — bodySm (13px) at opacity 0.6, textMuted
   - Mercy Mode indicator (renders only when mercyModeActive): "Mercy Mode active — counter-attacks halved" — bodySm, mercy color (#FFB347)

5. **Primary CTA button** (React Native Pressable, full-width minus lg padding)
   - Idle (no active battle): "Challenge Boss" — emerald background, white text, bodyMd weight 600, height 52px, radius.xl (16px)
   - Active battle: "View Battle Status" — same styling but secondary (surface background, emerald border 1.5px)
   - Disabled state: opacity 0.45, no press feedback
   - Loading state: button disabled with activity indicator replacing text

6. **Cooldown notice** (renders only when cooldown active, sits below CTA)
   - "Next challenge available in X days" — bodySm (13px) at opacity 0.6, textMuted
   - No icon — text only, centered

---

### 2. Archetype Gallery (within Arena Screen, idle state only)

Rendered as a vertical ScrollView below the dialogue box when no active battle.

**ArchetypeCard component** (per archetype, 6 total):

| Property | Spec |
|----------|------|
| Background | colors.dark.surface (#1E293B) |
| Border radius | radius.lg (12px) |
| Padding | cardPadding (16px) |
| Min touch target | 44px height (card is taller in practice) |
| Archetype name | headingMd (20px, Inter-SemiBold), textPrimary |
| Arabic title | bodySm (13px, Inter-Regular), textSecondary |
| Lore blurb | bodyMd (16px, Inter-Regular), textSecondary, max 2 lines with ellipsis |
| "Recommended" badge | bodySm, emerald-500 border 1px, emerald-900 background, emerald-400 text |
| Selected state | 2px solid border emerald-500, backgroundDeep fill |
| Unselected state | 1px solid border border-default (#1E293B) |
| Gap between cards | spacing.sm (8px) |

Archetype list order (D-12):
1. Al-Musawwif (The Procrastinator)
2. Al-Mulhi (The Distractor)
3. Al-Mushakkik (The Doubter)
4. Al-Sharah (The Glutton)
5. Al-Muqarin (The Comparer)
6. Al-Kamali (The Perfectionist)

---

### 3. BossHpBar (Skia drawn element inside Canvas)

| Property | Spec |
|----------|------|
| Track height | 16px |
| Track width | screen width − 64px (32px each side inside Canvas coordinate space) |
| Track background | colors.dark.xp (#FFD700) with opacity 0.25 (remaining boss HP "damage" zone) |
| Fill (boss remaining HP) | palette.ruby-600 (#DC2626) — fills right-to-left as HP decreases |
| Animated value | Reanimated withTiming(newHpRatio, { duration: 600, easing: Easing.out }) — shared value drives Skia rect width |
| Border radius | 4px (Skia RoundedRect) |
| Position | y = 16px from top of Canvas, centered |

---

### 4. HUD Arena Gate Icon (new HUD element, sibling to DungeonDoorIcon)

Parallel to DungeonDoorIcon.tsx pattern. Rendered as React Native View sibling outside Skia Canvas.

| Property | Spec |
|----------|------|
| Position | HUD overlay, top-right quadrant (opposite of DungeonDoorIcon placement) |
| Size | 44x44px touch target |
| Icon | Pixel-art arena gate / crossed swords SVG (PixelGearIcon pattern) |
| Idle state (no battle) | Icon at 70% opacity, no badge |
| Active battle indicator | 2px emerald ring pulse (Reanimated withRepeat withTiming opacity 0.4→1.0→0.4, duration 1600ms) + ruby-500 HP dot badge (12px circle, top-right of icon) |
| Press action | Opens Arena screen |
| Accessibility | aria-label="Boss Arena — battle active" (battle) / "Boss Arena" (idle) |

---

### 5. BossDefeatFanfare (celebration overlay)

Modeled on DungeonClearedFanfare.tsx pattern.

| Property | Spec |
|----------|------|
| Trigger | bossStore.pendingDefeatCelebration truthy |
| Overlay background | rgba(0, 0, 0, 0.85) full-screen |
| Heading | "Boss Defeated!" — headingXl (28px, Inter-Bold), gold-500 (#FFD700) |
| Subheading | Archetype name — headingMd (20px), textPrimary |
| XP award | "+X XP" — headingXl (28px, Inter-Bold), gold-500, count-up via state setInterval (not Reanimated — same worklet limitation) |
| Dismiss | Tap anywhere — Pressable wrapping entire overlay |
| Animation | Reanimated withTiming opacity 0→1 (duration 300ms) on mount |

Note: XP award uses headingXl (28px) — mapped from the previously undeclared 24px headingLg. The fanfare context (full-screen overlay, gold celebratory text) is visually appropriate at display scale.

---

### 6. BossEscapedNotice (partial XP, non-shaming state)

Shown when battle window expires without defeat. Not a modal — rendered inline at top of Arena screen.

| Property | Spec |
|----------|------|
| Background | surface (#1E293B), border 1px gold-500 (not ruby — this is positive framing) |
| Border radius | radius.lg (12px) |
| Padding | cardPadding (16px) |
| Heading | "The battle is over" — headingMd (20px), textPrimary |
| Body | "You dealt X% damage and earned Y XP for your effort." — bodyMd (16px), textSecondary |
| CTA | "Challenge Again" — secondary button style, emerald border |
| Cooldown sub-note | "Available in X days" — bodySm (13px) at opacity 0.6, textMuted |

---

## Copywriting Contract

| Element | Copy | Notes |
|---------|------|-------|
| Primary CTA (no battle) | "Challenge Boss" | Verb + noun, action-first |
| Primary CTA (battle active) | "View Battle" | Keeps player oriented |
| CTA disabled (level gate) | "Reach Level 10 to unlock" | Shown as helper text below disabled button, not on button itself |
| Archetype select CTA | "Begin Battle" | Shown after archetype is selected, replaces "Challenge Boss" |
| Empty state heading | "The Arena Awaits" | Shown on Arena screen before player has ever done a battle |
| Empty state body | "Reach Level 10 to challenge your nafs. Each archetype represents a struggle you can face and overcome." | Positive framing, no shame |
| Active battle notice | "Battle in Progress — Day X of Y" | Status, not judgment |
| Cooldown notice | "Next challenge available in X days" | Neutral, factual |
| Boss escape heading | "The battle is over" | No shame language — never "You failed" |
| Boss escape body | "You dealt X% damage and earned Y XP for your effort. The nafs retreats — for now." | Effort-affirming, positive |
| Defeat confirmation heading | "Boss Defeated!" | Exclamation appropriate here — celebration |
| Abandon battle label | "Abandon Battle" | Explicit destructive action name |
| Abandon confirmation | "If you abandon now, you'll earn partial XP for the damage dealt. Are you sure?" | Partial XP framing softens loss; no guilt |
| Abandon confirm button | "Yes, Abandon" | Clear, reversible-feeling language |
| Abandon cancel button | "Keep Fighting" | Positive alternative framing |
| Error state (generic) | "Something went wrong loading the Arena. Pull down to retry." | Problem + action path |
| Error state (battle save) | "Battle progress couldn't be saved. Your progress is safe locally — try again." | Reassure first, action second |
| Mercy Mode indicator | "Mercy Mode active — counter-attacks halved" | Factual, no condescension |
| Level gate subtext | "You're Level X — keep building discipline to unlock boss battles." | Encouraging, effort-based language |

Arabic terms usage (per adab safety rails — always with inline English context):
- Archetype names display as: "Al-Musawwif (The Procrastinator)"
- No standalone Arabic without English context in UI copy

---

## Motion Contract

All values reference src/tokens/motion.ts.

| Interaction | Duration | Easing | Implementation |
|-------------|----------|--------|----------------|
| HP bar decrease (damage) | 600ms | easeOut | Reanimated withTiming, shared value drives Skia rect width |
| HP bar increase (healing flash) | 300ms | easeOut | withTiming then withDelay 200ms back — brief flash |
| Hit flash overlay (damage) | 150ms in / 150ms out | easeInOut | withSequence withTiming opacity 0→0.45→0 (ruby tint) |
| Healing flash overlay | 150ms in / 150ms out | easeInOut | withSequence withTiming opacity 0→0.30→0 (gold tint) |
| Typewriter dialogue | 30ms per character | n/a | setInterval state-based (not Reanimated — worklet/Text limitation) |
| Arena Gate icon pulse | 800ms loop | easeInOut | withRepeat withTiming opacity 0.4→1.0, duration:800 |
| BossDefeatFanfare appear | 300ms | easeOut | withTiming opacity 0→1 |
| Screen transition (open Arena) | 300ms | easeOut | Expo Router default slide |
| Archetype card select | 150ms | easeInOut | withTiming borderColor transition |
| Reduced motion | — | — | Check AccessibilityInfo.isReduceMotionEnabled; disable withRepeat animations, use instant opacity changes |

---

## Accessibility Contract

Aligned with ui-ux-pro-max CRITICAL rules:

| Rule | Implementation |
|------|---------------|
| Touch target minimum | All Pressable elements 44x44px minimum — buttons, cards, HUD icon, dialogue tap |
| Color contrast | textPrimary (#F1F5F9) on surface (#1E293B): passes 4.5:1. Gold (#FFD700) on dark (#0F172A): passes. Ruby (#DC2626) on dark: passes |
| Color not sole indicator | HP bar uses both color (ruby fill) AND numerical percentage readout in bodySm below |
| Aria labels — header close | Arena screen header back/close icon: `aria-label="Close Arena"` (icon-only button) |
| Aria labels — HUD icon | HUD Arena Gate icon: `aria-label="Boss Arena — battle active"` (battle) / `aria-label="Boss Arena"` (idle) |
| Aria labels — general | All icon-only interactive elements must have aria-label |
| Reduce motion | AccessibilityInfo.isReduceMotionEnabled check — disable pulse animations, typewriter uses instant text reveal |
| Screen reader | Boss HP bar accessible value: `accessibilityValue={{ min: 0, max: 100, now: hpPercent }}` |
| Loading state | CTA disabled during async ops with ActivityIndicator replacing text |
| Destructive confirmation | Abandon Battle always requires two-tap confirmation — never single destructive action |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none — React Native project | not applicable |
| Third-party | none declared | not applicable |

No new npm packages introduced in Phase 14. All rendering uses already-installed @shopify/react-native-skia, react-native-reanimated, and Expo libraries.

---

## Interaction States — Summary Table

| Component | States |
|-----------|--------|
| ArchetypeCard | default, selected (emerald border), recommended-badge, press (opacity 0.7) |
| Challenge Boss CTA | idle, active-battle-variant, disabled (level gate), loading (disabled + spinner) |
| Begin Battle CTA | idle (archetype selected), loading |
| BossHpBar | animating (damage), animating (healing), static |
| HUD Arena Gate icon | idle, active-battle (pulse ring + HP dot) |
| Dialogue text box | typewriting, complete, idle (no active battle) |
| BossDefeatFanfare | hidden, entering (opacity 0→1), visible, dismissed |
| BossEscapedNotice | hidden, visible |
| Abandon confirmation | shown/hidden as sibling Modal outside ArenaScreen (same z-index pattern as EarlyExitConfirmation) |

---

## Z-Index Contract

Follows existing app z-index pattern:

| Layer | Z-index | Element |
|-------|---------|---------|
| Base | 0 | Skia Canvas (Arena screen) |
| Overlay | 10 | HUD Arena Gate icon (RN View sibling to Canvas) |
| Sheet | 20 | Abandon confirmation Modal |
| Celebration | 30 | BossDefeatFanfare overlay |
| System | 50+ | React Native system elements |

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
