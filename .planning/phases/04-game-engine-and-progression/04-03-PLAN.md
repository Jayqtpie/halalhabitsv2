---
phase: 04-game-engine-and-progression
plan: 03
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - src/components/game/XPFloatLabel.tsx
  - src/components/game/XPProgressBar.tsx
  - src/components/game/LevelBadge.tsx
  - src/components/game/LevelUpOverlay.tsx
  - src/components/game/TitleUnlockOverlay.tsx
  - src/components/game/CelebrationManager.tsx
  - app/(tabs)/habits.tsx
autonomous: false
requirements: [GAME-01, GAME-02, GAME-03]

must_haves:
  truths:
    - "User sees floating XP number rise from completed habit card"
    - "XP progress bar shows current level, XP progress, and XP total"
    - "Active title displays next to level badge"
    - "Level-up triggers full-screen celebration overlay"
    - "Title unlock triggers full-screen celebration overlay"
    - "XP float shows post-cap amount with multiplier breakdown"
  artifacts:
    - path: "src/components/game/XPFloatLabel.tsx"
      provides: "Floating XP animation component"
      min_lines: 30
    - path: "src/components/game/XPProgressBar.tsx"
      provides: "Persistent XP bar below daily progress"
      min_lines: 40
    - path: "src/components/game/LevelBadge.tsx"
      provides: "Level and active title display"
      min_lines: 20
    - path: "src/components/game/LevelUpOverlay.tsx"
      provides: "Full-screen level-up celebration"
      min_lines: 60
    - path: "src/components/game/TitleUnlockOverlay.tsx"
      provides: "Full-screen title unlock celebration"
      min_lines: 50
    - path: "src/components/game/CelebrationManager.tsx"
      provides: "Queue-based overlay manager consuming pendingCelebrations"
      min_lines: 30
  key_links:
    - from: "src/components/game/CelebrationManager.tsx"
      to: "src/stores/gameStore.ts"
      via: "consumeCelebration() to dequeue overlays"
      pattern: "consumeCelebration"
    - from: "src/components/game/XPProgressBar.tsx"
      to: "src/stores/gameStore.ts"
      via: "reads totalXP, currentLevel, xpToNext"
      pattern: "useGameStore"
    - from: "app/(tabs)/habits.tsx"
      to: "src/components/game/"
      via: "imports and renders XP UI components"
      pattern: "XPProgressBar|LevelBadge|CelebrationManager"
---

<objective>
Build the XP feedback UI layer: floating XP label on completion, persistent XP progress bar, level/title badge, and full-screen celebration overlays for level-up and title unlock. Integrate all into the habits screen.

Purpose: Make habit completions feel rewarding with immediate visual feedback and milestone celebrations -- the core game feel.
Output: 6 new components + updated habits screen with XP UI.
</objective>

<execution_context>
@C:/Users/jay_p/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jay_p/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-game-engine-and-progression/04-RESEARCH.md
@.planning/phases/04-game-engine-and-progression/04-02-SUMMARY.md

<interfaces>
<!-- From gameStore (after Plan 02) -->
```typescript
// src/stores/gameStore.ts
interface GameState {
  currentLevel: number;
  totalXP: number;
  xpToNext: number;
  activeTitle: Title | null;
  pendingCelebrations: Celebration[];
  consumeCelebration: () => Celebration | null;
  // ... other fields
}
type Celebration =
  | { type: 'level_up'; level: number; copy: string; unlockedTitles: string[] }
  | { type: 'title_unlock'; titleId: string; title: Title };
```

<!-- From xp-engine (after Plan 01) -->
```typescript
export interface XPResult {
  baseXP: number; multiplier: number; earnedXP: number; cappedXP: number;
  newTotalXP: number; newLevel: number; didLevelUp: boolean; previousLevel: number;
}
export function xpForLevel(level: number): number;
export function xpToNextLevel(level: number): number;
```

<!-- Habits screen structure -->
From app/(tabs)/habits.tsx:
- SafeAreaView container
- Header (Today + date + gear icon)
- DailyProgressBar
- HabitList (FlatList with HabitCard items)
- FAB button
- Modals: EditHabitSheet, HabitCalendar, CalcMethodPicker
- completeHabit now returns XPResult from gameStore.awardXP

<!-- Design tokens -->
From src/tokens/:
- colors.dark.primary (#10B981 emerald), colors.dark.background, colors.dark.textPrimary
- typography.headingMd (Press Start 2P), typography.bodySm (Inter)
- spacing.xs/sm/md/lg, radius.sm/md/lg/full

<!-- Animation patterns from research -->
- Reanimated 4: useSharedValue, useAnimatedStyle, withTiming, withSequence, withDelay
- Layout animations: ZoomIn, FadeIn, FadeOut from 'react-native-reanimated'
- NOT React Native Modal -- use absolute-positioned overlay with StyleSheet.absoluteFillObject
- Haptics: expo-haptics impactAsync(Heavy) x3 for level-up, notificationAsync(Success) for title
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: XP feedback components (float label, progress bar, level badge)</name>
  <files>src/components/game/XPFloatLabel.tsx, src/components/game/XPProgressBar.tsx, src/components/game/LevelBadge.tsx</files>
  <action>
    1. Create src/components/game/XPFloatLabel.tsx:
       - Props: { xpText: string; startY: number; startX: number; onDone: () => void }
       - Absolute positioned Animated.Text that floats up ~80px and fades out over 1.2s
       - Use useSharedValue for translateY and opacity
       - translateY: withTiming(-80, { duration: 1200 })
       - opacity: withDelay(600, withTiming(0, { duration: 600 }, finished => { if (finished) runOnJS(onDone)() }))
       - Font: Press Start 2P, color: emerald (#10B981), fontSize ~14
       - Format per locked decision: "15 x 1.5x = +22 XP" (shows cappedXP, not earnedXP)
       - Position via absolute left/top using startX/startY props
       - Rendered in an overlay container at root level (not inside FlatList)

    2. Create src/components/game/XPProgressBar.tsx:
       - Reads from gameStore: totalXP, currentLevel, xpToNext
       - Imports xpForLevel, xpToNextLevel from xp-engine
       - Computes progress: (totalXP - xpForLevel(currentLevel)) / xpToNextLevel(currentLevel)
       - Animated width using useSharedValue + useAnimatedStyle
       - On XP gain: withTiming to new progress (800ms, Easing.out(Easing.cubic))
       - On level-up: withSequence -- fill to 100% (400ms) -> hold (200ms) -> instant reset -> fill to new progress (600ms)
       - Layout: horizontal bar below DailyProgressBar
       - Format per locked decision: "Lv 8  [bar]  340/680 XP"
       - Level number in Press Start 2P (small), XP numbers in Inter
       - Bar: height ~8px, rounded, emerald fill on dark surface, track is colors.dark.surface
       - Use Animated.View for the fill (NOT width animation -- use scaleX or percentage-width inner View per research anti-pattern note)

    3. Create src/components/game/LevelBadge.tsx:
       - Reads from gameStore: currentLevel, activeTitle
       - Format per locked decision: "Lv 8 . The Steadfast"
       - Level in Press Start 2P, title in Inter italic or regular
       - Compact single-line display for header area
       - If no active title, show just "Lv 8"
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Three components created. XPFloatLabel animates upward and fades. XPProgressBar shows animated fill with level-up sequence. LevelBadge displays level and active title. All type-check clean.</done>
</task>

<task type="auto">
  <name>Task 2: Celebration overlays and habits screen integration</name>
  <files>src/components/game/LevelUpOverlay.tsx, src/components/game/TitleUnlockOverlay.tsx, src/components/game/CelebrationManager.tsx, app/(tabs)/habits.tsx</files>
  <action>
    1. Create src/components/game/LevelUpOverlay.tsx:
       - Props: { level: number; copy: string; unlockedTitles: string[]; onContinue: () => void }
       - Full-screen absolute overlay (StyleSheet.absoluteFillObject) with semi-transparent dark backdrop
       - NOT a React Native Modal component (research Pitfall 2)
       - Animated entry: ZoomIn.duration(350).springify() on the content container
       - Large level number in Press Start 2P with glow effect (text shadow)
       - Mentor copy text below level number
       - If unlockedTitles.length > 0, show "Title Unlocked: [name]" section (bundled per locked decision)
       - "Continue" button at bottom -- Pressable, not auto-dismiss (locked decision)
       - Heavy haptic burst on mount: 3x impactAsync(Heavy) with 100ms/80ms delays between
       - zIndex: 999 to float above everything
       - Colors: emerald primary, gold accents, dark backdrop at 0.85 opacity

    2. Create src/components/game/TitleUnlockOverlay.tsx:
       - Props: { title: Title; onEquip: () => void; onLater: () => void }
       - Same overlay pattern as LevelUpOverlay (absolute fill, not Modal)
       - Shows: title name in Press Start 2P, rarity badge (colored: common=white, rare=sapphire, legendary=gold), flavor text
       - "Equip" button (primary emerald) and "Later" button (secondary ghost)
       - Animated entry: FadeIn.duration(300) + ZoomIn on the card
       - Haptic: notificationAsync(Success) on mount

    3. Create src/components/game/CelebrationManager.tsx:
       - Reads pendingCelebrations from gameStore
       - On new celebration appearing, renders appropriate overlay (LevelUpOverlay or TitleUnlockOverlay)
       - On dismiss (Continue/Equip/Later), calls gameStore.consumeCelebration() to dequeue
       - If Equip pressed on title, also calls gameStore.equipTitle()
       - Shows one celebration at a time -- next appears after current is dismissed
       - Renders null when queue is empty

    4. Update app/(tabs)/habits.tsx:
       - Import and add LevelBadge to the header row (between title/date and gear icon)
       - Import and add XPProgressBar below DailyProgressBar
       - Import and add CelebrationManager at the end of the component tree (above modals, so overlay is on top)
       - Add XP float state management:
         a) Add state: floatXP: { text: string; x: number; y: number } | null
         b) When completeHabit returns XPResult, compute float text: "{baseXP} x {multiplier}x = +{cappedXP} XP"
         c) Get card position via ref.measureInWindow to position float correctly
         d) Show XPFloatLabel at card position, clear state on onDone
         e) Pass onComplete callback through HabitList -> HabitCard that returns position info
       - Call gameStore.loadGame(DEFAULT_USER_ID) in useEffect alongside loadDailyState
       - Ensure CelebrationManager renders above all other content with proper zIndex
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Level-up and title-unlock overlays render as full-screen absolute Views (not Modals). CelebrationManager dequeues from pendingCelebrations. Habits screen shows XP bar, level badge, float labels on completion, and celebration overlays on milestones.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">  <name>Task 3: Verify XP feedback UI on device</name>  <files>app/(tabs)/habits.tsx</files>  <action>    Human verifies XP feedback UI on device:    1. Open app on device via Expo Go    2. Go to Habits tab -- verify XP progress bar appears below daily progress bar showing "Lv 1  [bar]  0/40 XP"    3. Verify LevelBadge shows "Lv 1" in header area    4. Complete a habit -- verify floating XP number rises from the card (e.g., "15 x 1.0x = +15 XP") in emerald color    5. Check XP bar animates to reflect new XP total    6. Complete enough habits to reach Level 2 (40 XP) -- verify level-up celebration overlay appears with "The journey has begun. Keep going."    7. Verify "Continue" button dismisses the overlay    8. Verify haptic feedback on completion (existing) and level-up (new heavy burst)    9. If any titles unlock (e.g., "The Willing" at 1 total completion), verify title unlock overlay appears with Equip/Later buttons  </action>  <verify>User types "approved" or describes issues</verify>  <done>XP feedback UI verified on device: float label, progress bar, level badge, and celebration overlays all function correctly.</done></task>

</tasks>

<verification>
1. TypeScript compiles: `npx tsc --noEmit`
2. Existing tests pass: `npm test`
3. Visual verification on device (checkpoint)
</verification>

<success_criteria>
- XP float label shows multiplier breakdown per locked decision format
- XP progress bar animates on gain, handles level-up fill sequence
- LevelBadge shows level + active title per locked decision
- LevelUpOverlay is full-screen absolute View (not Modal), has Continue button, heavy haptics
- TitleUnlockOverlay has Equip/Later buttons, rarity badge
- CelebrationManager dequeues celebrations one at a time
- All rendered on habits screen without breaking existing functionality
</success_criteria>

<output>
After completion, create `.planning/phases/04-game-engine-and-progression/04-03-SUMMARY.md`
</output>
