---
phase: 04-game-engine-and-progression
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/domain/xp-engine.ts
  - src/domain/title-engine.ts
  - src/domain/quest-engine.ts
  - src/domain/quest-templates.ts
  - src/domain/title-seed-data.ts
  - src/domain/level-copy.ts
  - __tests__/domain/xp-engine.test.ts
  - __tests__/domain/title-engine.test.ts
  - __tests__/domain/quest-engine.test.ts
autonomous: true
requirements: [GAME-01, GAME-02, GAME-03, GAME-04, GAME-05, GAME-06]

must_haves:
  truths:
    - "XP calculation applies streak multiplier and soft daily cap correctly"
    - "Level derivation from total XP matches blueprint simulation table"
    - "Title unlock conditions evaluate correctly for all 26 titles"
    - "Quest template selection excludes recently-used and level-gated templates"
    - "Quest progress evaluation increments correctly per completion event"
    - "XP economy simulation matches blueprint targets (Level 5 at ~915 XP, Level 10 at ~7232 XP)"
  artifacts:
    - path: "src/domain/xp-engine.ts"
      provides: "XP calculation, level derivation, soft cap"
      exports: ["calculateXP", "xpForLevel", "xpToNextLevel", "levelForXP", "applySoftCap"]
    - path: "src/domain/title-engine.ts"
      provides: "Title unlock condition checking"
      exports: ["checkTitleUnlocks"]
    - path: "src/domain/quest-engine.ts"
      provides: "Quest template selection and progress evaluation"
      exports: ["selectQuestTemplates", "evaluateQuestProgress"]
    - path: "src/domain/quest-templates.ts"
      provides: "Static quest template pool (20 daily, 8 weekly, 3 stretch)"
      exports: ["QUEST_TEMPLATES"]
    - path: "src/domain/title-seed-data.ts"
      provides: "26 titles from blueprint with unlock conditions"
      exports: ["TITLE_SEED_DATA"]
    - path: "src/domain/level-copy.ts"
      provides: "Mentor copy per level range"
      exports: ["getLevelUpCopy"]
    - path: "__tests__/domain/xp-engine.test.ts"
      provides: "XP engine unit tests"
      min_lines: 80
    - path: "__tests__/domain/title-engine.test.ts"
      provides: "Title engine unit tests"
      min_lines: 40
    - path: "__tests__/domain/quest-engine.test.ts"
      provides: "Quest engine unit tests"
      min_lines: 60
  key_links:
    - from: "src/domain/xp-engine.ts"
      to: "blueprint simulation table"
      via: "xpForLevel cumulative sums matching blueprint values"
      pattern: "xpForLevel\\(5\\).*915"
    - from: "src/domain/title-engine.ts"
      to: "src/domain/title-seed-data.ts"
      via: "TitleCondition interface matching seed data shape"
      pattern: "TitleCondition"
    - from: "src/domain/quest-engine.ts"
      to: "src/domain/quest-templates.ts"
      via: "QuestTemplate interface consumed by selectQuestTemplates"
      pattern: "QuestTemplate"
---

<objective>
Build three pure TypeScript domain modules (xp-engine, title-engine, quest-engine) with full TDD coverage, plus static data files (quest templates, title seeds, level copy). These are the game's core logic -- no React, no DB, no side effects.

Purpose: Establish the game economy as testable pure functions before any wiring or UI. The streak-engine from Phase 3 is the template pattern.
Output: 6 domain files + 3 test files, all tests green.
</objective>

<execution_context>
@C:/Users/jay_p/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jay_p/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-game-engine-and-progression/04-RESEARCH.md

<interfaces>
<!-- Existing pattern to follow -->
From src/domain/streak-engine.ts (template pattern):
- Pure functions, no imports from React or DB
- Types defined in same file or imported from types/
- Exported functions receive data, return data, no side effects

From src/types/database.ts:
```typescript
export type Title = InferSelectModel<typeof titles>;   // { id, name, rarity, unlockType, unlockValue, unlockHabitType, flavorText, sortOrder }
export type Quest = InferSelectModel<typeof quests>;    // { id, userId, type, description, xpReward, targetType, targetValue, targetHabitId, progress, status, expiresAt, completedAt, createdAt }
```

From src/db/schema.ts (titles table):
```typescript
// titles: id, name (unique), rarity, unlockType, unlockValue, unlockHabitType, flavorText, sortOrder
// Rarity values: 'common', 'rare', 'legendary'
// unlockType values: 'onboarding' | 'total_completions' | 'habit_type_streak' | 'habit_streak' | 'level_reach' | 'quest_completions' | 'mercy_recoveries' | 'simultaneous_streaks' | 'muhasabah_streak' | 'habit_count'
```

From src/db/schema.ts (quests table):
```typescript
// quests: id, userId, type, description, xpReward, targetType, targetValue, targetHabitId, progress, status, expiresAt, completedAt, createdAt
// NOTE: No templateId column exists. Use description or add templateId in Plan 02 migration.
// targetType values from blueprint: 'any_completion' | 'habit_type' | 'all_salah' | 'streak_reach' | 'daily_complete_all' | 'muhasabah' | 'total_completions'
```

Blueprint XP formula (authoritative):
- xp_to_next(level) = floor(40 * level^1.85) -- PER-LEVEL cost
- Cumulative must SUM per-level costs
- Level 5 cumulative: ~915 XP
- Level 10 cumulative: ~7,232 XP
- Level 20 cumulative: ~47,816 XP
- Level 50 cumulative: ~427,628 XP
- Level 100 cumulative: ~1,918,688 XP

Locked decisions:
- Streak multiplier: 1.0x base, +0.1x/day, cap 3.0x
- Soft daily XP cap: ~500 XP (50% diminishing returns, invisible)
- XP float shows cappedXP, not earnedXP
- Level-up copy: specific strings locked for levels 5, 10, 20, 50
- Quest Board locked until Level 5
- No repeat of same quest template within 7 days
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: XP engine with TDD (xp-engine.ts + level-copy.ts)</name>
  <files>src/domain/xp-engine.ts, src/domain/level-copy.ts, __tests__/domain/xp-engine.test.ts</files>
  <behavior>
    - xpToNextLevel(1) returns 40
    - xpToNextLevel(2) returns floor(40 * 2^1.85) = ~137 (verify exact)
    - xpForLevel(1) returns 0 (starting level, no XP needed)
    - xpForLevel(2) returns 40
    - xpForLevel(5) returns ~915 (sum of levels 1-4)
    - xpForLevel(10) returns ~7232
    - levelForXP(0) returns 1
    - levelForXP(39) returns 1
    - levelForXP(40) returns 2
    - levelForXP(915) returns 5
    - levelForXP(7232) returns 10
    - applySoftCap(100, 0) returns 100 (under cap)
    - applySoftCap(100, 450) returns 50 + floor(50*0.5) = 75 (straddles cap)
    - applySoftCap(100, 500) returns floor(100*0.5) = 50 (over cap)
    - calculateXP(15, 1.5, 0, 0) returns correct XPResult with earnedXP=22, cappedXP=22, newTotalXP=22, level checks
    - calculateXP detects level-up: didLevelUp=true when XP crosses threshold
    - calculateXP returns previousLevel and newLevel correctly
    - getLevelUpCopy(5) returns "Your discipline grows stronger."
    - getLevelUpCopy(10) returns "A new horizon opens before you."
    - getLevelUpCopy(20) returns "Consistency is becoming your nature."
    - getLevelUpCopy(50) returns "Few have walked this far. MashaAllah."
    - getLevelUpCopy for non-milestone levels returns range-appropriate copy
  </behavior>
  <action>
    RED: Create __tests__/domain/xp-engine.test.ts with all behavior cases above. Import from src/domain/xp-engine.ts and src/domain/level-copy.ts. Run tests -- they must fail (modules don't exist yet).

    GREEN: Implement xp-engine.ts following the research pattern exactly:
    - xpToNextLevel(level): floor(40 * level^1.85)
    - xpForLevel(level): sum of xpToNextLevel for levels 1 through level-1. Level 1 returns 0.
    - levelForXP(totalXP): binary search 1-100
    - applySoftCap(earnedXP, dailyTotal): 500 threshold, 50% diminishing returns above
    - calculateXP(baseXP, multiplier, currentTotalXP, dailyTotalXP): returns XPResult with all fields

    Export XPResult interface.

    Implement level-copy.ts with getLevelUpCopy(level). Locked milestone copy for levels 5, 10, 20, 50 per CONTEXT.md. Range copy for other levels per Claude's discretion.

    Run tests -- all must pass.

    REFACTOR: Clean up if needed. Verify blueprint simulation values match (Level 5 = ~915, Level 10 = ~7232).
  </action>
  <verify>
    <automated>npm test -- --testPathPattern=domain/xp-engine</automated>
  </verify>
  <done>All XP engine tests green. xpForLevel(5) matches ~915 from blueprint. Soft cap and level-up detection work correctly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Title engine and quest engine with TDD</name>
  <files>src/domain/title-engine.ts, src/domain/title-seed-data.ts, src/domain/quest-engine.ts, src/domain/quest-templates.ts, __tests__/domain/title-engine.test.ts, __tests__/domain/quest-engine.test.ts</files>
  <behavior>
    Title engine:
    - checkTitleUnlocks returns empty array when all titles already unlocked
    - checkTitleUnlocks returns title ID when level_reach condition met (e.g., level 10 unlocks a level-based title)
    - checkTitleUnlocks returns title ID for habit_type_streak condition (e.g., 3 consecutive fajr)
    - checkTitleUnlocks returns title ID for total_completions condition
    - checkTitleUnlocks skips already-unlocked titles
    - checkTitleUnlocks handles simultaneous_streaks condition
    - TITLE_SEED_DATA has exactly 26 entries (10 common, 10 rare, 6 legendary)

    Quest engine:
    - selectQuestTemplates returns correct count for daily (3), weekly (2), stretch (1)
    - selectQuestTemplates excludes recently-used template IDs
    - selectQuestTemplates excludes templates above player level
    - selectQuestTemplates filters by player's active habit types for relevance
    - evaluateQuestProgress increments for any_completion type
    - evaluateQuestProgress increments for all_salah when all 5 complete
    - evaluateQuestProgress clamps to targetValue (no overflow)
    - evaluateQuestProgress returns unchanged progress when event doesn't match quest type
    - QUEST_TEMPLATES has 20 daily, 8 weekly, 3 stretch templates
  </behavior>
  <action>
    RED: Create __tests__/domain/title-engine.test.ts and __tests__/domain/quest-engine.test.ts with all behaviors above. Run -- must fail.

    GREEN:

    1. Create src/domain/title-seed-data.ts with all 26 titles from blueprint/04-worldbuilding.md. Structure each entry as: { id (kebab-case slug), name, rarity, unlockType, unlockValue, unlockHabitType (null unless type-specific), flavorText, sortOrder }. Group: 10 Common (sortOrder 1-10), 10 Rare (11-20), 6 Legendary (21-26). Reference the blueprint for exact names, conditions, and flavor text. Export as TITLE_SEED_DATA array.

    2. Create src/domain/title-engine.ts:
    - Define TitleCondition interface: { id, unlockType, unlockValue, unlockHabitType }
    - Define PlayerStats interface: { currentLevel, habitStreaks (Record<string,number>), habitTypes (Record<string,string>), totalCompletions, questCompletions, mercyRecoveries, muhasabahStreak, activeHabitCount, simultaneousStreaks14, simultaneousStreaks90 }
    - isTitleUnlocked(condition, stats): boolean -- switch on unlockType
    - checkTitleUnlocks(conditions, alreadyUnlocked: Set<string>, stats): string[] -- filter and map

    3. Create src/domain/quest-templates.ts with QUEST_TEMPLATES array:
    - 20 daily templates (25-50 XP, minLevel 1-5): "Complete all 5 salah", "Complete 3 habits", "Complete a Quran reading", etc.
    - 8 weekly templates (100-200 XP, minLevel 5-8): "Maintain 7-day streak", "Complete 25 habits this week", etc.
    - 3 stretch templates (300-500 XP, minLevel 8-10): "Complete 50 check-ins this week", "All salah for 5 consecutive days", etc.
    - Each template: { id (string slug), type, description, targetType, targetValue, targetHabitType? (for habit_type quests), minLevel, xpReward }

    4. Create src/domain/quest-engine.ts:
    - Export QuestTemplate interface matching template shape
    - selectQuestTemplates(pool, type, count, playerLevel, activeHabitTypes: Set<string>, recentlyUsedIds: Set<string>): QuestTemplate[] -- filters by type, level, relevance, no-repeat; shuffles and takes count
    - evaluateQuestProgress(quest: {targetType, targetValue, targetHabitType?, progress}, event: {habitType, completionCount?, allSalahComplete, allHabitsComplete, currentStreaks?}): number -- returns new progress value clamped to targetValue
    - isRelevantToPlayer(template, activeHabitTypes): boolean -- checks habit_type templates have matching habits

    Run tests -- all must pass.
  </action>
  <verify>
    <automated>npm test -- --testPathPattern="domain/(title|quest)-engine"</automated>
  </verify>
  <done>All title and quest engine tests green. 26 titles in seed data. 31 quest templates (20+8+3). No-repeat and level-gate logic verified.</done>
</task>

</tasks>

<verification>
Run full domain test suite:
```bash
npm test -- --testPathPattern=domain/
```
All tests pass including existing streak-engine tests.
</verification>

<success_criteria>
- xp-engine: calculateXP, levelForXP, applySoftCap all tested and passing
- title-engine: checkTitleUnlocks verified against seed data conditions
- quest-engine: selectQuestTemplates and evaluateQuestProgress tested
- Blueprint XP targets validated: Level 5 ~915, Level 10 ~7232
- All 26 title seeds present with correct rarity distribution (10/10/6)
- All 31 quest templates present with correct type distribution (20/8/3)
- Zero React or DB imports in any domain file
</success_criteria>

<output>
After completion, create `.planning/phases/04-game-engine-and-progression/04-01-SUMMARY.md`
</output>
