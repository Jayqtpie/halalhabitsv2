---
phase: 04-game-engine-and-progression
plan: 04
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - app/(tabs)/quests.tsx
  - src/components/quests/QuestCard.tsx
  - src/components/quests/QuestSection.tsx
  - src/components/quests/QuestLockedState.tsx
  - src/components/quests/TitleGrid.tsx
  - src/components/quests/QuestBoardHeader.tsx
autonomous: false
requirements: [GAME-04, GAME-05, GAME-03]

must_haves:
  truths:
    - "Quest Board shows daily, weekly, and stretch quests with progress bars"
    - "Quests auto-track progress without accept button"
    - "Completed quests show checkmark and XP reward as mini-trophy until reset"
    - "Quest Board is locked before Level 5 with clear progress indication"
    - "Titles tab shows all 26 titles grouped by rarity with unlock progress"
    - "User can equip an unlocked title from the titles browser"
    - "Locked titles show progress bar toward unlock condition"
    - "Tab toggle switches between Quests and Titles views"
  artifacts:
    - path: "app/(tabs)/quests.tsx"
      provides: "Full Quest Board screen replacing placeholder"
      min_lines: 80
    - path: "src/components/quests/QuestCard.tsx"
      provides: "Individual quest row with progress bar"
      min_lines: 40
    - path: "src/components/quests/QuestSection.tsx"
      provides: "Section header with quest cards"
      min_lines: 30
    - path: "src/components/quests/QuestLockedState.tsx"
      provides: "Level < 5 locked state display"
      min_lines: 25
    - path: "src/components/quests/TitleGrid.tsx"
      provides: "Rarity-grouped title browser with progress bars"
      min_lines: 60
  key_links:
    - from: "app/(tabs)/quests.tsx"
      to: "src/stores/gameStore.ts"
      via: "reads quests, currentLevel, titles, activeTitle"
      pattern: "useGameStore"
    - from: "src/components/quests/TitleGrid.tsx"
      to: "src/stores/gameStore.ts"
      via: "calls equipTitle to change active title"
      pattern: "equipTitle"
    - from: "app/(tabs)/quests.tsx"
      to: "src/components/quests/QuestLockedState.tsx"
      via: "renders locked state when currentLevel < 5"
      pattern: "currentLevel.*<.*5"
---

<objective>
Build the Quest Board screen with quest display, title browser, and locked state. Replace the quests tab placeholder with a fully functional screen featuring tab toggle between Quests and Titles views.

Purpose: Give players concrete goals (quests) and visible progression milestones (titles) -- the long-term motivation layer beyond daily XP.
Output: Quest Board screen with 6 components.
</objective>

<execution_context>
@C:/Users/jay_p/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jay_p/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-game-engine-and-progression/04-RESEARCH.md
@.planning/phases/04-game-engine-and-progression/04-02-SUMMARY.md

<interfaces>
<!-- From gameStore (after Plan 02) -->
```typescript
interface GameState {
  currentLevel: number;
  totalXP: number;
  xpToNext: number;
  titles: UserTitle[];
  activeTitle: Title | null;
  quests: Quest[];
  loadGame: (userId: string) => Promise<void>;
  generateQuests: (userId: string) => Promise<void>;
  equipTitle: (userId: string, titleId: string) => Promise<void>;
}
```

<!-- DB types -->
```typescript
type Quest = { id, userId, type, description, xpReward, targetType, targetValue, targetHabitId, templateId, progress, status, expiresAt, completedAt, createdAt };
type Title = { id, name, rarity, unlockType, unlockValue, unlockHabitType, flavorText, sortOrder };
type UserTitle = { id, userId, titleId, earnedAt };
```

<!-- From title-seed-data (Plan 01) -->
```typescript
export const TITLE_SEED_DATA: Array<{
  id: string; name: string; rarity: 'common'|'rare'|'legendary';
  unlockType: string; unlockValue: number; unlockHabitType: string | null;
  flavorText: string; sortOrder: number;
}>;
```

<!-- From xp-engine (Plan 01) -->
```typescript
export function xpForLevel(level: number): number;
export function xpToNextLevel(level: number): number;
```

Design tokens:
- colors.dark.primary (#10B981 emerald), sapphire (#3B82F6), gold (#F59E0B), ruby (#EF4444)
- colors.dark.surface, colors.dark.surfaceAlt, colors.dark.textPrimary, textSecondary
- typography: headingMd (Press Start 2P), bodyMd (Inter), bodySm
- spacing, radius
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Quest Board components (QuestCard, QuestSection, QuestLockedState, header)</name>
  <files>src/components/quests/QuestCard.tsx, src/components/quests/QuestSection.tsx, src/components/quests/QuestLockedState.tsx, src/components/quests/QuestBoardHeader.tsx</files>
  <action>
    1. Create src/components/quests/QuestBoardHeader.tsx:
       - Tab toggle at top: "Quests" | "Titles" as two Pressable tabs
       - Active tab: emerald underline + primary text color
       - Inactive tab: secondary text color, no underline
       - Props: { activeTab: 'quests' | 'titles'; onTabChange: (tab) => void }
       - Horizontal layout, centered, with spacing between tabs

    2. Create src/components/quests/QuestCard.tsx:
       - Props: { quest: Quest }
       - Card layout: description text, progress bar, progress count ("3/5"), XP reward badge
       - Progress bar: filled portion in emerald, track in surface color
       - Status handling:
         a) 'available' or 'in_progress': normal display with progress bar
         b) 'completed': checkmark icon, emerald "Completed! +{xpReward} XP" text, card has subtle emerald border glow (mini-trophy state per locked decision)
       - No "accept" button anywhere (locked decision: auto-track)
       - XP reward shown as "+{xpReward} XP" badge in secondary style
       - Accessible: accessibilityLabel with quest description and progress

    3. Create src/components/quests/QuestSection.tsx:
       - Props: { title: string; quests: Quest[] }
       - Section header text (e.g., "Daily Quests") in Press Start 2P small
       - Maps quests to QuestCard components
       - Empty state: if no quests in section, show subtle "No {type} quests active" text

    4. Create src/components/quests/QuestLockedState.tsx:
       - Props: { currentLevel: number }
       - Full-screen centered display
       - Lock icon (use Unicode lock character or simple SVG)
       - "Quest Board unlocks at Level 5" in Press Start 2P
       - Progress bar showing current XP toward Level 5: xpForLevel(currentLevel) / xpForLevel(5)
       - Current level indicator: "Currently Lv {currentLevel}"
       - Encouraging copy: "Keep building your discipline. You're getting closer."
       - No shame language (adab-safe)
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>QuestCard renders quest with progress bar and completed trophy state. QuestSection groups quests under header. QuestLockedState shows level progress toward Level 5. QuestBoardHeader has tab toggle.</done>
</task>

<task type="auto">
  <name>Task 2: Title browser and full Quests screen wiring</name>
  <files>src/components/quests/TitleGrid.tsx, app/(tabs)/quests.tsx</files>
  <action>
    1. Create src/components/quests/TitleGrid.tsx:
       - Props: { allTitles: Title[]; unlockedTitleIds: Set<string>; activeTitleId: string | null; playerStats: PlayerStats (partial -- just what's needed for progress display); onEquipTitle: (titleId: string) => void }
       - Grouped by rarity: Common, Rare, Legendary
       - Each group header: rarity name + count "Common 4/10" showing unlocked/total
       - Unlocked titles: name, rarity badge (colored dot or text: common=white, rare=sapphire #3B82F6, legendary=gold #F59E0B), tap to see details
       - Active title: highlighted with emerald accent border
       - Locked titles: name visible but greyed out (textSecondary color), rarity shown
       - Locked title detail on tap: show unlock condition text + progress bar
         - e.g., "Complete 40 consecutive Fajr prayers" with "████────── 12/40"
         - Progress derived from playerStats matching the title's unlockType and unlockHabitType
       - Equip button on unlocked non-active titles: "Equip" Pressable
       - Use FlatList or SectionList for scrollable grid
       - One active title at a time (locked decision) -- tapping Equip on another title switches

    2. Rewrite app/(tabs)/quests.tsx from placeholder to full screen:
       - State: activeTab ('quests' | 'titles')
       - Load game state on mount: call gameStore.loadGame if not already loaded
       - Read from gameStore: currentLevel, quests, titles (all Title[]), userTitles (UserTitle[]), activeTitle

       If currentLevel < 5 AND activeTab === 'quests':
         - Render QuestLockedState with currentLevel
         - Still allow switching to Titles tab (titles are viewable before Level 5)

       If currentLevel >= 5 AND activeTab === 'quests':
         - Call generateQuests on mount/focus if needed
         - Group quests by type: daily, weekly, stretch
         - Render QuestBoardHeader + QuestSection for each group
         - Completed quests stay visible as trophies (locked decision) -- filter by status in each section

       If activeTab === 'titles':
         - Build unlockedTitleIds Set from userTitles
         - Build partial playerStats for progress display (currentLevel, habit streaks from habitStore, etc.)
         - Render QuestBoardHeader + TitleGrid
         - Handle equipTitle callback -> gameStore.equipTitle(userId, titleId)

       Layout: SafeAreaView, ScrollView for content, QuestBoardHeader fixed at top

       Expired quests: silently not shown (locked decision: no "you missed it" message). The generateQuests call handles cleanup.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>TitleGrid shows 26 titles grouped by rarity with progress bars on locked titles. Quests screen has tab toggle, locked state for < Level 5, quest display with sections, and title browser. Equip title works.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">  <name>Task 3: Verify Quest Board and Title browser on device</name>  <files>app/(tabs)/quests.tsx</files>  <action>    Human verifies Quest Board on device:    1. Open app on device via Expo Go    2. Go to Quests tab -- if Level < 5, verify locked state shows "Quest Board unlocks at Level 5" with progress bar    3. Switch to Titles tab -- verify all 26 titles appear grouped by rarity (Common, Rare, Legendary)    4. Verify locked titles are greyed with progress bars showing unlock conditions    5. If any titles are unlocked (e.g., "The Willing"), verify tap shows detail and Equip button works    6. If Level >= 5: verify quest sections (Daily, Weekly, Stretch) show quest cards with progress bars    7. Complete a habit and return to Quests -- verify quest progress incremented    8. Verify no "accept" buttons on quests (auto-track)    9. Verify completed quests show checkmark with emerald "Completed! +XP" state    10. Verify no shame language anywhere on the screen  </action>  <verify>User types "approved" or describes issues</verify>  <done>Quest Board and Title browser verified on device: quest display, title browsing, locked state, and equip all function correctly.</done></task>

</tasks>

<verification>
1. TypeScript compiles: `npx tsc --noEmit`
2. Existing tests pass: `npm test`
3. Visual verification on device (checkpoint)
</verification>

<success_criteria>
- Quest Board replaces placeholder with real quest display
- Three quest sections: Daily (3), Weekly (2), Stretch (1)
- Quest cards show progress bars and XP rewards
- Completed quests are mini-trophies (visible until reset)
- No accept button, no shame on expiry (adab-safe)
- Tab toggle switches between Quests and Titles
- Title browser shows all 26 titles by rarity with count
- Locked titles show progress bars toward unlock
- User can equip unlocked titles
- Locked state shown for Level < 5 with encouraging copy
</success_criteria>

<output>
After completion, create `.planning/phases/04-game-engine-and-progression/04-04-SUMMARY.md`
</output>
